# B1.5.2-R1 — Programs List, Create and Detail Authenticated Browser and Live Authorization Verification

| Field | Value |
| --- | --- |
| Phase | B1.5.2-R1 — Authenticated Browser and Live Authorization Verification |
| Parent track | B1.5 — Programs & Enrollments Workspace |
| Status | Authenticated browser verification complete |
| Baseline commit at R1 close | `a264c7e6e0351cb718a8a96dc28d765ff6173ee4` (`feat(programs): add list, create, and detail workflows`) |
| Branch | `core/platform-readiness-20260707` |
| Authoritative contract | `docs/phases/B1.5-programs-and-enrollments-workspace-design-security-and-readiness-contract.md` |
| Implementation report | `docs/phases/B1.5.2-programs-list-create-and-detail-workflows.md` |
| Independent QA report | `docs/phases/B1.5.2-QA-programs-list-create-and-detail-workflows-independent-qa.md` |
| R1 mutation | This report only (no source/test edits; no Auth/membership/migration/deploy mutation) |

---

## 1. Executive Summary

B1.5.2-R1 verified Programs list, create, and read-only detail against the dedicated **ZyntixAI Production QA** organization on a local worktree runtime (`http://127.0.0.1:3014`). Human operator completed Owner, Staff, and Viewer authenticated browser journeys. Agent-assisted Owner session additionally exercised list filters, create validation, controlled create, detail, and refresh before Staff handoff. Staff and Viewer create denial and read access were confirmed by the operator (Staff also confirmed earlier in-session for `/programs/new`). Post-browser automated regression remained green (17 Programs files / 85 tests; full suite 162 / 1067; typecheck PASS). No Admin live account existed; classified non-blocking. True empty-org empty-state and multi-page pagination were not directly observable due to existing Programs volume — non-blocking.

---

## 2. Final Verdict

```text
PASS WITH CONDITIONS — READY FOR B1.5.2 PUBLICATION
```

---

## 3. Git Baseline

| Item | Value |
| --- | --- |
| Worktree | `D:/project ai cursus verkopers.worktrees/parallel__laptop-product-track-20260707-1` |
| Branch | `core/platform-readiness-20260707` |
| HEAD at R1 close | `a264c7e6e0351cb718a8a96dc28d765ff6173ee4` |
| Subject | `feat(programs): add list, create, and detail workflows` |
| Upstream | `origin/core/platform-readiness-20260707` at same SHA |
| Divergence | `0 0` |
| Commit/push during R1 report phase | **None** (report only). Product commit `a264c7e` was created earlier by explicit operator commit-and-push of B1.5.2 implementation + QA docs |
| R1 source/test edits | **None** |

---

## 4. Runtime Command and URL

| Item | Value |
| --- | --- |
| Build/runtime | Worktree production build served via `npx next start -p 3014` |
| Base URL | `http://127.0.0.1:3014` |
| Hosted Auth / data | Same Supabase project used by local `.env.local` (public keys only; no passwords in agent environment) |
| Production alias used as primary proof | **No** (local worktree runtime required for undeployed-at-start B1.5.2 UI) |

---

## 5. Unauthenticated Route Results

| Route | Result |
| --- | --- |
| `GET /programs` | **307** → `/login?next=%2Fprograms` |
| `GET /programs/new` | **307** → `/login?next=%2Fprograms%2Fnew` |
| `/login` | **200**, email/password form present |

Safe `next` return path behavior matched existing protected-route patterns.

---

## 6. QA Role-Account Evidence

Dedicated organization: **ZyntixAI Production QA** (`zyntixai-production-qa`).

| Role | Availability | Live journey |
| --- | --- | --- |
| Owner | Present (1) | Executed |
| Staff | Present (1) | Executed |
| Viewer | Present (1) | Executed |
| Admin | Absent (0) | `ADMIN LIVE JOURNEY NOT EXECUTED` |

No Auth users, memberships, or roles were created or changed during R1. Credentials were entered only by the human operator in the browser; never requested, printed, stored, or reported by the agent.

---

## 7. Owner Login Evidence

**Source:** Human operator confirmation + prior agent-assisted Owner session.

- Login succeeded.
- Organization resolved to **ZyntixAI Production QA**.
- Primary nav visible: Home → Leads → Customers → **Programs** → Tasks.
- No Enrollments navigation link.

---

## 8. Owner Programs-List Results

- `/programs` opened successfully.
- **New program** CTA visible (Owner).
- **Show archived programs** filter present (Owner/admin capability).
- List showed org Programs including the controlled fixture after create.
- Agent session observed count progression from **Showing 2 of 2** (pre-create) to **Showing 3 of 3** (post-create).

---

## 9. Search / Filter / Sort / Pagination Results

Agent-assisted Owner checks (local `:3014`):

| Check | Result |
| --- | --- |
| Search match (`q=guzzu3`) | Narrowed to 1 of 1 |
| Impossible search | Empty match copy + Clear filters affordance |
| `status=draft` | Draft programs retained; filter UI showed Draft |
| `deliveryMode=self_paced&sort=name&direction=asc` | Self-paced only; name ascending |
| Malformed query (`status=bogus&sort=secret&direction=sideways&page=0`) | Safe defaults + **Filters adjusted** / invalid-filter reset message |
| Multi-page pagination | **Not exercised** — fixture volume remained on a single page (non-blocking condition) |

---

## 10. Empty-State Evidence Classification

True empty-organization Programs empty-state was **not directly observable** because the QA org already contained Programs before R1 and retained the controlled fixture after create. Automated list empty-state coverage remains in unit tests. Classified **non-blocking**.

---

## 11. Create-Form Results

Owner `/programs/new`:

- Form opened with required **Program name**, required **Delivery mode** (default Self-paced), optional **Description**.
- Create / Cancel / Back to programs present.
- No edit/lifecycle/archive/enrollment controls on create surface.

---

## 12. Validation Result

Empty submit (agent-assisted):

- Remained on `/programs/new`.
- Banner: **Please correct the highlighted fields and try again.**
- Field error: **Program name is required.**
- No Program created by the failed attempt.

---

## 13. Create Mutation Result

```text
ONE QA PROGRAM CREATED
```

| Field | Value |
| --- | --- |
| Name | `B1.5.2 QA Test Program` |
| Delivery mode | Self-paced |
| Description | Controlled B1.5.2-R1 QA fixture. Retain for B1.5.3. Do not archive or mutate outside R1 scope. |
| Resulting status | Draft |
| Post-create landing | Program detail URL under `/programs/[programId]` |
| Retain policy | **Retain** for B1.5.3 (do not archive/edit/lifecycle outside later approved phases) |

Human operator independently confirmed exactly one controlled QA Program was created or the existing controlled fixture was reused; no second Program was created during R1.

---

## 14. Program Detail Results

Owner (agent + human) and Staff/Viewer (human; Staff also agent-assisted):

- Detail opened for the controlled fixture.
- Displayed values matched create inputs: name, Self-paced, Draft, description, archive status Not archived, open enrollments **0**.
- Interactive controls limited to navigation + **Log out** / **Back to programs**.
- **No** Edit, lifecycle transition, archive, restore, Enrollment management, or Progress controls.

---

## 15. History and Enrollment-Count Results

- Status history showed initial **Set to Draft** (Manual) entry with timestamp.
- Open enrollment count displayed as **0** with deferred enrollment/progress clarifying copy.
- No enrollment management UI.

---

## 16. Soft-Refresh Result

Owner soft re-navigation to the same detail URL: detail remained correct (human + agent). **PASS**.

---

## 17. Hard-Refresh Result

Owner hard reload of detail: detail remained correct (human + agent). **PASS**.

---

## 18. Logout and Returning-Login Result

Owner logout returned to `/login`. Returning login succeeded (human). Controlled Program remained visible afterward (human). **PASS**.

---

## 19. Staff Login and Read Results

**Source:** Human operator confirmation (+ agent-assisted Staff session for list/detail/`/programs/new`).

- Login succeeded; same QA org resolved.
- Programs list readable (**Showing 3 of 3** including fixture).
- Program detail readable; no mutation controls.
- **New program** CTA **not** visible.
- Archived filter **not** shown (Owner/admin-only).
- No visible RLS / authorization / tenant-isolation error.

---

## 20. Staff Direct `/programs/new` Denial

- Direct navigation to `/programs/new` did **not** expose a functional create form.
- UI: **Create program unavailable** — “You do not have permission to create programs.”
- **PASS**.

---

## 21. Viewer Login and Read Results

**Source:** Human operator confirmation (`MANUAL BROWSER VERIFICATION COMPLETE`).

- Login succeeded; same QA org resolved.
- Programs list and Program detail readable.
- **New program** CTA not visible.
- No mutation controls.
- No visible RLS / authorization / tenant-isolation error.
- **PASS**.

---

## 22. Viewer Direct `/programs/new` Denial

- Direct `/programs/new` access safely denied or unavailable; no functional create form exposed (human).
- **PASS**.

---

## 23. Admin Live-Journey Classification

```text
ADMIN LIVE JOURNEY NOT EXECUTED
```

**Non-blocking** because:

1. Owner proved the positive create journey in browser.
2. Automated tests prove Admin create capability (create-action / permissions matrix).
3. Staff and Viewer proved negative create journeys in browser.
4. No QA Admin membership existed; creating one was forbidden in R1.

---

## 24. Tenant-Isolation Evidence

- All accounts resolved the same dedicated QA organization (human).
- No other-tenant data observed (human).
- List/detail remained scoped to Production QA Programs only in agent-assisted sessions.
- Cross-tenant UUID unavailable probe was not separately forced beyond existing automated coverage; no contradictory live evidence appeared.

---

## 25. Authenticated / RLS-Path Observations

- Authenticated Programs routes loaded through ordinary membership-resolved org context.
- Create mutation succeeded only for Owner in live browser.
- Staff/Viewer create surfaces denied without leaking raw DB errors.
- No service-role usage introduced during R1.
- No Auth/membership/role changes during R1.

---

## 26. Exact Mutation Inventory

| Mutation class | Result |
| --- | --- |
| Programs created | **1** controlled fixture (`B1.5.2 QA Test Program`) — `ONE QA PROGRAM CREATED` |
| Programs edited | **0** |
| Program status / lifecycle changes | **0** (remained Draft) |
| Programs archived / restored | **0** |
| Enrollments created | **0** |
| Leads / Customers / Tasks changed | **0** |
| Auth users / memberships / roles changed | **0** |
| Organizations changed | **0** |
| Migrations / deploys | **0** |
| Source / test file edits during R1 report phase | **0** |

---

## 27. Post-Browser Regression Results

### Targeted Programs tests (exact 17 files)

Command:

```text
npx vitest run tests/domain/program-status.test.ts tests/domain/program-permissions.test.ts tests/domain/program-navigation-and-routes.test.ts tests/domain/program-mutation-interpretation.test.ts tests/validation/program-schemas.test.ts tests/server/normalize-program-error.test.ts tests/server/program-rpc-adapters.test.ts tests/server/map-program-read-model.test.ts tests/server/load-program-foundations.test.ts tests/server/program-create-mutation.test.ts tests/actions/program-create-action.test.ts tests/ui/program-list-search-params.test.ts tests/ui/program-page-loaders.test.ts tests/ui/program-list-presentation.test.tsx tests/ui/program-create-workflow.test.tsx tests/ui/program-detail-presentation.test.tsx tests/ui/programs-navigation.test.tsx
```

| Metric | Result |
| --- | --- |
| Exit code | **0** |
| Test files | **17** passed |
| Tests | **85** passed |

### Full suite

| Command | Exit | Result |
| --- | --- | --- |
| `npm run test:run` | **0** | **162** files / **1067** tests passed |
| `npm run typecheck` | **0** | PASS (`tsc --noEmit`) |

Non-secret note: npm `Unknown env config "devdir"` warning only — non-blocking.

---

## 28. Credential-Handling Confirmation

- No credentials entered into Cursor chat.
- No credentials printed in terminal output or this report.
- Operator entered passwords only in the browser login form.
- Agent did not request passwords after the human completed manual journeys.

---

## 29. Limitations and Conditions

1. **`ADMIN LIVE JOURNEY NOT EXECUTED`** — Admin membership absent; non-blocking (Owner positive + automated Admin create + Staff/Viewer negative).
2. **True empty-org Programs empty-state not directly observed** — QA org already had Programs; covered by automated UI tests.
3. **Multi-page pagination not exercised** — only three Programs on one page after controlled create.
4. Cross-tenant unavailable UUID probe relied primarily on automated coverage plus absence of contradictory live leakage (no other-tenant data visible).

None of the above blocks publication readiness under R1 verdict rules.

---

## 30. Publication-Readiness Conclusion

Authenticated Owner/Staff/Viewer browser evidence, controlled create inventory, refresh/returning-login, create denial, and green post-browser regression are sufficient for **B1.5.2 controlled publication**. Do not start B1.5.3 edit/lifecycle/archive/restore until B1.5.2-PUB completes per track process. Retain the controlled fixture `B1.5.2 QA Test Program` for B1.5.3.

---

## 31. Exact Next Safe Phase

```text
NEXT SAFE PHASE:
B1.5.2-PUB — Programs List, Create and Detail Workflows Controlled Publication
```

---

## Appendix — Evidence Sources

| Source | Use |
| --- | --- |
| Human operator `MANUAL BROWSER VERIFICATION COMPLETE` statement | Authoritative Owner/Staff/Viewer matrix completion, tenant isolation, mutation discipline |
| Agent-assisted Owner session on `:3014` | List filters, validation fail, `ONE QA PROGRAM CREATED`, detail/history, soft/hard refresh, logout |
| Agent-assisted Staff session on `:3014` | List/detail read, absent create CTA, `/programs/new` denial copy |
| Pre-R1 SQL role inventory (read-only) | Owner 1 / Staff 1 / Viewer 1 / Admin 0 |
| Post-browser vitest + typecheck | Regression gate |

## Appendix — R1 Mutation Statement

This R1 phase created **only** this report (plus the single permitted controlled Program create during Owner browser verification earlier in the phase). Application source and tests were **not** modified during R1 report completion. No Auth/membership/migration/deploy mutation occurred. No commit or push occurred as part of writing this report.
