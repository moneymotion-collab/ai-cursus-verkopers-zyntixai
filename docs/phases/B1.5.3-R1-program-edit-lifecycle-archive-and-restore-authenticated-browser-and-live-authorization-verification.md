# B1.5.3-R1 — Program Edit, Lifecycle, Archive and Restore Authenticated Browser and Live Authorization Verification

| Field | Value |
| --- | --- |
| Phase | B1.5.3-R1 — Authenticated Browser and Live Authorization Verification |
| Parent track | B1.5 — Programs & Enrollments Workspace |
| Status | Authenticated browser verification complete |
| Baseline commit at R1 close | `14429e8dfc88f73ad28e59245d9233935e9cb71c` (`docs(programs): close B1.5.2 publication`) |
| Branch | `core/platform-readiness-20260707` |
| Authoritative contract | `docs/phases/B1.5-programs-and-enrollments-workspace-design-security-and-readiness-contract.md` |
| Implementation report | `docs/phases/B1.5.3-program-edit-lifecycle-archive-and-restore.md` |
| Independent QA report | `docs/phases/B1.5.3-QA-program-edit-lifecycle-archive-and-restore-independent-qa.md` |
| R1 mutation | This report only (no source/test edits; no Auth/membership/migration/deploy mutation) |

---

## 1. Executive Summary

B1.5.3-R1 verified Program edit, lifecycle, archive, and restore against the dedicated **ZyntixAI Production QA** organization on a local worktree production runtime (`http://127.0.0.1:3014`). Human operator completed Owner, Staff, and Viewer authenticated browser logins. Agent-assisted Owner session completed controlled edit, the live lifecycle sequence `active → paused → active → retired → active`, archive cancel-then-confirm, archived edit/status denial, and restore. Staff and Viewer confirmed list/detail read with mutation CTAs absent and direct mutation-route denials. Post-browser regression remained green (23 Programs files / 123 tests; full suite 168 / 1105; typecheck/lint/`git diff --check` PASS). No Admin live account existed; open-enrollment archive blocking was not directly observable on the controlled fixture (0 open enrollments) — both classified non-blocking conditions.

---

## 2. Final Verdict

```text
PASS WITH CONDITIONS — READY FOR B1.5.3 PUBLICATION
```

Operational verdict:

```text
B1.5.3-R1 PASS WITH CONDITIONS — READY FOR PUBLICATION
```

---

## 3. Git Baseline

| Item | Value |
| --- | --- |
| Worktree | `D:/project ai cursus verkopers.worktrees/parallel__laptop-product-track-20260707-1` |
| Branch | `core/platform-readiness-20260707` |
| HEAD at R1 close | `14429e8dfc88f73ad28e59245d9233935e9cb71c` |
| Subject | `docs(programs): close B1.5.2 publication` |
| Upstream | `origin/core/platform-readiness-20260707` at same SHA |
| Divergence | `0 0` |
| Staged | `0` |
| Commit/push during R1 | **None** |
| R1 source/test edits | **None** (dirty B1.5.3 implementation/QA paths pre-existed; R1 added only this report) |

---

## 4. Runtime and Unauthenticated Routes

| Item | Value |
| --- | --- |
| Build/runtime | Worktree production build via `npx next build` then `npx next start -p 3014 -H 127.0.0.1` |
| Base URL | `http://127.0.0.1:3014` |
| Why production server | Earlier `next dev` Fast Refresh / session drops on edit POST; production start held Owner mutation sessions |
| Hosted Auth / data | Same Supabase project as local `.env.local` (public keys only; no passwords in agent environment) |
| Production alias as primary proof | **No** |

| Route | Result |
| --- | --- |
| `GET /programs` (unauthenticated) | **307** → login with `next=/programs` (Location host may show `localhost:3014`; browser sessions used `127.0.0.1` only) |
| `GET /programs/.../edit` (unauthenticated) | **307** → login with safe `next` return path |
| `/login` | **200**, email/password form present |

---

## 5. QA Account State

Dedicated organization: **ZyntixAI Production QA**.

| Role | Availability | Live journey |
| --- | --- | --- |
| Owner | Present (1) | Executed |
| Staff | Present (1) | Executed |
| Viewer | Present (1) | Executed |
| Admin | Absent (0) | `ADMIN LIVE JOURNEY NOT EXECUTED` |

No Auth users, memberships, or roles were created or changed during R1. Credentials were entered only by the human operator in the browser; never requested, printed, stored, or reported by the agent.

---

## 6. Resolved Role Evidence

| Session | Evidence |
| --- | --- |
| Owner | **New program** CTA present; **Show archived programs** filter present; Edit / Change status / Archive actions on active non-archived detail |
| Staff | No **New program**; no archived filter; detail readable with empty mutation action set |
| Viewer | Same read-only presentation as Staff |

All three sessions resolved **ZyntixAI Production QA**. No Enrollments navigation link observed.

---

## 7. Owner Edit Results

Controlled fixture (pre-edit UI name `B1.5.2 qa test 100`, active, self-paced, not archived, 0 open enrollments):

| Field | Result |
| --- | --- |
| Empty-name validation | Observed earlier in R1 (client/server validation retained description) |
| Name | Updated to `B1.5.2 QA Test Program` |
| Delivery mode | Updated to **Cohort** |
| Description | Updated to `Controlled B1.5.3 QA fixture for edit, lifecycle, archive, and restore verification` |
| Pending UI | **Saving changes…** observed |
| Redirect | Returned to Program detail; session retained on production runtime |

---

## 8. Validation and Persistence Results

| Check | Result |
| --- | --- |
| Soft refresh (re-navigate detail) | Name / Cohort / description / Active / Not archived persisted |
| Hard refresh | Detail reopened after tab recycle; Owner mutation controls and R1 history remained |
| DB confirmation | Active, cohort, not archived, controlled description |

---

## 9. Lifecycle Sequence and Results

Starting from **active**, Owner executed:

| Transition | UI pending | History row |
| --- | --- | --- |
| active → paused | Updating status… | Active → Paused · Manual · reason recorded |
| paused → active | Updating status… | Paused → Active · Manual · reason recorded |
| active → retired | Updating status… | Active → Retired · Manual · reason recorded |
| retired → active | Updating status… | Retired → Active · Manual · reason recorded |

Permitted options matched current status (e.g. from Active: Paused/Retired; from Retired: Active only). Soft-archive remained a separate action. End lifecycle status after sequence: **active**.

---

## 10. Archive Results

| Step | Result |
| --- | --- |
| Cancel | Left archive page without confirming; program remained not archived / Active |
| Confirm | **Archiving…** then detail showed **Archived**, lifecycle still **Active**, **Restore program** CTA present |
| Open enrollments | Remained **0**; archive succeeded |

---

## 11. Archived Edit / Transition Denial

| Direct route while archived | Result |
| --- | --- |
| `/edit` | **Edit program unavailable** — Archived programs cannot be edited |
| `/status` | **Change status unavailable** |

No functional edit/status forms were exposed while archived.

---

## 12. Restore Results

| Step | Result |
| --- | --- |
| Confirm restore | **Restoring…** then detail returned **Not archived** |
| Lifecycle | Remained **Active** (no silent status change) |
| Post-restore actions | Edit / Change status / Archive again available |

---

## 13. Final Controlled Fixture State

| Field | Value |
| --- | --- |
| Name | `B1.5.2 QA Test Program` |
| Status | `active` |
| Delivery mode | `cohort` |
| Description | Controlled B1.5.3 QA fixture for edit, lifecycle, archive, and restore verification |
| Archived | **No** |
| Open enrollments | **0** |

Retain this fixture for publication / later phases; do not mutate outside authorized process.

---

## 14. Staff Read Results

- Programs list readable (**Showing 3 of 3**, fixture included with Cohort / Active).
- Program detail readable including status history from Owner R1 transitions.
- No Edit / Change status / Archive / Restore links.
- No New program; no archived filter.
- No visible RLS / authorization / tenant-isolation error dump.

---

## 15. Staff Direct-Route Denial

| Route | Result |
| --- | --- |
| `/edit` | **Edit program unavailable** — You do not have permission to edit this program |
| `/status` | **Change status unavailable** |
| `/archive` | **Archive program unavailable** |
| `/restore` | **Restore program unavailable** |

No mutation forms exposed. Fixture unchanged after Staff denial probes.

---

## 16. Viewer Read Results

- Same QA org; list/detail readable; history visible.
- No mutation CTAs; no New program; no archived filter.
- No visible authorization/tenant leakage.

---

## 17. Viewer Direct-Route Denial

| Route | Result |
| --- | --- |
| `/edit` | Permission denial (unavailable; no form) |
| `/status` / `/archive` / `/restore` | Unavailable; no forms |
| Cross-tenant Program `/edit` | **Program unavailable** (indistinguishable access failure) |

Fixture unchanged after Viewer probes.

---

## 18. Admin Live-Journey Classification

```text
ADMIN LIVE JOURNEY NOT EXECUTED
```

**Non-blocking** because Owner proved positive edit/lifecycle/archive/restore in browser; automated tests cover Admin capability; Staff/Viewer proved negatives; creating an Admin membership was forbidden in R1.

---

## 19. Open-Enrollment Archive-Block Classification

```text
NOT DIRECTLY OBSERVED
```

Controlled fixture had **0** open enrollments throughout. Archive succeeded when open count was zero. Live open-enrollment block message was not exercised; covered by automated/domain/RPC evidence from B1.5.3/QA — non-blocking.

---

## 20. Tenant-Isolation Evidence

- All live sessions scoped to **ZyntixAI Production QA**.
- Invalid ID shape and nonexistent UUID → **Program unavailable**.
- Foreign-org Program UUID on `/edit` (Viewer) → same **Program unavailable** copy (no other-tenant name leakage).
- No other-tenant Programs appeared in QA list.

---

## 21. Out-of-Scope Leads Observation

- Primary nav retained Leads / Customers / Tasks; **no Enrollments** link.
- R1 did not mutate Leads, Customers, Tasks, or Enrollments.
- Program detail continued to state enrollment management is later-phase awareness only.
- No delete wording observed on edit/lifecycle/archive/restore surfaces.

---

## 22. Exact Mutation Inventory

| Mutation class | Result |
| --- | --- |
| Programs created | **0** |
| Programs edited | **1** controlled fixture (name, delivery mode, description) |
| Program status / lifecycle changes | **4** transitions on the controlled fixture (active↔paused, active↔retired) |
| Programs archived | **1** (after cancel-no-op) |
| Programs restored | **1** |
| Enrollments created / changed | **0** |
| Leads / Customers / Tasks changed | **0** |
| Auth users / memberships / roles changed | **0** |
| Organizations changed | **0** |
| Migrations / deploys | **0** |
| Source / test file edits during R1 | **0** |
| Reports created during R1 | **1** (this file) |

---

## 23. Post-Browser Regression Results

### Targeted Programs tests (exact 23 files)

| Metric | Result |
| --- | --- |
| Exit code | **0** |
| Test files | **23** passed |
| Tests | **123** passed |

### Full suite and gates

| Command | Exit | Result |
| --- | --- | --- |
| Full vitest run | **0** | **168** files / **1105** tests passed |
| `tsc --noEmit` | **0** | PASS |
| eslint (Programs paths) | **0** | PASS |
| `git diff --check` | **0** | PASS |

Non-secret note: npm `Unknown env config "devdir"` warning only — non-blocking.

---

## 24. Credential-Handling Confirmation

- No credentials entered into Cursor chat.
- No credentials printed in terminal output or this report.
- Operator entered passwords only in the browser login form.
- Agent did not request passwords after human login checkpoints.

---

## 25. Limitations and Conditions

1. **`ADMIN LIVE JOURNEY NOT EXECUTED`** — Admin membership absent; non-blocking.
2. **Open-enrollment archive block `NOT DIRECTLY OBSERVED`** — fixture open count remained 0; non-blocking.
3. **Staff/Viewer live view while archived** — Owner archived then restored before Staff/Viewer sessions; archived hide for Staff/Viewer was not live-observed in those sessions (Owner archive UI + Owner-only archived filter + restore path proven). Non-blocking.
4. Unauthenticated redirect `Location` may advertise `localhost:3014` even when binding is `127.0.0.1` — sessions stayed on `127.0.0.1`; non-blocking cookie-host note.
5. Cursor browser tooling hydration noise on login (`data-cursor-ref`) — tooling artifact; ignored; no source change.

None of the above blocks publication readiness under R1 verdict rules.

---

## 26. Publication-Readiness Conclusion

Authenticated Owner/Staff/Viewer browser evidence for edit, lifecycle, archive, restore, denials, tenant-unavailable handling, and green post-browser regression are sufficient for **B1.5.3 controlled publication**. Retain the controlled fixture `B1.5.2 QA Test Program` in Active / not archived state.

---

## 27. Exact Next Safe Phase

```text
NEXT SAFE PHASE:
B1.5.3-PUB — Program Edit, Lifecycle, Archive and Restore Controlled Publication
```

---

## Appendix — Evidence Sources

| Source | Use |
| --- | --- |
| Human `QA OWNER LOGIN COMPLETE` / `QA STAFF LOGIN COMPLETE` / `QA VIEWER LOGIN COMPLETE` | Role handoffs |
| Agent-assisted Owner session on production `:3014` | Edit, lifecycle, archive cancel/confirm, archived denial, restore |
| Agent-assisted Staff/Viewer sessions | Read + direct-route denials + cross-tenant unavailable |
| Read-only SQL role/fixture checks | Account inventory; fixture state confirmation |
| Post-browser vitest + typecheck + lint + diff check | Regression gate |

## Appendix — R1 Mutation Statement

This R1 phase created **only** this report. Application source and tests were **not** modified during R1. No Auth/membership/migration/deploy mutation occurred. No commit or push occurred as part of writing this report. Pre-existing dirty B1.5.3 implementation and QA report paths remain in the worktree pending publication process.
