# B1.6.3 — Controlled Progress Mutations: Implementation Evidence

| Field | Value |
| --- | --- |
| Phase | **B1.6.3 — Controlled Progress Mutations** |
| Document type | Implementation evidence (local gates) |
| Date | 2026-07-28 |
| Governing standard | `docs/governance/B1-GATE.1-100-percent-phase-completion-and-evidence-standard.md` |
| Preflight | `docs/phases/B1.6.3-design-security-scope-and-implementation-readiness.md` |
| Start baseline | `5e6c67b1b349cc1b40d8c114f204c51262b67ca0` |
| Branch | `core/platform-readiness-20260707` |
| Parallel | **B1.6.3 PARALLEL BLOCKED** — desktop-only; laptop frozen |
| Status | **B1.6.3 IMPLEMENTED — READY FOR CONTROLLED BROWSER VERIFICATION** |

---

## 1. Scope executed

### In-scope delivered

- `/progress/new` — record manual progress fact
- `/progress/[factId]/void` — destructive void confirmation with required reason
- `/progress/[factId]/correct` — correction via `record_progress_fact` + `correctedFromFactId` + idempotency key
- `"use server"` actions: `recordProgressFactAction`, `voidProgressFactAction`, `correctProgressFactAction`
- Progress-owned enrollment options for create (active/paused, non-archived)
- Role-aware CTAs on list (Record) and detail (Void / Correct)
- Revalidate `/progress` and fact detail paths on committed mutations
- Targeted tests + full suite green

### Out-of-scope (unchanged)

Restore, delete, generic UPDATE, PE deferral (B1.6.4), AI/NBA/health/Attention, schema/RPC/RLS/generated types, laptop work.

### Allowlist compliance

Touched files match the published preflight allowlist, plus:

- `src/app/(authenticated)/progress/page.module.css` (Record CTA styles)
- `tests/helpers/progress-test-fixtures.ts` (shared fixtures for new tests)

No denylist paths modified.

---

## 2. Flows

| Flow | Mechanism |
| --- | --- |
| Record | Form → `recordProgressFactAction` → `recordProgressFactMutation` → `record_progress_fact` |
| Void | Confirmation + reason → `voidProgressFactAction` → `voidProgressFactMutation` → `void_progress_fact` |
| Correct | Form + auto idempotency key → `correctProgressFactAction` → `recordProgressFactMutation` with `correctedFromFactId` |

Server always re-resolves organization membership before mutating. UI visibility is hint-only.

---

## 3. Role matrix (local)

| Role | Record CTA | Void/Correct CTA | Action |
| --- | --- | --- | --- |
| owner / admin | Yes | When enrollment gates allow | Allowed via foundation + RPC |
| staff | Yes | Only active/paused non-archived | Same |
| viewer | No | No | Denied without RPC |
| unauthenticated | Protected | Protected | Auth required |

---

## 4. Local gates

| Gate | Result |
| --- | --- |
| Targeted Progress mutation + PE loaders | **92 passed** |
| Full suite | **214 files / 1483 tests passed** |
| Typecheck | PASS |
| Lint | PASS |
| Production build | PASS (`/progress/new`, `/void`, `/correct` present) |
| `git diff --check` | PASS |

---

## 5. Conditions

| ID | Status |
| --- | --- |
| Open blockers | **0** |
| Open mandatory conditions | **0** |
| Accepted polish | Exact confirmation/success copy (P1) — functionally clear |

---

## 6. Rollback

- Pre-promote: revert publication commits
- Post-promote: promote prior Ready Vercel deployment
- No DB migration

---

## 7. Interim verdict

```text
B1.6.3 IMPLEMENTED — READY FOR CONTROLLED BROWSER VERIFICATION
```
