# B1.5.3-QA — Program Edit, Lifecycle, Archive and Restore Independent QA

| Field | Value |
| --- | --- |
| Phase | B1.5.3-QA — Independent QA, Security and Regression Verification |
| Parent implementation | `docs/phases/B1.5.3-program-edit-lifecycle-archive-and-restore.md` |
| Parent contract | `docs/phases/B1.5-programs-and-enrollments-workspace-design-security-and-readiness-contract.md` |
| Baseline HEAD | `14429e8dfc88f73ad28e59245d9233935e9cb71c` (`docs(programs): close B1.5.2 publication`) |
| Branch | `core/platform-readiness-20260707` |
| QA verdict | `PASS WITH CONDITIONS — READY FOR B1.5.3-R1` |
| Publication status | **Not published** — ready for authenticated browser / live authorization verification |

---

## 1. Executive Summary

Independent QA confirms B1.5.3 implements contract-aligned Program edit, lifecycle, archive and restore on the published B1.5.1/B1.5.2 foundation. Mutations use typed SECURITY DEFINER RPCs with membership-derived organization and role; Staff/Viewer fail closed at presentation, loader, action and mutation layers; no direct Program table writes or service-role client were found. Automated regression matches the expected baseline (23 Programs files / 123 tests; 168 / 1105 overall). Authenticated browser and live RPC authorization evidence remain deferred to B1.5.3-R1 by design.

## 2. Final QA Verdict

```text
PASS WITH CONDITIONS — READY FOR B1.5.3-R1
```

## 3. Git Baseline

| Item | Value |
| --- | --- |
| Worktree | `D:\project ai cursus verkopers.worktrees\parallel__laptop-product-track-20260707-1` |
| Repository root | `D:/project ai cursus verkopers.worktrees/parallel__laptop-product-track-20260707-1` |
| Branch | `core/platform-readiness-20260707` |
| HEAD | `14429e8dfc88f73ad28e59245d9233935e9cb71c` |
| HEAD subject | `docs(programs): close B1.5.2 publication` |
| Upstream | `origin/core/platform-readiness-20260707` @ same SHA |
| Upstream subject | `docs(programs): close B1.5.2 publication` |
| Divergence | `0 0` |
| Staged count | `0` |
| Deleted count | `0` |
| Renamed count | `0` |
| Conflicts | `0` |
| Commit / push during QA | None |

## 4. Pre-QA Dirty Inventory

All dirty paths classified as exclusively B1.5.3-scoped. Implementation report present; QA report absent at preflight.

| Path | Classification |
| --- | --- |
| `src/app/(authenticated)/programs/[programId]/edit/page.tsx` | Program edit route |
| `src/app/(authenticated)/programs/[programId]/status/page.tsx` | Program lifecycle route |
| `src/app/(authenticated)/programs/[programId]/archive/page.tsx` | Program archive route |
| `src/app/(authenticated)/programs/[programId]/restore/page.tsx` | Program restore route |
| `src/app/(authenticated)/programs/[programId]/page.tsx` | Programs detail/list integration |
| `src/features/programs/ui/program-edit-form.tsx` | Program edit UI |
| `src/features/programs/ui/program-status-form.tsx` | Program lifecycle UI |
| `src/features/programs/ui/program-status-transition-copy.ts` | Program lifecycle UI |
| `src/features/programs/ui/program-archive-form.tsx` | Program archive/restore UI |
| `src/features/programs/ui/program-restore-form.tsx` | Program archive/restore UI |
| `src/features/programs/ui/program-lifecycle-confirmation.tsx` | Program archive/restore UI |
| `src/features/programs/ui/program-lifecycle.module.css` | Program archive/restore UI |
| `src/features/programs/ui/program-form.module.css` | Program edit UI |
| `src/features/programs/ui/program-detail.tsx` | Programs detail/list integration |
| `src/features/programs/ui/program-detail.module.css` | Programs detail/list integration |
| `src/features/programs/ui/load-program-edit-page.ts` | Programs loader/action enhancement |
| `src/features/programs/ui/load-program-lifecycle-workflow-page.ts` | Programs loader/action enhancement |
| `src/features/programs/ui/program-navigation.ts` | Programs mutation foundation |
| `src/features/programs/ui/program-workflow-visibility.ts` | Programs mutation foundation |
| `tests/helpers/program-test-fixtures.ts` | B1.5.3 fixture |
| `tests/actions/program-mutation-actions.test.ts` | B1.5.3 test |
| `tests/server/program-mutation-workflows.test.ts` | B1.5.3 test |
| `tests/ui/program-detail-presentation.test.tsx` | B1.5.3 test |
| `tests/ui/program-edit-lifecycle-forms.test.tsx` | B1.5.3 test |
| `tests/ui/program-edit-lifecycle-loaders.test.ts` | B1.5.3 test |
| `tests/ui/program-mutation-navigation.test.ts` | B1.5.3 test |
| `tests/ui/program-workflow-visibility.test.ts` | B1.5.3 test |
| `docs/phases/B1.5.3-program-edit-lifecycle-archive-and-restore.md` | B1.5.3 implementation report |

No unrelated or uncertain paths. Precheck **not** blocked.

## 5. Authoritative Behavior Reconciliation

Cross-checked contract, migrations (`20260705180004` helpers + `20260705180007` final update/archive/restore), RLS (`20260705180005`), generated RPC types, domain status/permissions, adapters, mutations, actions, and the B1.5.3 implementation report.

| Concern | Authoritative source | Implementation match |
| --- | --- | --- |
| Editable fields | `update_program` name/description/delivery_mode/metadata | UI exposes name, deliveryMode, description; metadata adapter `{}` |
| Lifecycle matrix | `private.is_allowed_program_status_transition` | Domain + status loader + radio UI |
| Archive block | open pending/active/paused enrollments | RPC + error map + UI warning |
| Restore | clear `archived_at` only | `restore_program` + copy |
| Archived mutate | update/transition denied | Permissions + mutation pre-RPC checks + RPC |
| Roles | owner/admin mutate; staff/viewer read non-archived | Multi-layer |
| History | create + transition insert history | `refreshHints.history` true only for those ops |
| Repeated ops | no-op transition; already archived; not archived | Mapped codes |

**No contract/RPC mismatch.**

## 6. Editable-Field Verification

Proven UI fields: `name`, `deliveryMode`, `description`.

Not exposed: status, archive flag, organization, role, created_by, free-form metadata, deletion.

Schemas are `.strict()`; unsupported fields rejected at action boundary.

## 7. Lifecycle-Matrix Verification

| From | Permitted | Explicitly unsupported (spot-checked) |
| --- | --- | --- |
| draft | active, retired | draft→paused, draft→draft |
| active | paused, retired | active→draft |
| paused | active, retired | paused→draft |
| retired | active | retired→draft, retired→paused |

UI shows only allowed targets. RPC rejects others (`TRANSITION_NOT_ALLOWED`). Same-status → `INVALID_STATE` with refresh required.

## 8. Edit-Workflow Findings

- Route `/programs/[programId]/edit` protected via authenticated app shell + loader.
- Current values seeded from org-scoped `getProgramById`.
- Owner/Admin allowed; Staff denied at loader; Viewer denied via same permission path.
- `updateProgramAction` → `update_program`; no direct table update in Programs feature.
- Whitespace trim + Zod validation; pendingRef + form lock.
- Archived edit → `action_unavailable` / `ARCHIVED_RECORD`.
- Invalid/unavailable IDs fail safely.
- Success revalidates `/programs` and detail, redirects to detail.

## 9. Lifecycle-Workflow Findings

- Route `/programs/[programId]/status` uses `transition_program_status` only.
- No free-form status dropdown.
- Staff/Viewer fail closed; archived transitions denied.
- Stale/repeated no-op mapped safely.
- History refresh hints set for transition success.

## 10. Archive-Workflow Findings

- Owner/Admin only; Staff denied at loader/mutation.
- Confirmation states archive is not deletion; status unchanged.
- Open-enrollment warning in UI; RPC remains authoritative blocker (`ARCHIVE_BLOCKED_OPEN_ENROLLMENTS`).
- Repeated archive: already-archived permission/RPC path fail closed.
- No Program delete path introduced.
- Archived filter remains Owner/Admin-only (existing list behavior).

## 11. Restore-Workflow Findings

- Owner/Admin only when archived; Staff denied; non-archived restore unavailable.
- Uses `restore_program`; status preserved in copy and RPC semantics.
- Repeated restore / not-archived mapped (`NOT_ARCHIVED` / insufficient role before RPC).
- Detail revalidation wired.

## 12. Route and UI Findings

- Detail workflow links Owner/Admin only; Staff/Viewer read-only without links.
- Links target edit/status/archive/restore with list return query.
- Archived detail shows Restore only; non-archived shows Edit/Status/Archive.
- Lifecycle vs Archived badges distinct.
- Open enrollment count remains informational; no Enrollments management or Progress UI.
- List/create/detail routes retained in build.

## 13. Role-Permission Findings

| Action | Owner | Admin | Staff | Viewer | Proof layers |
| --- | ---: | ---: | ---: | ---: | --- |
| View list/detail | Allow | Allow | Allow | Allow | RLS + loaders + tests |
| Edit | Allow | Allow | Deny | Deny | visibility + loader + mutation + action |
| Transition | Allow | Allow | Deny | Deny | same |
| Archive | Allow | Allow | Deny | Deny | same |
| Restore | Allow | Allow | Deny | Deny | same |
| View archived | Allow | Allow | Deny | Deny | RLS admin SELECT + permissions + list filter |

UI hiding alone is **not** the only control.

## 14. Tenant-Isolation Findings

- Organization from `resolveOrganizationContext` membership check.
- Browser org UUID validated then membership-derived; malicious org rejected before mutation.
- Role from membership context; unknown roles fail closed.
- Program reads scoped by organizationId; unavailable/foreign collapse to same safe unavailable shape.
- Mutations call RPC with membership-resolved `p_organization_id`.
- No service-role client in Programs feature.

## 15. Error and Failure-Shape Findings

Safe mapped codes include `INVALID_INPUT`, `INSUFFICIENT_ROLE`, `PROGRAM_UNAVAILABLE`, `ARCHIVED_RECORD`, `TRANSITION_NOT_ALLOWED`, `INVALID_STATE`, `ARCHIVE_BLOCKED_OPEN_ENROLLMENTS`, `NOT_ARCHIVED`, `MUTATION_COMMITTED_REFRESH_REQUIRED`, `UNEXPECTED_ERROR`.

User-facing messages reviewed in normalize/interpretation layers do not expose SQL, policy names, stack traces, or internal enumeration of foreign tenants.

## 16. Test-Quality Findings

Behavioral coverage present for:

- Edit loader access, current values, Owner/Admin update, Staff/Viewer denial
- Invalid input, malicious organization, unavailable/invalid Program
- Archived edit/transition denial
- Allowed transition matrix (domain + navigation tests)
- Invalid transition / stale no-op mapping
- Owner/Admin archive/restore; Staff/Viewer denial
- Open-enrollment archive block mapping; repeated archive path
- Restore denied when not archived; status-preserved restore messaging
- Mutation error mapping; role-gated detail controls
- Revalidation via action tests; create/list/detail regressions retained

Source-string / static-render tests (forms, labels) are compensated by mutation/action/loader behavioral suites.

**Gaps (conditions, not blockers):**

1. The seven permitted transitions are proven via matrix + UI allowedTargets + RPC mapping, not seven separate mutation invocation cases each.
2. Pending/duplicate-submit guards exist in all mutation forms but lack dedicated interaction tests for edit/status/archive/restore (create uses the same pattern).
3. Foreign-tenant ID is covered by org-scoped unavailable path rather than a separately named foreign-fixture case.
4. Automated tests are **not** live authorization evidence.

## 17. Validation Results

| Command | Exit | Result |
| --- | ---: | --- |
| Targeted Programs vitest (23 files) | `0` | 123 passed |
| `npm run test:run` | `0` | 168 files / 1105 tests passed |
| `npm run typecheck` | `0` | clean |
| `npm run lint` | `0` | no warnings/errors |
| `npm run build` | `0` | success |
| `git diff --check` | `0` | clean |

Expected baseline matched exactly: 23/123 Programs; 168/1105 complete.

Build includes `/programs`, `/programs/new`, `/programs/[programId]`, `/edit`, `/status`, `/archive`, `/restore`.

Non-secret warnings: npm `Unknown env config "devdir"`; Next.js `next lint` deprecation notice.

## 18. Secret and Scope Review

Verified during QA:

- No migration added or executed
- No Auth / membership / database record mutation
- No environment or Vercel/deployment change
- No service-role usage in Programs
- No direct Program update/delete
- No Program deletion UI/RPC invention
- No Enrollments or Progress management UI
- No AI/NBA/attention/health-score additions
- No secrets committed in dirty paths
- No published B1.5.2 reports modified
- No application source or test files modified during this QA phase
- Exactly one new file intended: this QA report

## 19. Defect Inventory

```text
None.
```

## 20. Condition Inventory

1. **Authenticated browser / live RPC authorization** must be completed in B1.5.3-R1 before publication.
2. **Per-transition mutation cases** rely on domain matrix + UI constraints + RPC enforcement rather than seven dedicated mutation invocations.
3. **Pending/duplicate-submit** guards are source-proven across forms; dedicated interaction tests for B1.5.3 forms remain optional reinforcement for R1.
4. Soft-archive/restore do not write `program_status_history` (authoritative RPC behavior); only lifecycle transitions do. Detail revalidation still reloads history section.

## 21. Authenticated-Browser Requirements (B1.5.3-R1)

Verify live with real memberships (without inventing fixtures beyond authorized QA process):

1. Owner and Admin: edit supported fields; each permitted transition; archive without open enrollments; restore.
2. Staff and Viewer: no mutation controls; direct route and action denial.
3. Open-enrollment archive block message.
4. Archived programs: no edit/transition; restore for Owner/Admin; Staff/Viewer cannot view archived.
5. Cross-tenant / unavailable Program ID indistinguishable failure.
6. List/detail refresh after mutations; history row after transition.
7. Confirm no Enrollments/Progress controls and no delete wording.

## 22. Publication-Readiness Conclusion

```text
NOT READY FOR PUBLICATION — READY FOR B1.5.3-R1
```

Independent automated QA is complete. Publication requires authenticated browser and live authorization verification.

## 23. Exact Next Safe Phase

```text
B1.5.3-R1 — Authenticated browser and live authorization verification
```

Safest next action: execute B1.5.3-R1 against this B1.5.3 dirty worktree (or an authorized committed snapshot), without publication until R1 closes.
