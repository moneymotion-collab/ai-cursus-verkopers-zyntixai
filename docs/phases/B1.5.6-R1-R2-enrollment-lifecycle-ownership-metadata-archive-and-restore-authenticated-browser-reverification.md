# B1.5.6-R1-R2 — Enrollment Lifecycle, Ownership, Metadata, Archive and Restore Authenticated Browser Reverification

| Field | Value |
| --- | --- |
| Phase | B1.5.6-R1-R2 — Controlled Fixture Recovery and Authenticated Browser Reverification |
| Parent track | B1.5 — Programs & Enrollments Workspace |
| Status | **PASS WITH CONDITIONS** — ready for B1.5.6 publication |
| Baseline commit | `e318f3c7b54b340c6a2216a323c47cd4549a4f40` (`docs(enrollments): close B1.5.5 publication`) |
| Branch | `core/platform-readiness-20260707` |
| Implementation report | `docs/phases/B1.5.6-enrollment-lifecycle-ownership-metadata-archive-and-restore.md` |
| Independent QA report | `docs/phases/B1.5.6-QA-enrollment-lifecycle-ownership-metadata-archive-and-restore-independent-qa.md` |
| Historical blocked R1 | `docs/phases/B1.5.6-R1-enrollment-lifecycle-ownership-metadata-archive-and-restore-authenticated-browser-and-live-authorization-verification.md` |
| Fixture preparation | `docs/phases/B1.5.6-R1-FIXTURE-controlled-qa-eligibility-preparation.md` |
| Repository mutation | Exactly one new R1-R2 report (this file). No source/test/fixture or prior-report edits |
| Application-data mutation | Exactly one new Enrollment + authorized owner/lifecycle/archive/restore journey on that Enrollment only |

---

## 1. Executive Summary

B1.5.6-R1-R2 completed the authenticated browser and live authorization verification that was previously blocked by fixture scarcity. Using the prepared Customer `B1.5.6 Lifecycle QA Customer` and active Program `B1.5.2 QA Test Program`, Staff created exactly one dedicated Enrollment and executed owner assignment, four successful lifecycle transitions (ending in `cancelled`), validation rejections, and Staff archive/restore denial. Owner archived and restored that Enrollment. Viewer denials were verified both while archived and after restore. The retained B1.5.5 pending Enrollment remained unchanged. Post-browser automated regression matched the green baseline. Publication is authorized under non-blocking conditions.

---

## 2. Final R1-R2 Verdict

```text
PASS WITH CONDITIONS — READY FOR B1.5.6 PUBLICATION
```

Operational verdict:

```text
B1.5.6-R1-R2 PASS WITH CONDITIONS — READY FOR PUBLICATION
```

---

## 3. Git Baseline

| Item | Value |
| --- | --- |
| Worktree / repository root | `D:\project ai cursus verkopers.worktrees\parallel__laptop-product-track-20260707-1` |
| Branch | `core/platform-readiness-20260707` |
| HEAD / upstream | `e318f3c7b54b340c6a2216a323c47cd4549a4f40` |
| Subject | `docs(enrollments): close B1.5.5 publication` |
| Divergence | `0 0` |
| Staged | `0` |
| Commit / push during R1-R2 | **None** |
| Source / test edits during R1-R2 | **None** |

Preflight dirty inventory remained exclusively B1.5.6-scoped (implementation + QA + blocked R1 + fixture docs/routes/UI/tests). R1-R2 adds only this report.

---

## 4. Historical Blocked R1

Historical operational verdict: `B1.5.6-R1 CONTROLLED FIXTURE BLOCKED`.

Blocking facts:

- only one eligible Customer and one active Program;
- that pair already held the retained B1.5.5 open pending Enrollment;
- reuse of that Enrollment was prohibited;
- no B1.5.6 mutation journey was executed in the first R1.

---

## 5. Fixture-Preparation Evidence

Fixture report: `docs/phases/B1.5.6-R1-FIXTURE-controlled-qa-eligibility-preparation.md`

| Item | Value |
| --- | --- |
| Path | Path A — Customer fixture |
| Supporting Customer | `B1.5.6 Lifecycle QA Customer` |
| Customer status | `onboarding` · not archived |
| Active Program | `B1.5.2 QA Test Program` |
| Pair eligibility | no non-archived open Enrollment for this pair |
| Fixture verdict | `B1.5.6-R1-FIXTURE PASS — READY FOR AUTHENTICATED REVERIFICATION` |

R1-R2 baseline reconfirmed the prepared Customer/Program and that the pair remained eligible before mutation.

---

## 6. Runtime and Unauthenticated Behavior

| Item | Value |
| --- | --- |
| Build | Fresh `npm run build` for this dirty worktree |
| Runtime | `npx next start -p 3014 -H 127.0.0.1` |
| Base URL | `http://127.0.0.1:3014` |

| Route | Result |
| --- | --- |
| `GET /login` | **200** |
| `GET /enrollments` | **307** → `/login?next=%2Fenrollments` |
| `GET /enrollments/new` | **307** → internal `next` |
| Detail / edit / status / archive / restore (placeholder UUID) | **307** → internal `next` paths |

All `next=` values were root-relative internal paths. No open redirect observed.

---

## 7. QA Account State

Dedicated organization UI label: **ZyntixAI Production QA**.

| Role | Live journey | Independent capability evidence |
| --- | --- | --- |
| Staff | Executed | No New program; no archived Enrollment filter; New enrollment present; lifecycle + owner present; archive/restore denied |
| Owner | Executed (pre-archive + restore) | New program present; archived filters present; archive/restore functional |
| Viewer | Executed (archived + final) | No New enrollment; no archived filter; mutation routes denied; archived fail-closed |
| Admin | **Not present** | See §33 |

No Auth users, memberships, passwords, or roles were created or changed.

---

## 8. Retained B1.5.5 Fixture Baseline and Unchanged Final State

| Attribute | Baseline | Final |
| --- | --- | --- |
| Count | Exactly one | Exactly one (plus the new B1.5.6 Enrollment) |
| Customer | Different from prepared Customer (`zynrtix`) | Unchanged |
| Program | `B1.5.2 QA Test Program` | Unchanged |
| Status | `pending` | `pending` |
| Archive | Not archived | Not archived |
| Source | `manual` | `manual` |
| Owner | Unassigned | Unassigned |
| Enrolled | Jul 25, 2026, 10:13 PM | Jul 25, 2026, 10:13 PM |

No mutation was performed on this Enrollment during R1-R2.

---

## 9. Prepared Customer State

| Attribute | Value |
| --- | --- |
| Display name | `B1.5.6 Lifecycle QA Customer` |
| Status | `onboarding` |
| Archive | Not archived |
| Organization | ZyntixAI Production QA |

---

## 10. Dedicated B1.5.6 Enrollment Creation

Created by Staff via `/enrollments/new` exactly once.

| Field | Value |
| --- | --- |
| Internal designation | `B1.5.6 CONTROLLED LIFECYCLE ENROLLMENT` |
| Customer | `B1.5.6 Lifecycle QA Customer` |
| Program | `B1.5.2 QA Test Program` |
| Initial status | `pending` |
| Owner at create | Unassigned |
| Source | `manual` (server-controlled; not editable) |
| Archive | Not archived |
| History | Create entry as Pending |

Raw Enrollment identifier is not disclosed in this report.

---

## 11. Staff Role Evidence

| Signal | Observation |
| --- | --- |
| `/programs` | No **New program** |
| `/enrollments` | **New enrollment** present; no archived filter |
| Org label | ZyntixAI Production QA |
| Resolved role | `staff` |

---

## 12. Owner-Assignment Journey

`/edit` verified as ownership-only (no Customer/Program/status/source/metadata/organization/archive controls).

Successful updates on the controlled Enrollment:

1. Unassigned → QA Staff
2. QA Staff → Unassigned
3. Unassigned → QA Staff

After each update: status remained `pending`; source `manual`; not archived; Customer/Program unchanged.

**UI label note:** Staff/Viewer memberships without profile display names render as **Team member** in selects and often **Unavailable member** on detail. Assignment targeted the non-Owner active membership corresponding to QA Staff (second active membership after Owner by join order). Final required owner state: QA Staff.

---

## 13. Pending → Active

Targets shown: Active, Cancelled only.

Submitted `pending → active` with reason `B1.5.6 R1-R2 initial activation`.

| Check | Result |
| --- | --- |
| Status | `active` |
| `started_at` | Populated: **Jul 26, 2026, 8:04 AM** (recorded for equality) |
| `completed_at` / `cancelled_at` | Empty |
| Owner | QA Staff |
| History | +1 transition entry with reason |

---

## 14. Active → Paused

Targets: Paused, Completed, Cancelled.

Submitted `active → paused` with short reason `pause` after overlong rejection (§17).

| Check | Result |
| --- | --- |
| Status | `paused` |
| Original `started_at` | Unchanged (Jul 26, 2026, 8:04 AM) |
| Terminal timestamps | Still empty |
| History | +1 transition entry |

---

## 15. Paused → Active

Targets: Active, Completed, Cancelled.

Submitted `paused → active` with reason `B1.5.6 R1-R2 controlled reactivation`.

| Check | Result |
| --- | --- |
| Status | `active` |
| `started_at` | Unchanged (not replaced) |
| History | +1 transition entry |

---

## 16. Active → Cancelled

Submitted after terminal-confirmation rejection (§17), with confirmation enabled and reason `B1.5.6 R1-R2 controlled terminal transition`.

| Check | Result |
| --- | --- |
| Status | `cancelled` |
| `cancelled_at` | Populated (Jul 26, 2026, 8:09 AM) |
| `completed_at` | Empty |
| Archive | Not archived |
| Lifecycle actions | Absent |
| Owner | QA Staff |
| History | +1 transition entry |

---

## 17. Validation-Rejection Evidence

| Attempt | Result |
| --- | --- |
| Reason longer than 500 characters toward Paused | Field error: “String must contain at most 500 character(s)” · status remained `active` · no history entry |
| Cancelled without terminal confirmation | “Confirm this terminal status change before continuing.” · status remained `active` · no history entry |

No raw SQL/RLS/server stack traces observed.

---

## 18. Lifecycle Timestamps

| Timestamp | Final observation |
| --- | --- |
| Enrolled / Created | Jul 26, 2026, 8:02 AM |
| `started_at` | Jul 26, 2026, 8:04 AM (first activation only; preserved across pause/reactivation) |
| `completed_at` | Not provided |
| `cancelled_at` | Jul 26, 2026, 8:09 AM |
| Archived at (after restore) | Not provided |

---

## 19. Lifecycle History

Deterministic order observed (newest first in UI):

1. Set to Pending (create)
2. Pending → Active (`B1.5.6 R1-R2 initial activation`)
3. Active → Paused (`pause`)
4. Paused → Active (`B1.5.6 R1-R2 controlled reactivation`)
5. Active → Cancelled (`B1.5.6 R1-R2 controlled terminal transition`)

Rejected validation attempts produced **no** history entries. Archive and restore produced **no** history entries.

---

## 20. Staff Archive/Restore Denial

On cancelled non-archived controlled Enrollment as Staff:

- Readable; no lifecycle action; Edit owner still present
- No Archive / Restore / metadata controls on detail
- Direct `/archive` and `/restore`: unavailable copy · no functional submit · no mutation

Enrollment remained cancelled, not archived, owner QA Staff.

---

## 21. Owner Role Evidence

| Signal | Observation |
| --- | --- |
| `/programs` | **New program** present |
| Archived filters | Present on Programs / Enrollments |
| Resolved role | `owner` |

---

## 22. Archive Cancellation

Opened `/archive`: wording says Archive, states archive is not deletion, shows terminal status Cancelled, cancel/back available.

Cancelled once (left without submit): no mutation; status Cancelled; not archived; history unchanged.

---

## 23. Successful Archive

Submitted archive exactly once.

| Check | Result |
| --- | --- |
| Status | Remained `cancelled` |
| Archive | Archived |
| Owner | Unchanged (QA Staff) |
| History | Unchanged (no archive entry) |
| Normal list | Controlled row absent (**1 of 1** = B1.5.5 only) |
| Owner `?archived=true` | Controlled row visible as Cancelled Archived |
| Deletion | Did not occur |

---

## 24. Archived-State Restrictions

Archived detail (Owner):

- Archived state prominent
- Status Cancelled; owner QA Staff; source Manual
- Only **Restore** action
- No lifecycle / owner-edit / metadata / archive actions
- History still create + four transitions

Direct archived `/edit`: “Archived enrollments cannot be edited.”
Direct archived `/status`: unavailable · no form.

---

## 25. Viewer Archived-State Denial

| Check | Result |
| --- | --- |
| Resolved role | `viewer` (no New enrollment / New program / archived filter) |
| Normal list | Archived Enrollment absent |
| `?archived=true` | Fail-closed (“Filters adjusted”) · still only B1.5.5 |
| Direct detail / edit / archive / restore | Safe **Enrollment unavailable** shape · no form · no data leak |
| `/metadata` | **404** |

---

## 26. Restore Cancellation

Owner reopened `/restore`, left without submit: Enrollment remained archived; status Cancelled; owner unchanged; history unchanged.

---

## 27. Successful Restore

Submitted restore exactly once.

| Check | Result |
| --- | --- |
| Archive cleared | Yes (Not archived; Archived at Not provided) |
| Status | Remained `cancelled` |
| Owner | QA Staff |
| Source | Manual |
| Customer / Program | Unchanged |
| History | Unchanged (no restore entry) |
| Normal list | Controlled row returned (**2 of 2**) |
| Lifecycle controls | Still absent (terminal) |
| Archive action | Available again to Owner |

Did **not** archive again.

---

## 28. Preserved Status and Owner

Final controlled Enrollment after restore:

- status `cancelled`
- not archived
- owner QA Staff
- source `manual`
- Customer/Program unchanged

---

## 29. Final Viewer Read-Only Evidence

| Check | Result |
| --- | --- |
| List | Shows controlled Cancelled Enrollment + B1.5.5 Pending |
| Detail | Readable: Cancelled · Not archived · Customer/Program · Owner · Manual · timestamps · history |
| Mutation controls | None (no Edit owner / Change status / Archive / Restore / metadata) |
| Progress / AI | Deferred progress copy only; no AI section |
| `/edit` | Permission denial · no form |
| `/status` `/archive` `/restore` | Unavailable · no form |
| `/metadata` | 404 |

---

## 30. Metadata Omission Classification

No metadata route, editor, or raw JSON/key-value UI exists. Omission is intentional and **non-blocking**, aligned with B1.5.6 implementation/QA conditions and contract §16.4. Metadata preservation after restore is not directly visible in UI (no product metadata fields).

---

## 31. Tenant-Isolation Findings

| Check | Result |
| --- | --- |
| Malformed Enrollment ID | Same **Enrollment unavailable** shape |
| Random missing UUID | Same unavailable shape · no ownership/existence leak |
| Org/role browser control | Not observed as mutable |
| Service-role / Admin API | Not used |
| Cross-tenant enumeration | Not performed (forbidden) |

---

## 32. Stale-Copy Condition

Create-form help text (observed at Staff create):

> Enroll an eligible customer into an active program. Lifecycle, owner, and metadata changes follow in a later phase.

Lifecycle and ownership now exist; metadata remains deferred. Copy is **stale** but does **not** misdirect security-sensitive authorization or block workflow operation. Classified **non-blocking**. Not edited during R1-R2.

---

## 33. Admin Classification

```text
ADMIN LIVE JOURNEY NOT EXECUTED
```

No dedicated QA Admin membership exists. Creating one was forbidden. Non-blocking because:

- Owner archive/restore passed live;
- Staff lifecycle and ownership passed live;
- Viewer denials passed live;
- automated Admin coverage passed in regression.

---

## 34. Exact Mutation Inventory

### Supporting fixture (prior FIXTURE phase)

- Customers created: **1** (`B1.5.6 Lifecycle QA Customer`)

### R1-R2 Enrollment

| Class | Count / detail |
| --- | --- |
| Enrollments created | **1** (prepared Customer + active Program; source manual) |
| Owner updates | **3** (→ Staff → Unassigned → Staff) |
| Successful transitions | **4** (pending→active; active→paused; paused→active; active→cancelled) |
| Successful archives | **1** |
| Successful restores | **1** |
| Transition to completed | **0** |
| Mutations on B1.5.5 Enrollment | **0** |
| Customer / Program / Lead / Task updates | **0** |
| Auth / membership / role / org changes | **0** |
| Migrations / deploy / Vercel | **0** |

### Rejected / cancelled interactions

- 1 overlong reason rejection
- 1 missing terminal-confirmation rejection
- 1 archive cancellation
- 1 restore cancellation
- Denied direct-route checks (Staff/Viewer/archived/unavailable)

---

## 35. Final Controlled Enrollment State

| Attribute | Required | Observed |
| --- | --- | --- |
| Customer | `B1.5.6 Lifecycle QA Customer` | Yes |
| Program | `B1.5.2 QA Test Program` | Yes |
| Status | `cancelled` | Yes |
| Archive | not archived after restore | Yes |
| Source | `manual` | Yes |
| Owner | QA Staff | Yes (UI label Unavailable member) |
| History | create + four transitions | Yes |

---

## 36. Post-Browser Regression

| Command / suite | Exit | Result |
| --- | ---: | --- |
| Targeted B1.5.6 (13 files) | `0` | 144 tests |
| All Enrollments (22 files) | `0` | 213 tests |
| B1.5 Programs + Enrollments (45 files) | `0` | 336 tests |
| `npm run test:run` | `0` | 190 files / 1318 tests |
| `npm run typecheck` | `0` | PASS |
| `npm run lint` | `0` | PASS |
| `npm run build` | `0` | PASS; edit/status/archive/restore routes present |
| `git diff --check` | `0` | PASS |

Routes present: `/enrollments`, `/new`, `[enrollmentId]`, `/edit`, `/status`, `/archive`, `/restore`.

---

## 37. Credential-Handling Confirmation

- Operator entered credentials only in the browser
- No passwords, cookies, tokens, emails, user IDs, or organization IDs recorded in this report
- Enrollment raw identifier withheld

---

## 38. Limitations and Conditions

1. Metadata UI intentionally omitted (no approved product fields).
2. Admin live journey not executed (no QA Admin).
3. Stale create-form copy still mentions later-phase lifecycle/owner/metadata.
4. No unsafe live cross-tenant enumeration performed.
5. Metadata preservation not directly visible in UI.
6. Staff/Viewer owner labels lack profile display names (Unavailable member / Team member).

None blocked required journeys or authorization proof.

---

## 39. Publication-Readiness Conclusion

Authenticated Owner/Staff/Viewer browser evidence, controlled Enrollment mutation inventory, retained B1.5.5 unchanged proof, and green post-browser regression are sufficient for **B1.5.6 controlled publication** under the conditions above.

Manual-testing classification:

```text
AUTHENTICATED BROWSER REVERIFICATION COMPLETE — NO ADDITIONAL TEST REQUIRED BEFORE PUBLICATION
```

---

## 40. Exact Next Safe Phase

```text
B1.5.6-PUB — Enrollment Lifecycle, Ownership, Metadata, Archive and Restore Controlled Publication
```

Do not start B1.5.6-PUB until track process authorizes it. Retain both the B1.5.5 pending Enrollment and the cancelled restored B1.5.6 controlled Enrollment unless a later phase explicitly supersedes them.
