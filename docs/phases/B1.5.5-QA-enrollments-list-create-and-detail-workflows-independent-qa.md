# B1.5.5-QA — Enrollments List, Create and Detail Workflows Independent QA

| Field | Value |
| --- | --- |
| Phase | B1.5.5-QA — Independent QA, Security and Regression Verification |
| Parent track | B1.5 — Programs & Enrollments Workspace |
| Implementation under review | `docs/phases/B1.5.5-enrollments-list-create-and-detail-workflows.md` |
| Authoritative contract | `docs/phases/B1.5-programs-and-enrollments-workspace-design-security-and-readiness-contract.md` |
| Published foundation | B1.5.4 (implementation `4e3e962…`; closure `528106d…`) |
| Branch | `core/platform-readiness-20260707` |
| Baseline HEAD | `528106dfe7e341c522285ca55cfa00ca34815dd2` |
| QA mutation | Exactly one new QA report (this file). No source/test/fixture changes. |

---

## 1. Executive Summary

Independent code, security, and regression review of B1.5.5 confirms that Enrollments list, create, and read-only detail workflows are contract-aligned with the published B1.5.4 foundation. Create uses the authoritative `create_enrollment` path; Viewer fails closed at loader and mutation layers; archived visibility is enforced in URL parsing and server queries; detail remains read-only without deferred mutation controls; navigation is active and correctly ordered. All automated validation gates reproduced the accepted green baseline. Authenticated browser and live RLS verification remain deferred to B1.5.5-R1.

---

## 2. Final QA Verdict

```text
PASS WITH CONDITIONS — READY FOR B1.5.5-R1
```

Operational verdict:

```text
B1.5.5-QA PASS WITH CONDITIONS — READY FOR AUTHENTICATED BROWSER VERIFICATION
```

---

## 3. Git Baseline

| Item | Value |
| --- | --- |
| Worktree | `D:/project ai cursus verkopers.worktrees/parallel__laptop-product-track-20260707-1` |
| Branch | `core/platform-readiness-20260707` |
| HEAD / upstream | `528106dfe7e341c522285ca55cfa00ca34815dd2` |
| Subject | `docs(enrollments): close B1.5.4 publication` |
| Divergence | `0 0` |
| Staged | `0` |
| Deletes / renames / conflicts | `0` |

---

## 4. Complete Pre-QA Dirty Inventory

### Modified

| Path | Classification |
| --- | --- |
| `src/features/enrollments/domain/enrollments-navigation.ts` | Navigation activation |
| `tests/domain/enrollment-navigation-and-routes.test.ts` | B1.5.5 test |
| `tests/ui/programs-navigation.test.tsx` | Navigation activation (Programs regression) |

### Untracked — routes

| Path | Classification |
| --- | --- |
| `src/app/(authenticated)/enrollments/page.tsx` | Enrollments list route |
| `src/app/(authenticated)/enrollments/page.module.css` | Enrollments list UI |
| `src/app/(authenticated)/enrollments/new/page.tsx` | Enrollment create route |
| `src/app/(authenticated)/enrollments/[enrollmentId]/page.tsx` | Enrollment detail route |
| `src/app/(authenticated)/enrollments/[enrollmentId]/page.module.css` | Enrollment detail UI |

### Untracked — loaders / support

| Path | Classification |
| --- | --- |
| `src/features/enrollments/domain/mutation-interpretation.ts` | Mutation interpretation |
| `src/features/enrollments/server/load-enrollment-create-options.ts` | Create-options loader |
| `src/features/enrollments/server/resolve-enrollment-labels.ts` | Owner/member label support |
| `src/features/enrollments/ui/load-enrollments-page.ts` | List-loader enhancement |
| `src/features/enrollments/ui/load-enrollment-create-page.ts` | Create-page loader |
| `src/features/enrollments/ui/load-enrollment-detail-page.ts` | Detail-loader enhancement |

### Untracked — UI

| Path | Classification |
| --- | --- |
| `src/features/enrollments/ui/enrollment-list-search-params.ts` | Enrollments list UI |
| `src/features/enrollments/ui/enrollment-list.tsx` / `.module.css` | Enrollments list UI |
| `src/features/enrollments/ui/enrollment-list-filters.tsx` / `.module.css` | Enrollments list UI |
| `src/features/enrollments/ui/enrollment-create-form.tsx` | Enrollment create UI |
| `src/features/enrollments/ui/enrollment-form.module.css` | Enrollment create UI |
| `src/features/enrollments/ui/enrollment-form-state.ts` | Create-action enhancement (UI) |
| `src/features/enrollments/ui/enrollment-detail.tsx` / `.module.css` | Enrollment detail UI |
| `src/features/enrollments/ui/enrollment-history.tsx` / `.module.css` | Enrollment detail UI |
| `src/features/enrollments/ui/enrollment-navigation.ts` | Enrollments shared UI |
| `src/features/enrollments/ui/enrollment-workflow-visibility.ts` | Enrollments shared UI |
| `src/features/enrollments/ui/enrollment-presentation.ts` | Enrollments shared UI |
| `src/features/enrollments/ui/enrollment-organization-required-panel.tsx` | Enrollments shared UI |

### Untracked — tests and reports

| Path | Classification |
| --- | --- |
| `tests/server/enrollment-create-options.test.ts` | B1.5.5 test |
| `tests/ui/enrollment-list-search-params.test.ts` | B1.5.5 test |
| `tests/ui/enrollment-list-presentation.test.tsx` | B1.5.5 test |
| `tests/ui/enrollment-page-loaders.test.ts` | B1.5.5 test |
| `tests/ui/enrollment-create-workflow.test.tsx` | B1.5.5 test |
| `tests/ui/enrollment-detail-presentation.test.tsx` | B1.5.5 test |
| `tests/ui/enrollments-navigation.test.tsx` | B1.5.5 test |
| `docs/phases/B1.5.5-enrollments-list-create-and-detail-workflows.md` | B1.5.5 implementation report |

No unrelated, uncertain, or sensitive dirty paths. Target QA report did not exist at preflight.

---

## 5. Contract Traceability

| Requirement | Status |
| --- | --- |
| `/enrollments` list | Implemented and verified (automated) |
| `/enrollments/new` create | Implemented and verified (automated) |
| `/enrollments/[enrollmentId]` read-only detail | Implemented and verified (automated) |
| Uses published B1.5.4 typed foundation | Implemented and verified |
| Create via `create_enrollment` only | Implemented and verified |
| No direct insert / service-role | Implemented and verified |
| Membership-derived org/role | Implemented and verified |
| Viewer create denial | Implemented and verified (loader + mutation) |
| Archived visibility role matrix | Implemented and verified (URL + query + detail permissions) |
| Eligible Customer/Program options | Implemented and verified (query filters + tests) |
| Navigation activation + order | Implemented and verified |
| Lifecycle / edit / archive / restore UI | Intentionally deferred |
| Progress / AI / NBA / attention | Intentionally deferred |
| Live RLS proof | Browser verification required |
| Multi-role authenticated browser journeys | Browser verification required |

No contract deviation found for B1.5.5 scope.

---

## 6. Create-Input Verification

| Field | Finding |
| --- | --- |
| `customerId` / `programId` | Required; present in form and schema |
| Organization | Server-resolved via membership; not a form field |
| `ownerMemberId` | Optional; empty → null |
| `initialStatus` | Restricted to `pending` \| `active` |
| `metadata` | Schema default `{}`; **not UI-exposed** |
| `source` | Adapter hard-codes `"manual"`; **not UI-exposed** |

Forbidden UI exposures (org ID, role, source, metadata, lifecycle timestamps, archive state, created-by) are absent from the create form.

---

## 7. Customer Eligibility Findings

Create options query:

- `.eq("organization_id", organizationId)`
- `.is("archived_at", null)`
- `.in("status", ["onboarding", "active"])`

Covered by `tests/server/enrollment-create-options.test.ts`. Final eligibility remains RPC-authoritative (C4 from foundation preserved).

---

## 8. Program Eligibility Findings

Create options query:

- `.eq("organization_id", organizationId)`
- `.is("archived_at", null)`
- `.eq("status", "active")`

Covered by create-options tests. Final eligibility remains RPC-authoritative.

---

## 9. List-Workspace Findings

- Protected via `isProtectedApplicationPath("/enrollments…")`
- Org from `resolveEnrollmentPageOrganization`
- URL allowlists for status/sort/direction/pagination; search trimmed/capped at 200
- Staff/Viewer `archived=true` URL params forced off with warning; server still applies `archived_at is null`
- Presentation: Customer, Program, Status, Owner labels, enrolled date, archived badge
- Empty vs no-results vs archived-empty distinct; recoverable list error panel
- Create CTA gated by `canShowCreateEnrollmentWorkflow`

---

## 10. Archived-Visibility Findings

| Role | Filter control | List includeArchived | Archived detail |
| --- | --- | --- | --- |
| Owner/Admin | Shown | Allowed when requested | Allowed (`canViewEnrollment`) |
| Staff/Viewer | Hidden | Forced off in parse + query | Denied → same `ENROLLMENT_UNAVAILABLE` |

Proven by search-params tests, read-query tests, permission tests, and detail foundation checks. Unknown/absent membership fails closed via org resolver.

---

## 11. Create-Page Findings

- Owner/Admin/Staff → `ready` with options
- Viewer → `action_unavailable` (loader-level)
- Eligible options only; membership-scoped owners
- Empty customer/program states understandable; submit disabled
- No organization/role selectors on the form page
- Options load errors use a safe generic message

---

## 12. Create-Form Findings

Approved fields only: Customer, Program, Initial status, optional Owner.

- Labels use display names (not raw IDs as primary labels)
- `pendingRef` + pending UI state for duplicate-submission protection
- Field errors via mutation interpretation
- Values preserved in React state after validation failure
- Accessibility: labels, `aria-invalid`, described-by help/error ids

Some create-form tests assert markup strings for forbidden-field absence; behavioral create enforcement is compensated by mutation and loader tests.

---

## 13. Create-Action Findings

Path: `createEnrollmentAction` → `createEnrollmentMutation` → `callCreateEnrollmentRpc` → `create_enrollment`.

- Membership org/role resolved server-side; browser org ID does not grant authority
- Viewer denied before RPC (`insufficientRoleError`)
- Zod validates IDs and initial status
- Source fixed to `manual` in adapter
- Metadata not browser-controlled by current UI
- Duplicate-open / eligibility / unavailable mapped safely via normalize + mutation-interpretation
- Success redirects to detail; published action revalidates list/detail paths
- Mutation tests prove no direct insert/delete on create

---

## 14. Detail-Workflow Findings

Read-only detail presents status, archive state, Customer/Program (linked when available), owner, source, lifecycle and audit timestamps, history, and back navigation.

Absent: lifecycle actions, edit, owner reassignment, metadata mutation, archive/restore, Progress, AI/NBA/attention/churn/health-score.

Boundary note explicitly states deferred mutation workflows.

---

## 15. History Findings

- Loaded only after enrollment access succeeds (org-scoped)
- Deterministic foundation ordering; labels + timestamps rendered
- Actor labels via membership-scoped member resolution
- States: ready / empty / error (reload) / hidden
- No Progress mixing; no false archive/restore history claims beyond status-history rows

---

## 16. Route-Protection Findings

- `/enrollments`, `/new`, and `/[id]` protected and safe-return allowlisted (B1.5.4 foundation + tests)
- Open redirects rejected
- Unauthenticated access follows normal protected-app login flow (middleware); live redirect path deferred to R1 browser

---

## 17. Navigation Findings

- `ENROLLMENTS_NAV_VISIBLE = true`
- Exact order: Home → Leads → Customers → Programs → Enrollments → Tasks
- Single link to `/enrollments`
- Programs regression test updated; no dead Enrollments link in build

---

## 18. Role-Permission Findings

| Behavior | Owner | Admin | Staff | Viewer | Enforcement layers |
| --- | ---: | ---: | ---: | ---: | --- |
| View non-archived list/detail | Allow | Allow | Allow | Allow | presentation + loader + query/RLS-preserving client |
| View archived | Allow | Allow | Deny | Deny | URL parse + query filter + detail permissions |
| See create CTA | Allow | Allow | Allow | Deny | presentation (`canShowCreate…`) |
| Open create page | Allow | Allow | Allow | Deny | page loader |
| Submit create | Allow | Allow | Allow | Deny | mutation before RPC + RPC/RLS |

UI hiding alone is not the sole control.

---

## 19. Tenant-Isolation Findings

- Org-scoped reads/options/history
- Member labels resolved within organization_id
- Malformed / missing / foreign Enrollment IDs collapse to `enrollment_unavailable`
- No service-role or Admin client in Enrollments feature or routes
- Related Customer/Program summaries loaded with organization filters

Live cross-tenant proof remains R1 (condition C1).

---

## 20. Safe-Error Findings

- Mutation interpretation maps duplicate-open, eligibility, unavailable, invalid owner, transport errors to safe copy
- Create-options failures return a generic message without SQL/constraint text
- Detail unavailable copy does not distinguish foreign vs missing
- No credentials or internal identifiers observed in dirty evidence

---

## 21. Test-Quality Findings

Targeted B1.5.5 pack: **8 files / 54 tests**, all passing.

Behavioral coverage is strong for list URL allowlists, archived denial, create-options eligibility, create-page Viewer denial, detail unavailable collapse, read-only presentation, and navigation.

Foundation suites continue to prove Viewer action denial, direct-insert absence, duplicate/eligibility error mapping, and archived read enforcement.

Gaps / compensations (non-blocking):

- Create-form tests lean on static markup for forbidden fields; compensated by schema/mutation/loader tests.
- Explicit Admin create-page case is not separate from Owner/Staff (same permission path).
- Live authenticated multi-role browser journeys not in automated suite (expected → R1).

No source-string-only suite that fails to compensate for critical security behaviors.

---

## 22. Validation Results

Independent QA re-run:

| Command | Exit | Result |
| --- | ---: | --- |
| Targeted B1.5.5 vitest (8 files) | `0` | **54** tests PASS |
| All Enrollments-relevant vitest | `0` | **18** files / **188** tests PASS |
| B1.5 Programs + Enrollments vitest | `0` | **41** files / **311** tests PASS |
| `npm run test:run` | `0` | **185** files / **1277** tests PASS |
| `npm run typecheck` | `0` | PASS |
| `npm run lint` | `0` | PASS (`next lint`; deprecation notice only) |
| `npm run build` | `0` | PASS; `/enrollments`, `/enrollments/new`, `/enrollments/[enrollmentId]` present; Programs routes retained |
| `git diff --check` | `0` | PASS |
| Secret-pattern scan (dirty scope) | `0` | PASS |

Counts match the known baseline exactly. Non-secret warnings: npm `devdir`; Next.js `next lint` deprecation.

---

## 23. Scope and Secret Review

| Check | Result |
| --- | --- |
| Migration / Auth / DB mutation / env / Vercel | None |
| Service-role | None |
| Direct Enrollment insert/delete | None |
| General unrestricted update UI/RPC | None |
| Lifecycle / owner-metadata / archive / restore UI | None |
| Progress / AI / NBA / churn / health-score | None |
| Secrets / build artifacts / temp files | None observed |
| Published B1.5.4 reports modified | No |
| Unrelated formatting churn | No |

---

## 24. Defect Inventory

No blocking defects found in this independent QA.

---

## 25. Condition Inventory

| ID | Condition |
| --- | --- |
| C1 | Live RLS/grant enforcement is not proven by mocked automated tests; database remains authoritative. |
| C2 | Authenticated multi-role browser journeys were not executed in this QA phase. |
| C3 | Eligible create-option lists are capped at 100 rows per category. |
| C4 | Create-form forbidden-field checks rely partly on markup assertions; compensated by behavioral mutation/loader/schema tests. |
| C5 | Metadata remains intentionally non-UI; schema default / RPC behavior still apply. |

---

## 26. Authenticated-Browser Requirements

B1.5.5-R1 should verify with real memberships:

1. Owner/Admin/Staff/Viewer list visibility and create CTA
2. Staff/Viewer cannot surface archived rows via URL tampering
3. Owner/Admin archived filter and archived detail
4. Create page option eligibility and Viewer denial
5. Successful Owner/Admin/Staff create → detail redirect (without documenting secrets)
6. Foreign Enrollment ID unavailable shape
7. Navigation order and Enrollments link target
8. Protected-route unauthenticated redirect / safe return

Do **not** use service-role during R1.

---

## 27. Manual-Testing Classification

```text
MANUAL BROWSER TEST REQUIRED — B1.5.5-R1
```

---

## 28. Publication-Readiness Conclusion

Ready for **B1.5.5-R1** authenticated browser and live authorization verification, subject to conditions C1–C5. Not yet ready for controlled publication until R1 closes (or an explicitly accepted publication charter supersedes R1).

Do not implement lifecycle/edit/archive/restore during R1 unless that phase’s charter requires it.

---

## 29. Exact Next Safe Phase

```text
B1.5.5-R1 — Enrollments List, Create and Detail Authenticated Browser and Live Authorization Verification
```

---

## Process Confirmations

- Application source, tests, and fixtures unchanged during QA
- Exactly one new report added (this file)
- No commit or push
- No database, Auth, migration, deployment, or Vercel mutation
- HEAD remains `528106dfe7e341c522285ca55cfa00ca34815dd2`; divergence `0 0`; staged `0`
