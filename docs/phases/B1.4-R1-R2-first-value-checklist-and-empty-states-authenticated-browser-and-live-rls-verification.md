# B1.4-R1-R2 — Authenticated Browser and Live RLS Reverification

| Field | Value |
| --- | --- |
| Phase | B1.4-R1-R2 — Authenticated Browser and Live RLS Reverification |
| Parent track | B1 — Controlled Beta Entry |
| Status | **PASS WITH CONDITIONS — READY FOR B1.4-PUB** |
| Baseline HEAD | `5318672067d83191be08a00e0b842c9d20d859fa` |
| Branch | `core/platform-readiness-20260707` |
| Contracts | B1.4 contract + B1.4-QA + historical B1.4-R1 (BLOCKED, preserved) |
| Production alias claimed to contain B1.4 | **NO** |
| Commit / push / deploy | **NO** |
| Staging | **NO** |
| Historical B1.4-R1 overwritten | **NO** |

---

## 1. Executive Verdict

```text
PASS WITH CONDITIONS — READY FOR B1.4-PUB
```

R2 unblocked the historical R1 authentication gap. On a local B1.4 production build (`next start` `:3014`) pointed at hosted Auth/DB for the dedicated QA tenant, an authenticated owner completed Journey A (checklist + unfiltered empty CTA), Journey B (filtered + archived empty copy without first-ever CTA), and Journey F (live dismiss via normal UI under RLS). Dismiss persisted across soft navigation and full logout/re-login. Read-only SQL confirmed only the QA org gained a non-null `first_run_checklist_dismissed_at`; leads/tasks remained zero; no service-role bypass was used for dismiss.

**Conditions (not blockers for PUB readiness under R1 rules):**

1. Journeys C–E (create first lead/task; fully-complete hide rule) were **not** executed — R2 permitted mutation was limited to checklist dismiss via product UI.
2. Journey G (admin/staff/viewer live roles) remains **not live-verified** — QA org still has owner-only membership; covered by automated tests from B1.4-QA.
3. Public production alias was **not** used as B1.4 UI proof (B1.4 remains undeployed relative to this verification).

---

## 2. Verified Git Baseline

| Item | Result |
| --- | --- |
| Worktree | `D:/project ai cursus verkopers.worktrees/parallel__laptop-product-track-20260707-1` |
| Branch | `core/platform-readiness-20260707` |
| HEAD | `5318672067d83191be08a00e0b842c9d20d859fa` — `feat(onboarding): add first-value checklist and empty states` |
| Upstream | `origin/core/platform-readiness-20260707` |
| Divergence | `0 0` |
| Staged / product dirty paths at R2 end | **None** (report file may be untracked) |
| Historical B1.4-R1 report | Preserved as **BLOCKED**; not rewritten |

---

## 3. Environment Safety

| Check | Result |
| --- | --- |
| Initial local boot blocker | `.env.local` initially targeted **local** Supabase (`127.0.0.1`); Auth down (`ECONNREFUSED` `:54441`) — hosted QA password could not succeed |
| Operator-approved remediation | Repoint gitignored `.env.local` to hosted project `dmctinrcjvsgmoxwwodw`, rebuild, `next start -p 3014` |
| Keys present (names only) | `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`, `NEXT_PUBLIC_SITE_URL`, `SITE_URL` |
| Site origin for local R2 | `http://127.0.0.1:3014` |
| Service role used for dismiss | **NO** |
| QA password printed / stored by agent | **NO** (human-entered in browser only) |
| Hosted Supabase project | `dmctinrcjvsgmoxwwodw` |

Secrets and env **values** are omitted from this report.

---

## 4. Dedicated QA Tenant — Pre / Post State

Organization: `ZyntixAI Production QA` / `zyntixai-production-qa`  
Organization id: `2fc07699-ece5-44b9-bbb3-abbc23e9fffb`

| Field | Pre-dismiss | Post-dismiss / post re-login |
| --- | --- | --- |
| Onboarding complete | **YES** | **YES** |
| Checklist dismissed | **NO** (`dismissed_at` null) | **YES** (`dismissed_at` set) |
| Active / total leads | **0** | **0** |
| Active / total tasks | **0** | **0** |
| Active owner memberships | **1** | **1** |
| Admin / staff / viewer on this org | **0** | **0** |

Tenant isolation snapshot after dismiss (read-only aggregates):

| Metric | Value |
| --- | ---: |
| QA org dismissed | **1** |
| Other orgs with dismiss set | **0** |
| Organizations total | **5** |

---

## 5. Local Application Identity

| Check | Result |
| --- | --- |
| App started from this worktree | **YES** — `npm run build` + `npx next start -p 3014` after hosted env repoint |
| BASE_URL | `http://127.0.0.1:3014` |
| `/login` | **200** |
| Authenticated `/leads` | Renders QA org lead list |
| B1.4 strings observed live | **YES** — `Get your first results`, `Dismiss checklist`, empty-state copy |
| Production alias used for B1.4 UI proof | **NO** |

---

## 6. Browser Journey Results

| Journey | Environment | Result |
| --- | --- | --- |
| A Owner checklist / empty CTA | Local `:3014` + hosted Auth | **PASS** — checklist visible (`1 of 3 complete`, company setup Done, lead/task To do + CTAs); unfiltered empty: “No leads are available.” + “Add your first lead to start organizing your pipeline.” + empty CTA “Add your first lead” |
| B Filtered / archived empty CTA | Local | **PASS** — `status=open` → “No leads match the selected filters.” (no first-ever empty CTA); `archived=true` → “No archived leads are available.” (no first-ever empty CTA) |
| C First-lead completion | — | **NOT EXECUTED** — create lead not in permitted R2 mutation set |
| D First-task completion | — | **NOT EXECUTED** — create task not in permitted R2 mutation set |
| E Fully completed hide rule | — | **NOT EXECUTED** — depends on C/D |
| F Live dismiss + RLS via app | Local + hosted DB | **PASS** — UI “Dismiss checklist” → pending “Dismissing…” → panel removed; SQL `dismissed_at` set; empty list remains |
| G Admin / staff / viewer live roles | Hosted QA org | **NOT LIVE-VERIFIED** — owner only; automated coverage retained from B1.4-QA |
| H Tenant isolation (product path) | Hosted | **PASS WITH LIMITED SCOPE** — only QA org dismiss column changed; no lead/task creates; no foreign-org dismiss observed |
| I Auth / session persistence | Local | **PASS** — checklist remains hidden after tasks↔leads roundtrip and after logout + re-login |

Unauthenticated smoke (from R2 boot): protected routes require sign-in; login form present.

---

## 7. Live RLS / Dismiss Evidence

| Evidence class | Finding |
| --- | --- |
| Dismiss path | Product UI button → server action (user session JWT) |
| Service-role SQL force-dismiss | **NO** |
| Pre SQL | `first_run_checklist_dismissed_at` null on QA org |
| Post SQL | `first_run_checklist_dismissed_at` non-null on QA org |
| Other orgs dismiss newly set | **0** |
| Leads/tasks created during R2 | **NO** |

**Explicit:** authenticated product dismiss under RLS was exercised; SQL was read-only confirmation only.

---

## 8. Automated Regression Gates (re-run during R2)

| Gate | Result |
| --- | --- |
| Focused onboarding + leads UI suites | **12 files / 91 tests PASS** |
| Typecheck (`tsc --noEmit`) | **PASS** |
| Production build (hosted env) | **PASS** (`ƒ /onboarding`, `ƒ /leads`) |

Full-suite re-run was not required to close the R1 authentication blocker; focused gates + live journeys suffice for this R2 scope.

---

## 9. Defects and Remediation

| Item | Action |
| --- | --- |
| Product B1.4 defect found in R2 | **None** |
| Product code changed in R2 | **NO** |
| Extra test files added in R2 | **NO** |
| Operational env change | Gitignored `.env.local` repointed local→hosted for R2 (operator-approved) |

---

## 10. Data Mutations

| Mutation | Performed? |
| --- | --- |
| QA checklist dismiss via normal UI | **YES** (sole intentional product write) |
| QA lead/task create | **NO** |
| Non-QA org changes | **NO** |
| Schema / RPC / membership changes | **NO** |
| Service-role bypass for dismiss | **NO** |

---

## 11. Remaining Assumptions / Unverified Claims

1. Admin/staff/viewer live role journeys still require additional controlled memberships or remain automated-only.
2. First-lead / first-task completion and fully-complete hide behavior remain proven by automated tests, not by this R2 live path.
3. Public production deployment was not audited as containing B1.4; local HEAD `5318672` is the verified B1.4 surface.

---

## 12. Publication / Git Hygiene Confirmation

| Action | Performed? |
| --- | --- |
| Stage | **NO** |
| Commit | **NO** |
| Push | **NO** |
| Merge / rebase | **NO** |
| Deploy / Vercel alias change | **NO** |
| Claim production contains B1.4 | **NO** |
| Overwrite historical B1.4-R1 | **NO** |

---

## 13. Changed-Path Inventory (ending)

Product tree remains at HEAD `5318672` with no R2 product edits.

New documentation path from this phase:

- `docs/phases/B1.4-R1-R2-first-value-checklist-and-empty-states-authenticated-browser-and-live-rls-verification.md` (**this report**)

Historical R1 path unchanged:

- `docs/phases/B1.4-R1-first-value-checklist-and-empty-states-controlled-browser-and-rls-verification.md` (**BLOCKED**, preserved)

---

## 14. Relationship to Historical R1

| Item | Result |
| --- | --- |
| R1 blocking cause | Safe QA credentials unavailable in that session |
| R2 unblock method | Human QA owner login in browser against hosted Auth + local B1.4 build |
| R1 verdict rewritten | **NO** |
| R2 supersedes R1 for go/no-go | **YES** — authenticated journeys + live dismiss now complete |

---

## 15. Final Verdict

```text
PASS WITH CONDITIONS — READY FOR B1.4-PUB
```

Authenticated owner browser behavior for checklist visibility, empty-state CTA rules, live dismiss under RLS, and dismiss persistence (including logout/return) are proven on the dedicated QA tenant against local B1.4 HEAD `5318672`. Remaining conditions are role live-coverage and first-lead/task completion journeys deferred by mutation policy — acceptable for PUB readiness under prior R1/QA condition rules.

---

## 16. Exact Next Step

```text
NEXT SAFE PHASE:
B1.4-PUB — controlled publication / production record for first-value checklist
(do not claim production contains B1.4 until PUB completes)
```
