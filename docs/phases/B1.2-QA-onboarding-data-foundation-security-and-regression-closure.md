# B1.2-QA — Onboarding Data Foundation Security and Regression Closure

| Field | Value |
| --- | --- |
| Phase | B1.2-QA — Onboarding Data Foundation Security and Regression Closure |
| Parent track | B1 — Controlled Beta Entry |
| Status | Independent QA complete |
| Baseline HEAD | `04d2e792778ee288db376e8105faf41191de1169` |
| Branch | `core/platform-readiness-20260707` |
| Implementation report | `docs/phases/B1.2-first-run-onboarding-data-foundation.md` |
| Authoritative contract | `docs/phases/B1.0-beta-entry-production-access-first-run-onboarding-design-and-readiness-contract.md` |

---

## 1. Executive Verdict

```text
PASS — READY FOR CONTROLLED PRODUCTION MIGRATION VERIFICATION
```

Independent QA confirmed the B1.2 additive migration, owner-only `apply_organization_onboarding` RPC, canonical value parity, and application foundation. After starting the already-installed Docker Desktop engine, clean local migration replay, incremental upgrade with an existing organization fixture, CHECK constraint tests, and live local Auth/RPC authorization tests all passed. Focused and full automated suites, typecheck, lint, build, and diff check passed. No production mutation occurred. No product code was modified during QA.

---

## 2. Verified Repository Baseline

| Item | Evidence |
| --- | --- |
| Worktree | `D:/project ai cursus verkopers.worktrees/parallel__laptop-product-track-20260707-1` |
| Branch | `core/platform-readiness-20260707` |
| HEAD | `04d2e792778ee288db376e8105faf41191de1169` |
| Subject | `docs(auth): close B1.1 production verification` |
| Upstream | `origin/core/platform-readiness-20260707` |
| Divergence | `0 0` |
| Initial worktree | Dirty with uncommitted B1.2 implementation + B1.2 report only |

---

## 3. Changed-Path Inventory

| Path | Classification | Purpose | B1.2 scope | Risk |
| --- | --- | --- | ---: | --- |
| `supabase/migrations/20260720140000_add_organization_first_run_onboarding.sql` | migration | Columns + CHECKs + apply RPC | yes | med |
| `src/types/database.generated.ts` | generated / types | Org columns + RPC typing | yes | low |
| `src/features/onboarding/domain/*` | validation/type contract | Options, Zod, result types | yes | low |
| `src/features/onboarding/server/*` | server operation | Read/apply/error map | yes | med |
| `src/features/onboarding/actions/onboarding-actions.ts` | application foundation | Thin server actions | yes | low |
| `tests/onboarding/*` | unit/integration (mocked DB) | Schema + server helpers | yes | low |
| `tests/security/onboarding-*.test.ts` | unit test | SQL/source contracts | yes | low |
| `docs/phases/B1.2-first-run-onboarding-data-foundation.md` | documentation | Implementation report | yes | none |
| `docs/phases/B1.2-QA-…` (this file) | documentation | QA artifact | yes | none |

**Confirmations:** no `/onboarding` route; no middleware onboarding; no landing change; no checklist behavior; no Programs/AI/Social; no package/lockfile; no `.env`; no production dump; no secrets.

---

## 4. Migration Environment

| Item | Result |
| --- | --- |
| Docker Desktop installed | YES |
| Docker engine | Initially unavailable; started existing Desktop app without elevation → available |
| Supabase CLI | 2.109.0 (`npx supabase`) |
| Local-only target verified | YES — `db reset` / local DB / no `--linked` / no `db push` |
| Runtime migration verification available | YES (after engine start) |

---

## 5. Migration SQL Review

| Finding | Assessment |
| --- | --- |
| Timestamp `20260720140000` unique | Pass |
| Additive `ALTER TABLE … ADD COLUMN` | Pass |
| No rename/drop/delete of existing data | Pass |
| New fields nullable; completion starts NULL | Pass |
| No auto-complete backfill | Pass |
| text + CHECK constraints | Pass (repo-standard) |
| Completion CHECK requires enums + non-empty name | Pass |
| `apply_organization_onboarding` SECURITY DEFINER, `search_path = ''` | Pass |
| `auth.uid()`, active `owner` membership, advisory lock | Pass |
| Grants to `authenticated`; revoke `anon`/`public` | Pass |
| No dynamic SQL; no extensions; no embedded production values | Pass |
| `first_run_checklist_dismissed_at` | **Justified additive foundation** (B1.0); no checklist behavior |

---

## 6. Clean Migration Replay

| Item | Result |
| --- | --- |
| Migration count | 44 (includes B1.2) |
| Command | `npx supabase db reset` (local) |
| Exit code | 0 |
| Columns + function present after reset | YES |
| Seed organizations count | 0 |

**Clean replay: PASS**

---

## 7. Incremental Upgrade Verification

| Step | Result |
| --- | --- |
| Pre-B1.2 history (43 migrations) | `db reset` PASS |
| Fixture organization | Name `QA Incremental Org` preserved after apply |
| Apply B1.2 SQL via local `psql` | PASS |
| New columns | NULL |
| `onboarding_completed_at` | NULL (not auto-completed) |
| Migration file restored | YES (content unchanged) |

**Incremental upgrade: PASS**

Lead/customer/task relation preservation: not seeded in the minimal incremental fixture; full-history reset retained CRM schema without breakage. No evidence of FK breakage from additive org columns.

---

## 8. Canonical Value Review

Application `onboarding-options.ts` values match migration CHECK lists exactly for:

- business types
- primary audiences
- primary offerings
- primary goals
- team-size bands

Display labels are separate. Zod enums import the same arrays. No contradictory duplicate lists found.

**Application/database alignment: PASS**

---

## 9. Authorization Review

Live local RPC tests (authenticated clients against local Supabase):

| Case | Result |
| --- | --- |
| Owner draft | PASS |
| Owner complete | PASS |
| Non-owner admin denied | PASS (`owner membership required`) |
| Cross-tenant owner denied | PASS |
| Anonymous denied | PASS (execute privilege / auth boundary) |
| App-layer owner check before RPC | PASS (source + mocked unit tests) |
| Multi-membership without org id | `organization_ambiguous` (application resolver) |

Arbitrary organization ID is not authorization by itself (membership + owner required in RPC).

---

## 10. Draft-Save Review

| Behavior | Evidence |
| --- | --- |
| Partial fields accepted | Zod draft + RPC null-means-unchanged |
| Unknown keys rejected | Zod `.strict()` |
| Completion not set | Live owner draft left `onboarding_completed_at` unset until complete |
| Profile update only for `auth.uid()` | RPC `WHERE p.id = v_user_id` |
| Blank org name rejected | RPC + Zod |

---

## 11. Completion Review

| Behavior | Evidence |
| --- | --- |
| Required fields enforced | Zod complete schema + RPC complete branch + DB CHECK |
| Client cannot submit completion timestamp | Not an RPC argument |
| First complete sets timestamp | Live test |
| Repeated complete preserves timestamp | Live test **PASS** |
| Incomplete cannot complete | RPC + CHECK |

---

## 12. Atomicity and Concurrency Review

| Topic | Finding |
| --- | --- |
| Profile + org updates | Same SECURITY DEFINER function / single transaction |
| False completion if profile missing | Blocked before org update when display name empty |
| Advisory lock per organization | Present (`872002`) |
| Concurrent stress | Sequential retry verified; parallel stress not executed |
| Stale draft clearing completion | Draft path does not write `onboarding_completed_at` except preserving existing value |

---

## 13. Existing-Organization Compatibility

Incremental fixture org remained named and incomplete after B1.2 apply. No automatic completion. Existing profile/org name reuse confirmed by design and tests.

---

## 14. Application Foundation Review

| Area | Finding |
| --- | --- |
| Read context | Authorized org + profile fields + missing list + completion flag |
| Write actions | Parse → owner resolve → RPC; mapped errors |
| Raw PG/Supabase errors | Not returned to callers |
| UI / middleware | Absent |

**Test-quality note:** Vitest server tests mock Supabase; authorization/idempotency at PostgreSQL were independently covered by live local RPC tests in this QA phase.

---

## 15. Automated Test Evidence (fresh)

| Gate | Result |
| --- | --- |
| Focused onboarding | **4 files / 25 tests** PASS |
| Auth regression slice | **2 files / 21 tests** PASS |
| Full suite | **138 files / 918 tests** PASS |
| Typecheck | PASS |
| Lint | PASS |
| Build | PASS |
| Diff check | PASS |

---

## 16. Existing-Product Regression Review

| Area | Finding |
| --- | --- |
| Auth form safety / registration fail-closed | Regression slice PASS |
| Middleware onboarding | None |
| `/onboarding` route | None |
| Landing / Leads / Customers / Tasks code | Not modified in B1.2 diff |
| Production app | Remains B1.1 deployment (not redeployed) |

---

## 17. Security Review

| Check | Result |
| --- | --- |
| Arbitrary tenant trust | NO |
| Non-owner write | Denied |
| Cross-tenant write | Denied |
| Fixed `search_path` | YES (`''`) |
| Grants | authenticated execute; anon/public revoked |
| RLS preserved | YES (org policies unchanged) |
| Admin key for normal use | NO |
| Secret exposure | NO |

---

## 18. Scope Confirmation

| Check | Result |
| --- | --- |
| Onboarding UI implemented | NO |
| `/onboarding` route added | NO |
| Middleware redirect added | NO |
| First-value checklist behavior | NO (column only) |
| Production migration applied | NO |
| Deployment performed | NO |
| Commit created | NO |
| Push performed | NO |

---

## 19. Risks and Conditions

### Blocking defects

None.

### Non-blocking observations

1. Application unit tests mock the database; live RPC coverage was performed in QA, not in the Vitest suite.
2. Parallel multi-client completion stress was not executed; advisory lock + sequential idempotency provide the primary safety evidence.
3. Direct postgres can set completion when CHECK fields are satisfied (expected superuser bypass); public path remains RPC + grants.

### Production-only

Controlled production migration apply and post-migration data verification remain for B1.2-R1.

### Deferred B1.3

Onboarding UI, redirects, checklist UX.

---

## 20. Exact Next Step

```text
NEXT SAFE PHASE:
B1.2-R1 — Controlled Production Migration and Data Foundation Verification
```
