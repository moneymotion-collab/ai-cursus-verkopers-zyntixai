# Attention Production Schema — Read-Only Migration Inventory & Apply Readiness

| Field | Value |
| --- | --- |
| Document type | Read-only inventory / apply readiness preflight |
| Authorization | `ATTENTION PRODUCTION SCHEMA READINESS AUTHORIZED — READ-ONLY INVENTORY AND APPLY PLAN ONLY` |
| Date (local) | 2026-08-07 |
| Authoritative worktree | `D:\project ai cursus verkopers.worktrees\parallel__laptop-product-track-20260707-1` |
| Branch | `core/platform-readiness-20260707` |
| HEAD | `24289e2c6f57ca702beab085f4103d224ce484e2` |
| Upstream | `24289e2c6f57ca702beab085f4103d224ce484e2` |
| Divergence | `0 0` |
| Worktree at preflight | clean |
| Production alias | `https://zyntixai.vercel.app` |
| Production deployment | `dpl_3hf9nMr2nSU8pcRM6nuT3gQuNaee` |
| Verdict | `ATTENTION PRODUCTION SCHEMA READY FOR OWNER-APPROVED APPLY — MIGRATION PLAN VERIFIED` |

## Related incident

Prior diagnostic:

`B1.7.6-F DIAGNOSTIC COMPLETE — ATTENTION READ FAILURE IDENTIFIED`

Evidence:

- Authenticated production `/attention` → UI `Unable to load Attention` / `Unable to load Attention. Please try again.`
- Document HTTP 200 (soft `query_error`)
- Supabase API: `GET /rest/v1/attention_items` → **404**
- Catalog/SQL: `relation "attention_items" does not exist` (`42P01`)
- Org/auth resolution succeeded; `/enrollments` and `/tasks` worked
- Active org observed: **ZyntixAI Production QA**

**B1.7.6-F remains blocked** until schema apply + read smoke + credentials/fixtures + product deploy of `24289e2…` + lifecycle matrix.

---

## 1. Git baseline (verified)

```text
toplevel: D:/project ai cursus verkopers.worktrees/parallel__laptop-product-track-20260707-1
branch:   core/platform-readiness-20260707
HEAD:     24289e2c6f57ca702beab085f4103d224ce484e2
upstream: 24289e2c6f57ca702beab085f4103d224ce484e2
divergence: 0 0
status: clean
log-1: 24289e2 feat(attention): add B1.7.6-E archive lifecycle QA
```

## 2. Supabase CLI & linked project

| Item | Value |
| --- | --- |
| CLI | v2.109.0 (`db push --dry-run` supported) |
| Linked | yes |
| Project ref | `dmctinrcjvsgmoxwwodw` |
| Region / status | `eu-central-1` / `ACTIVE_HEALTHY` |
| Local config `project_id` | `project_ai_cursus_verkopers` (local name only) |
| `.env.local` host ref | same `dmctinrcjvsgmoxwwodw` |
| Proof this is production DB | Prior `/attention` failure hit PostgREST on this host; other authenticated product routes succeed against same project |

**Secrets were not printed** (no tokens, passwords, JWTs, service-role keys, credentialed connection strings).

`supabase status` failed (local Docker unavailable) — irrelevant for linked remote.

---

## 3. Remote migration history vs local

| Metric | Count |
| --- | ---: |
| Local migration files | 50 |
| Remote history (`supabase_migrations.schema_migrations`) | 44 |
| Remote latest version | `20260720140000` |
| Local-only | **6** (Attention B1.7.2 + B1.7.3) |
| Remote-only | **0** |
| Unrelated pending | **0** |
| Order drift / repaired / squashed | none observed |

### Dry-run (read-only)

```powershell
npx supabase db push --linked --dry-run
```

CLI output: would push **only**:

1. `20260805124430_create_attention_items_and_signals.sql`
2. `20260805124448_enable_attention_rls_deny_by_default.sql`
3. `20260805134823_create_attention_item_events.sql`
4. `20260805134825_add_attention_helpers_and_rpcs.sql`
5. `20260805134830_enable_attention_operational_rls.sql`
6. `20260805134833_attention_security_hardening.sql`

No unexpected statements. Dry-run stated migrations will **not** be applied.

---

## 4. Attention migration chain (full)

### B1.7.2

| Version | File | Purpose |
| --- | --- | --- |
| `20260805124430` | `create_attention_items_and_signals.sql` | `attention_items` + `attention_signals`; checks; composite FKs; indexes (incl. nonterminal dedupe); signal UPDATE immutability trigger; `attention_items_set_updated_at` |
| `20260805124448` | `enable_attention_rls_deny_by_default.sql` | RLS enable + revoke; **no** operational policies yet |

### B1.7.3

| Version | File | Purpose |
| --- | --- | --- |
| `20260805134823` | `create_attention_item_events.sql` | `attention_item_events` + event immutability |
| `20260805134825` | `add_attention_helpers_and_rpcs.sql` | private helpers + nine public RPCs + execute grants |
| `20260805134830` | `enable_attention_operational_rls.sql` | authenticated SELECT grants + six SELECT policies |
| `20260805134833` | `attention_security_hardening.sql` | privilege/RLS hardening matrix |

**No later Attention fix migrations** after this chain. Product code at HEAD expects this full chain.

### Public RPCs (must exist post-apply; match adapters)

- `create_manual_attention_item`
- `record_attention_signal`
- `acknowledge_attention_item`
- `assign_attention_item`
- `update_attention_severity`
- `resolve_attention_item`
- `dismiss_attention_item`
- `archive_attention_item`
- `evaluate_attention_rules`

---

## 5. Migration comparison table

| Migration | Local | Remote history | Remote schema effect | Attention | Apply needed | Risk |
| --- | ---: | ---: | ---: | ---: | ---: | --- |
| ≤ `20260720140000` (44) | yes | yes | present | no | no | low |
| `20260805124430` | yes | no | **absent** | yes | **yes** | low–med (CREATE locks) |
| `20260805124448` | yes | no | absent | yes | **yes** | low (RLS) |
| `20260805134823` | yes | no | absent | yes | **yes** | low |
| `20260805134825` | yes | no | absent | yes | **yes** | med (large function DDL) |
| `20260805134830` | yes | no | absent | yes | **yes** | med (RLS/policy) |
| `20260805134833` | yes | no | absent | yes | **yes** | med (privilege matrix) |

---

## 6. Remote Attention schema inventory (pre-apply)

| Class | Remote status |
| --- | --- |
| Tables `attention_items` / `attention_signals` / `attention_item_events` | **missing (0)** |
| Attention types / functions | **none** |
| Policies / grants on Attention | n/a |
| Official public Attention RPCs | **missing** |

---

## 7. Dependency analysis

| Required | Remote present |
| --- | ---: |
| `organizations` | yes |
| `organization_members` (+ unique `(organization_id, id)`) | yes |
| `profiles` | yes |
| `customers` / `programs` / `enrollments` | yes |
| `enrollments` unique `(organization_id, id, customer_id, program_id)` | yes |
| `enrollments` unique `(organization_id, id)` | yes |
| `enrollment_progress_facts` | yes |
| `public.set_updated_at` | yes |
| `private` schema | yes |
| `private.has_org_role` / `private.is_org_member` | yes |

Empty Attention relations ⇒ no existing Attention-row FK/unique validation risk.

---

## 8. Destructive / data-impact classification

| Migration | Class | Notes |
| --- | --- | --- |
| `…430` | 1 non-destructive additive | no DROP/DELETE/TRUNCATE |
| `…448` | 3 privilege/RLS | revoke; deny-by-default |
| `…823` | 1 additive | events + immutability |
| `…825` | 1 + 4 (new functions) | `CREATE OR REPLACE` of **new** names; no mass DML |
| `…830` | 3 privilege/RLS | SELECT grants + policies |
| `…833` | 3 privilege/RLS | revoke/grant hardening |

No Attention class 5 (data mutation) or class 6 (destructive/blocking) in the pending set.

**Partial-apply risk:** if a later file fails after earlier succeed, remote history can be partial — stop and require separate owner-approved repair (not authorized here).

---

## 9. Exact later apply plan (not executed)

### Gate A — Baseline (owner)

- Clean Git on `24289e2…`
- Linked project `dmctinrcjvsgmoxwwodw` confirmed
- Remote history snapshot still ends at `20260720140000` with only the six Attention locals pending
- **Owner confirms Dashboard backup and/or PITR window** (agent could not list backups via MCP)
- Maintenance/impact: short additive DDL window; low expected user impact (Attention already failing)

### Gate B — Applyset

Exact six files in the order listed in §3. Unrelated pending: **none**.

### Gate C — Apply command (do **not** run without separate apply authorization)

```powershell
npx supabase db push --linked --dry-run
npx supabase db push --linked --yes
```

Do **not** use `--include-all` unless history has diverged. Do **not** use migration repair without explicit owner approval.

### Gate D — Post-apply schema checks

- Tables, columns, constraints, indexes present
- RLS enabled; six SELECT policies; authenticated SELECT-only on Attention tables
- Nine RPC signatures + execute to authenticated
- Immutability triggers; private helpers revoked from anon/authenticated/service_role per hardening

### Gate E — Read-only product smoke (current `dpl_3hf9…`)

- `/attention` loads (empty or list; **not** “Unable to load Attention”)
- Org resolution; filters; detail if fixture exists; malformed/unavailable safe
- No relation-not-found; no 5xx

### Gate F — Security smoke (no mutations)

- Owner vs viewer; suspended / no membership; second tenant; archived visibility when data exists

### Gate G — B1.7.6-F resume (later)

Only after schema + read smoke:

- Safe credentials + disposable fixtures
- Deploy product commit `24289e2…`
- Lifecycle mutation matrix + closure

---

## 10. Backup & recovery (required before apply auth)

| Topic | Rule |
| --- | --- |
| Vercel promote / rollback | **Not** a database rollback |
| Migration txn failure | File may abort; prior successful files can remain in history |
| Successful schema | Rollback requires Dashboard restore / PITR / support — no handwritten down migration without separate review |
| Owner gate | Confirm recent backup or PITR before authorizing apply |

---

## 11. Stopconditions (no apply if any true)

- Wrong linked project
- Remote migration history inconsistent
- Unexpected unrelated pending migration
- Destructive migration without approval
- Backup/PITR status unknown or unconfirmed by owner
- Missing dependency
- Constraint would break existing data
- Function signature drift vs adapters
- RLS/policy impact not established
- Dry-run shows unexpected statements
- Dirty worktree
- HEAD/upstream mismatch
- Mid-chain failure

---

## 12. Closure evidence impact (proposal only — docs not rewritten during preflight)

### B1.7.5

Claim `B1.7.5 CLOSED WITH EVIDENCE — ATTENTION WORKSPACE AND DETAIL UX PRODUCTION READY` did not prove authenticated Attention data reads. Production smoke was largely unauthenticated.

**Later correction (new commit, no history rewrite):** remediation addendum and/or superseding production verification document. Do not delete prior evidence.

### B1.7.6

- A–E remain valid as code/test publications on `24289e2…`
- **F stays blocked** until Gates E–G prerequisites are met

---

## 13. Recommended next owner decision

1. Confirm Supabase Dashboard backup/PITR for project `dmctinrcjvsgmoxwwodw`.
2. Issue a **separate** apply authorization, for example:

   `ATTENTION PRODUCTION SCHEMA APPLY AUTHORIZED — LINKED DB PUSH ONLY`

3. Execute Gate C with Gates A–F verification.
4. Do **not** resume B1.7.6-F until Gate E passes.

---

## Preflight boundaries confirmation

During readiness preflight:

- No `db push` apply
- No migration repair / `db reset` / mutating `db pull`
- No remote DDL/DML beyond read-only catalog SQL
- No fixtures / Attention mutations
- No Vercel deploy / promote
- No commit / push of this document was required by the readiness auth (document saved later by owner request to persist session)

Final closing string from preflight:

`ATTENTION PRODUCTION SCHEMA READY FOR OWNER-APPROVED APPLY — MIGRATION PLAN VERIFIED`

---

## 14. Post-Apply Production Evidence — 2026-08-07

> **Status notice**
>
> The original sections above are retained as historical pre-apply readiness
> evidence.
>
> Their operational verdict `ATTENTION PRODUCTION SCHEMA READY FOR
> OWNER-APPROVED APPLY` has now been **superseded by the completed production
> apply and post-apply verification recorded below**.
>
> Do not interpret the original pre-apply instructions as the current next
> action.

### 14.1 Owner authorization and safety preparation

Before production schema mutation:

- the linked production project was confirmed;
- the authoritative Git baseline remained
  `24289e2c6f57ca702beab085f4103d224ce484e2`;
- branch remained `core/platform-readiness-20260707`;
- HEAD/upstream divergence remained `0 0`;
- a logical production backup was created outside the repository;
- no Attention migration was applied before backup completion.

Backup location:

`D:\ZyntixAI-backups\2026-08-07-pre-attention`

Backup artifacts:

- `roles.sql`
- `schema.sql`
- `data.sql`
- `BACKUP-MANIFEST.txt`

Recorded dump sizes:

| File | Size |
| --- | ---: |
| `roles.sql` | 297 bytes |
| `schema.sql` | 247086 bytes |
| `data.sql` | 553600 bytes |

Recorded SHA-256:

```text
roles.sql  | 25873CEC56A2CC6514E204F420231777F85C03DA818CAA7090CDCDFA89776ECD
schema.sql | 198A24CA6DCF7F98CDFEE1EC1F7384CE8643C06AFE502738A8A2CD80119B51D1
data.sql   | 76B9CB0ED0D9E7AEF6FA7EED2C68B8ABDCA0B6AFC139C5A4D7C2C6E88B2BDE77
```

The logical backup was verified to exist and was recorded with a manifest.

It is classified as:

```text
PRE-ATTENTION PRODUCTION LOGICAL BACKUP — PASS WITH MANIFEST
```

The dump is not claimed as independently restore-tested.

The data-only dump emitted circular foreign-key restore warnings for existing
`enrollment_progress_facts` and `tasks` relationships. These were warnings,
not dump failures. Any future restore must therefore follow a controlled
restore procedure rather than assuming a blind data-only replay.

### 14.2 Final migration applyset verification

Immediately before production mutation, the local migration inventory was
checked again.

Exactly these six Attention migrations were present in the intended applyset:

1. `20260805124430_create_attention_items_and_signals.sql`
2. `20260805124448_enable_attention_rls_deny_by_default.sql`
3. `20260805134823_create_attention_item_events.sql`
4. `20260805134825_add_attention_helpers_and_rpcs.sql`
5. `20260805134830_enable_attention_operational_rls.sql`
6. `20260805134833_attention_security_hardening.sql`

A linked production dry-run reported exactly those six as pending.

No unrelated migration appeared.

Verdict:

```text
PRODUCTION ATTENTION APPLY DRY-RUN — PASS
EXACTLY 6/6 EXPECTED ATTENTION MIGRATIONS PENDING
```

### 14.3 Production schema apply

After explicit owner confirmation, the linked production database apply was
executed.

The CLI reported each of the six expected Attention migrations as applying:

```text
20260805124430_create_attention_items_and_signals.sql
20260805124448_enable_attention_rls_deny_by_default.sql
20260805134823_create_attention_item_events.sql
20260805134825_add_attention_helpers_and_rpcs.sql
20260805134830_enable_attention_operational_rls.sql
20260805134833_attention_security_hardening.sql
```

The command completed with:

```text
Finished supabase db push.
```

No additional migration was intentionally applied.

### 14.4 Post-apply CLI warning classification

After the migrations were applied, the CLI emitted a migration-catalog cache
warning associated with `pg-delta`.

The reported condition referenced a missing local/container certificate file:

```text
pgdelta-target-ca.crt
ENOENT
```

This warning occurred during migration-catalog caching after the migration
apply sequence.

It was **not** silently classified as harmless based on the warning alone.

Instead, a fresh linked database dry-run was executed afterward to determine
the authoritative remote migration state.

### 14.5 Independent post-apply migration-state verification

A subsequent command:

```text
supabase db push --linked --dry-run
```

reported:

```text
Remote database is up to date.
```

Therefore the six Attention migrations were no longer pending on the linked
production database.

This independently resolves ambiguity created by the post-apply `pg-delta`
catalog-cache warning.

Verdict:

```text
ATTENTION PRODUCTION SCHEMA APPLY — PASS
6/6 EXPECTED MIGRATIONS APPLIED
REMOTE DATABASE UP TO DATE
```

### 14.6 Production Attention read smoke

The original incident condition before schema apply was:

```text
GET /rest/v1/attention_items -> 404
42P01
relation "attention_items" does not exist
```

After the schema apply and remote-state verification, the authenticated
production `/attention` route was loaded again under the controlled Production
QA context.

The Attention workspace rendered again.

The prior missing-relation / `Unable to load Attention` schema-read failure no
longer reproduced.

Verdict:

```text
PRODUCTION ATTENTION READ SMOKE — PASS
```

An empty Attention workspace remains an acceptable read result where no
controlled Attention fixtures exist.

### 14.7 Git integrity after production apply

Post-apply Git verification confirmed:

```text
worktree:
D:\project ai cursus verkopers.worktrees\parallel__laptop-product-track-20260707-1

branch:
core/platform-readiness-20260707

HEAD:
24289e2c6f57ca702beab085f4103d224ce484e2

upstream:
24289e2c6f57ca702beab085f4103d224ce484e2

divergence:
0 0
```

The production database operation did not alter tracked product code or
migration files.

This readiness/evidence document itself remained an explicit documentation
artifact and was not treated as product-code drift.

### 14.8 B1.7.6-F status after schema remediation

The production schema blocker is resolved.

However:

```text
B1.7.6-F IS NOT CLOSED BY SCHEMA APPLY ALONE
```

The authoritative B1.7.6 contract still requires controlled production
verification of lifecycle behavior using explicitly designated QA fixtures and
authorized QA actors.

Required production verification includes applicable:

- acknowledge;
- severity;
- assign / unassign;
- resolve;
- dismiss;
- archive;
- role restrictions;
- tenant/security boundaries;
- timeline/audit event;
- refresh persistence;
- duplicate-submit behavior.

The repository separately defines the successor fixture-readiness sequence:

```text
B1.7-R1-FIXTURES
```

Fixture preparation must not be silently claimed inside the schema remediation
or inside B1.7.6-F.

### 14.9 Superseding operational verdict

The original pre-apply verdict remains retained above for historical audit
purposes.

Its operational status is superseded by:

```text
ATTENTION PRODUCTION SCHEMA APPLIED AND VERIFIED —
6/6 MIGRATIONS LIVE
REMOTE DATABASE UP TO DATE
PRODUCTION ATTENTION READ SMOKE PASS
```

Current remaining gate:

```text
B1.7.6-F REMAINS OPEN FOR CONTROLLED LIFECYCLE PRODUCTION QA

NEXT AUTHORITATIVE SEQUENCE PREPARATION:
B1.7-R1-FIXTURES
```

No additional schema apply is pending from this six-migration Attention set.

Do not re-run the completed production schema apply merely because the earlier
pre-apply sections remain preserved in this document.
