# B1.7.5 — Attention Workspace & Detail UX — Closure Evidence

| Field | Value |
| --- | --- |
| Phase | **B1.7.5 — Workspace & Detail UX** |
| Official title (B1.7.0 §27) | **Workspace & detail UX** |
| Subtitle | Attention Workspace UX |
| Document type | Closure / production verification evidence (B1.7.5-F) |
| Date | 2026-08-06 |
| Formal status | `CLOSED WITH EVIDENCE` |
| Governing contract | `docs/phases/B1.7.5-attention-workspace-and-detail-ux.md` |
| Contract approval commit | `6e0e777c…` (`docs(attention): approve B1.7.5 workspace UX contract`) |
| Implementation baseline (pre-A) | `b2a65020…` predecessor chain starts at contract + A |
| Closure deploy commit | `78b4cd7dffdcd835dcd7645a938dc5eb589a00ce` |
| Branch | `core/platform-readiness-20260707` |
| Worktree | `D:\project ai cursus verkopers.worktrees\parallel__laptop-product-track-20260707-1` |

**This phase does not claim** lifecycle mutation UX, B1.7.6 actions, new RPCs/migrations/RLS, AI/NBA/notifications, or a full external WCAG certification.

---

## 1. Formal status and verdict

```text
B1.7.5 CLOSED WITH EVIDENCE — ATTENTION WORKSPACE AND DETAIL UX PRODUCTION READY
```

---

## 2. Owner authorization (B1.7.5-F)

```text
B1.7.5-F AUTHORIZED — FULL QA, AUTHORIZED DEPLOYMENT, READ-ONLY PRODUCTION VERIFICATION AND PHASE CLOSURE
```

---

## 3. Closed subphases A–F

| Subphase | Commit | Subject / verdict |
| --- | --- | --- |
| Contract APPROVED-1 | `6e0e777c…` | `docs(attention): approve B1.7.5 workspace UX contract` |
| **A** Presentation Foundation | `62c5c7ab…` | CLOSED — presentation foundation ready |
| **B** List workspace | `ad0fc438…` | CLOSED — list workspace ready |
| **C** Filters/sort/pagination | `b2a65020…` | CLOSED — list controls & URL state ready |
| **D** Detail & timeline | `f796a0db…` | CLOSED — read-only detail and timeline ready |
| **E** Navigation & enrollment access | `78b4cd7d…` | CLOSED — navigation, a11y, enrollment access ready |
| **F** QA / deploy / closure | *(this docs commit)* | CLOSED — production ready |

Ancestry: A→E commits are ancestors of deploy HEAD `78b4cd7d…` on `origin/core/platform-readiness-20260707`.

---

## 4. Git baseline (F start → deploy)

| Field | Value |
| --- | --- |
| F preflight HEAD / upstream | `78b4cd7dffdcd835dcd7645a938dc5eb589a00ce` |
| Divergence | `0 0` |
| Worktree | clean |
| Package manager | npm |
| Deploy commit (exact) | `78b4cd7dffdcd835dcd7645a938dc5eb589a00ce` |

---

## 5. Delivered capabilities

1. Organization-scoped read-only `/attention` list workspace (filters, sort, pagination, URL state).
2. Read-only `/attention/[attentionItemId]` detail with signals + immutable event timeline from `getAttentionItemById`.
3. Uniform unavailable presentation (no existence / cross-tenant leaks).
4. Safe list ↔ detail return via validated `/attention` query state (Progress pattern).
5. `ATTENTION_NAV_VISIBLE = true` with AppShell active state for list and detail.
6. Protected-path + safe-return allowlist for `/attention` and `/attention/*` (prefix `/attention-evil` rejected).
7. Enrollment detail read-only entrypoint **View attention** → `/attention?org=…&enrollmentId=…` (no count/preflight query).
8. Accessibility structure: landmarks, labels, breadcrumbs, unique list link names, pagination semantics.

---

## 6. Explicit exclusions

- B1.7.6 lifecycle controls (acknowledge/assign/resolve/dismiss/archive UX and working mutation CTAs).
- New RPCs, read models, migrations, RLS, package/lockfile changes.
- Attention count badges / preflight existence queries.
- AI summaries, NBA, notifications, timeline export, raw audit payload UI.
- Full external WCAG audit certification.
- Live multi-tenant authenticated production probes with secondary QA identities (no credentials available in agent session).

---

## 7. Quality gates executed

### Repository quality

| Gate | Result |
| --- | --- |
| `npm run typecheck` | PASS |
| `npm run lint` | PASS (0 warnings/errors) |
| `npm run build` (local production) | PASS — routes include `/attention` and `/attention/[attentionItemId]` |
| `git diff --check` | PASS (clean tree) |

### Automated regression

Focused Attention + platform suite (25 files):

| Suite | Result |
| --- | --- |
| B1.7.5 A–E UI/loaders/presentation/a11y/nav/enrollment | PASS |
| B1.7.4 reads/mappers/permissions/RPC adapters/security | PASS |
| Safe-return / protected paths | PASS |
| **Total** | **152 / 152 passed** |

Security matrix coverage (tests, not live multi-tenant):

| Scenario | Evidence |
| --- | --- |
| Authorized detail / timeline | detail loader + presentation tests |
| Malformed ID → unavailable without service call | detail loader tests |
| Cross-tenant / permission → uniform unavailable | detail loader + safe error presentation |
| Archived visibility by role | permissions + list filter warnings + B1.7.4 read tests |
| Safe return rejects external / protocol-relative | detail loader + `safe-return-path` tests |
| Prefix `/attention-evil` not Attention | domain + safe-return tests; production 404 |
| Nav visibility ≠ authorization | routes still require auth; nav flag only UI |
| No mutation controls | boundary + presentation tests; lifecycle flag false |
| Enrollment entrypoint href shape | enrollment presentation + entrypoint tests |

---

## 8. Browser / viewport / accessibility

### Tooling

- No Playwright package in repository (not installed).
- Local production server `next start -p 3010` + Cursor browser MCP.
- Production alias browser probes after promote.

### Viewports checked (login / auth gate surfaces)

| Viewport | Result |
| --- | --- |
| Desktop ~1440–1920 wide | No horizontal overflow; single H1; labelled email/password |
| Tablet 768×1024 | Emulation applied; structure intact |
| Mobile 390×844 | No horizontal overflow; single H1 |

Authenticated list/detail/timeline browser journeys were **not** executed live (no QA login session in agent). Structure and behavior covered by DOM/component tests (`attention-accessibility`, detail/list presentation, enrollment entrypoint).

### Accessibility (automated structural + DOM)

Verified via tests/DOM:

- Primary nav landmark + `aria-current` when Attention active.
- Filter form `aria-labelledby` + control labels.
- List link accessible names (`Open attention item: …`).
- Pagination nav label + `aria-current="page"`.
- Detail breadcrumb landmark; timeline ordered list; empty timeline copy.
- Status/severity as text labels (not color-only).
- Focus-visible styles present on nav/list/detail/enrollment links.

**Not claimed:** full WCAG AA certification; live screen-reader audit; contrast meter run; reduced-motion product animation audit (Attention UI is largely static).

---

## 9. Deployment

| Field | Value |
| --- | --- |
| Project | `zyntixai` (`prj_CannZeiiNP8JdiEPHF7qD4qJDJaW`) |
| Scope | `guus-projects-ai` |
| Method | Established candidate-then-promote (`vercel deploy --prod --yes --skip-domain` → `vercel promote`) |
| Deploy commit | `78b4cd7dffdcd835dcd7645a938dc5eb589a00ce` |
| Deployment ID | `dpl_HjvV7tkeGA9D7LAJfKU2G2eV6VUh` |
| Immutable URL | `https://zyntixai-nf6h18jpj-guus-projects-ai.vercel.app` |
| Ready status | Ready |
| Primary alias | `https://zyntixai.vercel.app` → `dpl_HjvV7tkeGA9D7LAJfKU2G2eV6VUh` after promote |
| Rollback baseline (prior Ready) | `dpl_7VcDK2PB4JtVPeFBWVfqFKjpicVW` |
| Rollback executed? | **No** |

Build output confirms Attention lambdas (`attention`, `attention/[attentionItemId]`).

---

## 10. Read-only production smoke

**No product data was created, updated, deleted, acknowledged, assigned, resolved, dismissed, or archived.**

| Probe | Result |
| --- | --- |
| `GET /attention` (unauthenticated) | Redirect → `/login?next=%2Fattention` |
| `GET /attention/{uuid}` (unauthenticated) | Redirect → `/login?next=%2Fattention%2F{uuid}` |
| `GET /attention/not-a-uuid` (unauthenticated) | Redirect → `/login?next=%2Fattention%2Fnot-a-uuid` |
| `GET /enrollments` (unauthenticated) | Redirect → `/login?next=%2Fenrollments` |
| `GET /attention-evil` | **404** (not treated as Attention) |
| Alias binding | Primary alias serves new Ready deployment with Attention routes |

Authenticated in-app smoke (nav visible, list filters, detail timeline, enrollment **View attention**) requires an operator QA session and was **not** performed by the agent. Automated tests cover those behaviors locally.

Observability (read-only): deployment Ready; no agent-observed 5xx on probed Attention auth redirects; login H1 renders; no hydration error observed on probed pages.

---

## 11. Remediations during F

None. No code remediation commit required; all blocking automated gates passed on `78b4cd7d…`.

---

## 12. Deferred items

| Item | Target |
| --- | --- |
| Lifecycle mutation UX | **B1.7.6** |
| Live authenticated multi-role / multi-tenant production probes | Owner / later PROD-R verification |
| Full browser device matrix + external a11y audit | Optional hardening |
| Attention count badges on enrollment | Only if authorized read capability exists later |

---

## 13. Recommended next phase

Do **not** start from this document alone without separate owner authorization:

```text
B1.7.6 — Attention Lifecycle Actions (owner authorization required)
```

Optional: authenticated production re-verification of Attention nav/list/detail/enrollment entry by an owner QA session on `https://zyntixai.vercel.app`.

---

## 14. Commands executed (F)

```text
git rev-parse --show-toplevel / branch / HEAD / @{upstream} / divergence / status / log
npm run typecheck
npm run lint
npm run build
npm run test:run -- <25 attention/platform test files>
npm run start -- -p 3010
npx vercel inspect zyntixai.vercel.app --scope guus-projects-ai
npx vercel deploy --prod --yes --project zyntixai --scope guus-projects-ai --skip-domain --meta …
npx vercel inspect dpl_HjvV7tkeGA9D7LAJfKU2G2eV6VUh --scope guus-projects-ai
npx vercel promote dpl_HjvV7tkeGA9D7LAJfKU2G2eV6VUh --scope guus-projects-ai --yes
npx vercel inspect zyntixai.vercel.app --scope guus-projects-ai
browser probes: local + https://zyntixai.vercel.app Attention/login/enrollments/attention-evil
```

---

## 15. Closure confirmation

- No production data mutation.
- No migration / RLS / RPC / package changes in F.
- Rollback baseline retained (`dpl_7VcDK2PB4JtVPeFBWVfqFKjpicVW`); not used.
- Production alias points at Ready deployment for commit `78b4cd7d…` (pre-closure-docs). Docs-only closure commit may trigger a follow-up Ready deployment; verify alias after push.
