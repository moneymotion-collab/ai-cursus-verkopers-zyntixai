# B1.4-PROD-R1 — Controlled Production Deployment and Production Record Verification

| Field | Value |
| --- | --- |
| Phase | B1.4-PROD-R1 — Controlled Production Deployment and Production Record Verification |
| Parent track | B1 — Controlled Beta Entry |
| Status | **BLOCKED** — public production alias does not resolve to the new Ready deployment |
| Operational verdict | `B1.4-PROD-R1 ALIAS MISMATCH` |
| Authoritative HEAD | `27a78fde797ffb955059ef4dc76d1f8b903e4042` |
| Branch | `core/platform-readiness-20260707` |
| Commit / push during this phase | **NO** |

---

## 1. Executive Summary

Local B1.4 closure HEAD was clean and validated. Pre-operation production alias `https://zyntixai.vercel.app` resolved to older deployment `dpl_6TkcmecE1SFBWbkz6UAJCbr3bpbr` (created 2026-07-21), which **cannot** contain expected commit `27a78fd` (authored 2026-07-23). One controlled production deployment was therefore required and executed from the authoritative worktree.

The new deployment `dpl_9wNjmVwYFCNUCMpkfQsxKQupWc7S` built successfully and is **Ready**, but the public alias `https://zyntixai.vercel.app` **still resolves to the previous deployment**. Per phase rules, alias settings were **not** modified automatically. Authenticated production verification against the public alias was therefore **not** executed for the new deployment.

```text
BLOCKED
```

```text
B1.4-PROD-R1 ALIAS MISMATCH
```

---

## 2. Final Verdict

```text
BLOCKED
```

Production cannot be claimed to run the B1.4 closure commit via the public alias until `https://zyntixai.vercel.app` targets the verified Ready deployment (or an equivalent proven successor of the same source).

---

## 3. Authoritative Worktree and Branch

| Item | Value |
| --- | --- |
| Worktree | `D:/project ai cursus verkopers.worktrees/parallel__laptop-product-track-20260707-1` |
| Branch | `core/platform-readiness-20260707` |
| Upstream | `origin/core/platform-readiness-20260707` |

---

## 4. Local and Upstream Git Evidence

| Check | Result |
| --- | --- |
| Local HEAD | `27a78fde797ffb955059ef4dc76d1f8b903e4042` |
| Subject | `docs(onboarding): close B1.4 production verification` |
| Parent | `5318672067d83191be08a00e0b842c9d20d859fa` |
| Upstream SHA | identical |
| Divergence | `0 0` |
| Preflight dirty counts | staged/modified/deleted/renamed/untracked/conflicts all **0** |

---

## 5. Pre-Verification Validation Commands

| Command | Exit | Result |
| --- | ---: | --- |
| `npx vitest run tests/onboarding/first-value-checklist.test.ts tests/ui/first-value-checklist-ui.test.tsx` | 0 | **2 files / 24 tests PASS** |
| `npm run typecheck` | 0 | **PASS** |
| `npm run build` | 0 | **PASS** (`ƒ /onboarding`, `ƒ /leads`) |

Non-secret warning only: npm `devdir` config notice.

---

## 6. Vercel Project Identity

| Item | Value |
| --- | --- |
| Account/team (scope) | `guus-projects-ai` |
| Project | `zyntixai` |
| Expected production alias | `https://zyntixai.vercel.app` |
| CLI identity | authenticated (`vercel whoami` succeeded; value not required here) |

---

## 7. Production State Before This Operation

| Item | Value |
| --- | --- |
| Alias inspected | `https://zyntixai.vercel.app` |
| Deployment ID | `dpl_6TkcmecE1SFBWbkz6UAJCbr3bpbr` |
| Immutable URL | `https://zyntixai-agrhhb232-guus-projects-ai.vercel.app` |
| Status | Ready |
| Created | 2026-07-21 13:35:00 +0200 |
| Expected commit author date | 2026-07-23 15:22:23 +0200 |
| Pre-state classification | **PRODUCTION BEHIND EXPECTED COMMIT** |

Evidence that the pre-state deployment cannot contain `27a78fd`:

1. Vercel creation timestamp of the alias target precedes the expected commit authorship date.
2. Published B1.3 production record identifies the same deployment ID as the B1.3 remediation app commit `ceb2ef4…`, which is an ancestor of `27a78fd` but not equal to it.
3. Commits between that ancestor and HEAD include B1.4 implementation `5318672…` and docs closure `27a78fd…`.

Vercel inspect JSON for the old deployment exposed **no** `gitCommitSha` / `githubCommitSha` fields (CLI-style deploy metadata gap). Temporal + historical deployment-ID linkage was used to classify **BEHIND**, not **AMBIGUOUS**.

---

## 8. Whether a New Deployment Was Necessary

**YES** — state B (`PRODUCTION BEHIND EXPECTED COMMIT`). Duplicate deploy was not optional.

---

## 9. Exact Deployment Command (Secrets Redacted)

```text
npx vercel deploy --prod --yes --project zyntixai --scope guus-projects-ai \
  -m gitCommitSha=27a78fde797ffb955059ef4dc76d1f8b903e4042 \
  -m gitCommitMessage=docs(onboarding): close B1.4 production verification \
  -m gitCommitRef=core/platform-readiness-20260707
```

| Item | Value |
| --- | --- |
| Exit code | `0` |
| Build | Succeeded on Vercel (Next.js 15.5.20) |

No `--token` was passed on the command line.

---

## 10. Deployment ID

`dpl_9wNjmVwYFCNUCMpkfQsxKQupWc7S`

---

## 11. Immutable Deployment URL

`https://zyntixai-7pnvx1uha-guus-projects-ai.vercel.app`

---

## 12. Deployment Status

**Ready** (`readyState=READY`, target=`production`)

Assigned aliases observed on the **new** deployment (not the primary public alias):

- `https://zyntixai-guus-projects-ai.vercel.app`
- `https://zyntixai-guusvermolen-6030-guus-projects-ai.vercel.app`

---

## 13. Source Commit Evidence

| Evidence class | Finding |
| --- | --- |
| Pre-deploy local HEAD | Exact `27a78fde797ffb955059ef4dc76d1f8b903e4042` |
| Worktree cleanliness at deploy | Clean |
| Deploy command metadata flags | `-m gitCommitSha=27a78fd…` supplied |
| Vercel inspect JSON git fields after deploy | **Not returned** by CLI inspect payload (no `meta` / commit fields exposed) |
| Inference from UI strings alone | **Not used** as source proof |

Source identity for the new deployment is therefore proven by **controlled deploy-from-exact-HEAD** process evidence, not by Vercel inspect commit fields.

---

## 14. Alias Target Evidence (Blocking)

| Check | Result |
| --- | --- |
| `vercel inspect zyntixai.vercel.app` after deploy | Still `dpl_6TkcmecE1SFBWbkz6UAJCbr3bpbr` |
| Expected new deployment on public alias | `dpl_9wNjmVwYFCNUCMpkfQsxKQupWc7S` |
| Match | **NO** |
| Automatic alias/domain/project-setting mutation | **NOT PERFORMED** (forbidden) |

Re-check after short wait: mismatch **persisted**.

---

## 15. Unauthenticated Route Verification

### Immutable new deployment

Unauthenticated HTTP probes redirected to Vercel SSO / deployment-protection challenge (`vercel.com/sso-api?...`). Public app HTML smoke against the immutable hostname was therefore **not** freely available without Vercel auth wall.

### Public alias (still old deployment)

| Route | Result |
| --- | --- |
| `/` | 307 → `/login` |
| `/login` | 200, Sign-in UI present |
| `/leads` (unauthenticated) | 307 → `/login?next=%2Fleads` |

These alias results prove the **old** production app still serves the public hostname; they are **not** evidence that B1.4 is live on the alias.

---

## 16. Authenticated QA Method

Not executed for the new deployment against the public alias.

Planned method (when alias matches): human operator enters dedicated QA owner credentials directly in the browser on `https://zyntixai.vercel.app/login`. Password never requested, inspected, printed, or stored by the agent.

---

## 17–22. Authenticated / Checklist / Refresh / Logout Evidence

**NOT EXECUTED** — blocked by public-alias mismatch to the new Ready deployment.

Prior B1.4-R1-R2 local+hosted evidence remains historical context only and is **not** reused here as production-alias proof.

---

## 23. Authorization / RLS Observations (this phase)

No authenticated production-alias session was opened against the new deployment. No RLS conclusion for the new alias target is claimed.

---

## 24. Tenant-Isolation Observations (this phase)

Not executed on the public alias for the new deployment.

---

## 25. Zero Leads / Zero Tasks

Not re-verified in this phase against production UI (blocked). No product-data mutation was performed that would change those counts.

---

## 26. Complete Mutation Inventory

| Mutation | Performed? |
| --- | --- |
| One controlled Vercel production deployment from authoritative HEAD | **YES** |
| Public alias / domain / project-setting change | **NO** |
| Vercel environment-variable change | **NO** |
| Database / SQL / service-role mutation | **NO** |
| Supabase Auth mutation | **NO** |
| Application data create/update (leads/tasks/customers/checklist) | **NO** |
| Git stage / commit / push | **NO** |
| Application source / tests / migrations / config edits | **NO** |

---

## 27. No Database / Auth / Environment / Domain / Project-Setting Mutation

Confirmed for this phase, except the single permitted production deployment upload/build.

---

## 28. Credentials and Secret-Handling Confirmation

- No QA password requested or handled.
- No tokens, cookies, Authorization headers, or environment values printed in this report.
- Deploy used existing CLI login; no token flag on argv.

---

## 29. Limitations or Uncertainties

1. **Blocking:** `https://zyntixai.vercel.app` does not target `dpl_9wNjmVwYFCNUCMpkfQsxKQupWc7S`.
2. Vercel inspect does not expose git commit SHA fields for these CLI deployments; source proof relies on deploy-from-exact-HEAD controls + command metadata intent.
3. Immutable deployment hostname is behind Vercel SSO/deployment protection for anonymous probes.
4. Why `--prod` assigned team aliases but not the primary `zyntixai.vercel.app` hostname is unresolved without changing project/domain settings (forbidden here).

---

## 30. Final Production-Readiness Conclusion

```text
NOT PRODUCTION-READY VIA PUBLIC ALIAS
```

A Ready deployment of the authoritative B1.4 closure HEAD exists, but the public production record (`https://zyntixai.vercel.app`) still serves the prior deployment. B1.4 must **not** be claimed live on the public alias.

### Required next action (outside this automatic phase)

Operator-authorized assignment/promotion so that `https://zyntixai.vercel.app` targets `dpl_9wNjmVwYFCNUCMpkfQsxKQupWc7S` (or redeploy with proven primary-domain attachment), then re-run alias verification and authenticated QA journeys.

### Safest next product phase

```text
B1.4-PROD-R1-R2 — public-alias attachment + authenticated production reverification
(after operator-approved alias/domain promotion to the Ready deployment)
```

---

## 31. Repository Hygiene After Report Creation

Expected ending dirty set for this phase:

- exactly one untracked path: this report;
- HEAD remains `27a78fde797ffb955059ef4dc76d1f8b903e4042`;
- divergence `0 0`;
- no commit/push.
