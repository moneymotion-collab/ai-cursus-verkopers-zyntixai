# B1.5.5-R1 — Enrollments List, Create and Detail Authenticated Browser and Live Authorization Verification

| Field | Value |
| --- | --- |
| Phase | B1.5.5-R1 — Authenticated Browser and Live Authorization Verification |
| Parent track | B1.5 — Programs & Enrollments Workspace |
| Status | Authenticated browser verification complete |
| Baseline commit at R1 close | `528106dfe7e341c522285ca55cfa00ca34815dd2` (`docs(enrollments): close B1.5.4 publication`) |
| Branch | `core/platform-readiness-20260707` |
| Authoritative contract | `docs/phases/B1.5-programs-and-enrollments-workspace-design-security-and-readiness-contract.md` |
| Implementation report | `docs/phases/B1.5.5-enrollments-list-create-and-detail-workflows.md` |
| Independent QA report | `docs/phases/B1.5.5-QA-enrollments-list-create-and-detail-workflows-independent-qa.md` |
| R1 mutation | This report only for source/docs inventory (plus exactly one permitted Staff UI Enrollment create). No source/test edits; no Auth/membership/migration/deploy mutation |

---

## 1. Executive Summary

B1.5.5-R1 verified Enrollments list, create, and read-only detail against the dedicated **ZyntixAI Production QA** organization on a local worktree production runtime (`http://localhost:3014`, rebuilt and served via `npx next start -p 3014 -H 127.0.0.1`). Human operator completed Owner, Staff, and Viewer authenticated browser handoffs. Agent-assisted sessions exercised Owner list/filter/create-page (no submit), Staff validation fail + exactly one create + duplicate-open conflict + persistence, and Viewer read/denial/archived fail-closed. Post-browser automated regression matched the known green baseline. No Admin live account existed; classified non-blocking. Archived enrollment detail was not directly observable (no archived fixture). Multi-page pagination remained single-page.

---

## 2. Final Verdict

```text
PASS WITH CONDITIONS — READY FOR B1.5.5 PUBLICATION
```

Operational verdict:

```text
B1.5.5-R1 PASS WITH CONDITIONS — READY FOR PUBLICATION
```

---

## 3. Git Baseline

| Item | Value |
| --- | --- |
| Worktree | `D:/project ai cursus verkopers.worktrees/parallel__laptop-product-track-20260707-1` |
| Branch | `core/platform-readiness-20260707` |
| HEAD at R1 close | `528106dfe7e341c522285ca55cfa00ca34815dd2` |
| Subject | `docs(enrollments): close B1.5.4 publication` |
| Upstream | `origin/core/platform-readiness-20260707` at same SHA |
| Divergence | `0 0` |
| Commit/push during R1 | **None** |
| R1 source/test edits | **None** |

Pre-existing dirty inventory remains the unpublished B1.5.5 implementation + QA docs/tests/routes (unchanged by R1 except addition of this report).

---

## 4. Runtime Command and URL

| Item | Value |
| --- | --- |
| Build/runtime | Worktree production build; `npx next start -p 3014 -H 127.0.0.1` |
| Base URL used in browser | `http://localhost:3014` (consistent host for cookies; `127.0.0.1` also served) |
| Hosted Auth / data | Supabase project bound by local `.env.local` public keys only |
| Production alias used as primary proof | **No** (local worktree runtime required for undeployed B1.5.5 UI) |

---

## 5. Unauthenticated Route Results

| Route | Result |
| --- | --- |
| `GET /login` | **200**, sign-in form present |
| `GET /enrollments` | **307** → `/login` with safe `next=/enrollments` |
| `GET /enrollments/new` | **307** → `/login` with safe `next=/enrollments/new` |
| `GET /enrollments/[id]` | **307** → `/login` with safe `next` path |

Safe return-path behavior matched existing protected-route patterns.

---

## 6. QA Role-Account Evidence

Dedicated organization: **ZyntixAI Production QA**.

| Role | Availability | Live journey |
| --- | --- | --- |
| Owner | Present (1) | Executed |
| Staff | Present (1) | Executed |
| Viewer | Present (1) | Executed |
| Admin | Absent (0) | `ADMIN LIVE JOURNEY NOT EXECUTED` |

No Auth users, memberships, or roles were created or changed during R1. Credentials were entered only by the human operator in the browser; never requested, printed, stored, or reported by the agent.

Operator checkpoints used:

- `QA OWNER LOGIN COMPLETE`
- `QA STAFF LOGIN COMPLETE`
- `QA VIEWER LOGIN COMPLETE`

---

## 7. Owner Login Evidence

**Source:** Human operator confirmation + agent-assisted Owner session.

- Login succeeded; org resolved to **ZyntixAI Production QA**.
- Primary nav: Home → Leads → Customers → Programs → **Enrollments** → Tasks (Enrollments once; href `/enrollments`).
- Owner-like signals: create CTA present; **Show archived enrollments** filter present; Leads assignee filter showed Owner display name.

---

## 8. Owner Enrollments-List Results

- `/enrollments` loaded successfully before any create.
- **New enrollment** CTA visible.
- **Show archived enrollments** present.
- Empty active list: **Showing 0 of 0 enrollments** with “No enrollments yet” copy.
- No lifecycle/edit/archive controls on list.

---

## 9. Search / Filter / Sort / Archived Results (Owner)

| Check | Result |
| --- | --- |
| Impossible search (`q=zzz-impossible-enrollment-search-xyz`) | “No enrollments match the selected filters.” + Clear filters |
| Clear / return to empty list | “No enrollments yet”; Showing 0 of 0 |
| `status=pending&sort=updated_at&direction=asc` | Filters reflected Pending / Updated date / Ascending; no-results on empty set |
| `archived=true` | Checkbox checked; “No archived enrollments are available.” |
| `archived=1` (non-canonical) | Did **not** activate archived mode (param must be `true`) |
| Multi-page pagination | **Not exercised** — single page only (non-blocking) |

---

## 10. Empty-State Evidence Classification

- Active empty state: **directly observed** (Owner, pre-create).
- Filtered no-results: **directly observed** (Owner impossible search / pending filter).
- Archived empty state: **directly observed** (Owner `archived=true`).
- Archived enrollment **detail**: `ARCHIVED ENROLLMENT DETAIL NOT DIRECTLY OBSERVED` (no archived fixture; non-blocking).

---

## 11. Owner Create-Page Results (No Submit)

Owner opened `/enrollments/new` without submitting:

- Fields: Customer (required), Program (required), Initial status (`pending` \| `active`, default Pending), Owner (optional, default Unassigned).
- Eligible options observed: customer `zynrtix`; program `B1.5.2 QA Test Program`.
- No organization picker, role picker, source control, or metadata editor in the form inputs.
- Supporting copy mentions deferred metadata changes (not a form field).
- **No Owner create mutation** (create reserved for Staff per R1 plan).

---

## 12. Owner Unavailable Detail Results

| Probe | Result |
| --- | --- |
| Malformed id (`/enrollments/not-a-uuid`) | “Enrollment unavailable” safe copy |
| Well-formed missing UUID | Same unavailable copy; no leakage |

Owner then logged out fully to `/login`.

---

## 13. Staff Login and List Results

**Source:** Human operator confirmation + agent-assisted Staff session.

- Login succeeded; org **ZyntixAI Production QA**.
- Viewer/Owner contrast: Leads had no Owner-named assignee label; Enrollments list had **New enrollment** CTA and **no** archived checkbox.
- Empty list initially (Showing 0 of 0) before create.
- URL tamper `?archived=true`: no archived checkbox; not archived-empty copy; remained non-archived empty behavior (fail closed).

---

## 14. Staff Create Validation Result

Empty submit on `/enrollments/new`:

- Remained on create page.
- Banner: **Please correct the highlighted fields and try again.**
- Field errors: **Invalid uuid** on Customer and Program.
- No Enrollment created by the failed attempt.

---

## 15. Create Mutation Result

```text
ONE QA ENROLLMENT CREATED
```

| Field | Value |
| --- | --- |
| Actor | Staff (UI only) |
| Customer | `zynrtix` |
| Program | `B1.5.2 QA Test Program` |
| Initial status | Pending |
| Owner | Unassigned |
| Source (read model) | Manual |
| Post-create landing | Enrollment detail under `/enrollments/[enrollmentId]` |
| Retain policy | **Retain** for later phases; do not transition/edit/archive/restore outside approved scope |

---

## 16. Enrollment Detail Results (Staff)

- Title: `zynrtix · B1.5.2 QA Test Program`
- Status Pending; Archive status Not archived; Owner Unassigned; Source Manual
- Status history showed initial “Set to Pending” (Manual)
- Explicit read-only deferral copy present; no status/owner/metadata/archive/restore controls

---

## 17. Soft/Hard Persistence Results

| Check | Result |
| --- | --- |
| Hard refresh of detail | Values persisted (Pending / zynrtix / program / Unassigned / Manual) |
| List after create | **Showing 1 of 1 enrollments**; row matched create |
| Post-duplicate list | Still **Showing 1 of 1** |

---

## 18. Duplicate-Open Conflict Result

Second Staff create attempt with the same customer + program:

- Remained on `/enrollments/new`
- Safe message: **An open enrollment already exists for this customer and program.**
- No second Enrollment created

---

## 19. Staff Logout

Staff logout returned to `/login`.

---

## 20. Viewer Login and Read Results

**Source:** Human operator confirmation + agent-assisted Viewer session.

- Login succeeded; org **ZyntixAI Production QA**.
- Viewer signals: Leads had **no** New lead CTA; Enrollments had **no** New enrollment CTA and **no** archived checkbox.
- List readable: **Showing 1 of 1** with the Staff-created row.
- Detail readable (hard navigation): Pending / zynrtix / program / read-only copy; no create CTA.

---

## 21. Viewer Create Denial

Direct `/enrollments/new`:

- **Create enrollment unavailable**
- “You do not have permission to create enrollments.”
- No functional create form

---

## 22. Viewer Archived Restriction

`?archived=true`:

- No archived checkbox
- Warning: **Filters adjusted** / “Some filters were reset because they were invalid.”
- Non-archived list retained (**Showing 1 of 1**; row still visible)
- Not archived-empty mode

Viewer logout returned to `/login`.

---

## 23. Admin Live Journey

```text
ADMIN LIVE JOURNEY NOT EXECUTED
```

**Non-blocking** because:

1. Staff proved the positive create journey in browser.
2. Owner proved archived-filter positive capability.
3. Automated tests cover Admin permission paths with Owner/Staff create capability.
4. Viewer proved create denial and archived fail-closed.
5. No QA Admin membership existed; creating one was forbidden in R1.

---

## 24. Tenant-Isolation Evidence

- All accounts resolved the same dedicated QA organization.
- List/detail showed only the QA enrollment fixture pair (customer/program labels expected for this org).
- Unavailable probes did not distinguish foreign vs missing (safe collapse).
- No other-tenant data observed.

---

## 25. Authenticated / RLS-Path Observations

- Authenticated Enrollments routes loaded through ordinary membership-resolved org context.
- Create mutation succeeded only for Staff in the live mutation slot (Owner intentionally did not create; Viewer denied).
- Duplicate-open rejected by authoritative open-participation constraint with safe UI copy.
- Staff/Viewer archived URL tamper failed closed without archived mode.
- No service-role usage for Enrollment mutations during R1.
- No Auth/membership/role changes during R1.
- Read-only SQL count after journeys: org enrollments **1** (active **1**, pending **1**).

---

## 26. Exact Mutation Inventory

| Mutation class | Result |
| --- | --- |
| Enrollments created | **1** (Staff UI) — `ONE QA ENROLLMENT CREATED` |
| Failed create attempts retained | **0** (validation fail + duplicate-open rejected) |
| Enrollment status / owner / metadata changes | **0** |
| Enrollments archived / restored | **0** |
| Customers / Programs created or changed | **0** |
| Leads / Tasks changed | **0** |
| Auth users / memberships / roles changed | **0** |
| Organizations changed | **0** |
| Migrations / deploys | **0** |
| Source / test file edits during R1 | **0** |
| Docs added during R1 | **This report only** |

---

## 27. Post-Browser Regression Results

### Targeted B1.5.5 workflow pack (8 files / 54 tests)

| Metric | Result |
| --- | --- |
| Exit code | **0** |
| Test files | **8** passed |
| Tests | **54** passed |

### All Enrollments-relevant vitest

| Metric | Result |
| --- | --- |
| Exit code | **0** |
| Test files | **18** passed |
| Tests | **188** passed |

### B1.5 Programs + Enrollments vitest

| Metric | Result |
| --- | --- |
| Exit code | **0** |
| Test files | **41** passed |
| Tests | **311** passed |

### Broader gates

| Command | Exit | Result |
| --- | ---: | --- |
| `npm run test:run` | **0** | **185** files / **1277** tests PASS |
| `npm run typecheck` | **0** | PASS |
| `npm run lint` | **0** | PASS (`next lint`; deprecation notice only) |
| `git diff --check` | **0** | PASS |

Counts match the accepted B1.5.5 / B1.5.5-QA baseline. Non-secret warnings: npm `devdir`; Next.js `next lint` deprecation.

---

## 28. Credential-Handling Confirmation

- No credentials entered into Cursor chat.
- No credentials printed in terminal output or this report.
- Operator entered passwords only in the browser login form.
- Agent did not request passwords after human login checkpoints.
- User IDs, org IDs, membership IDs, and enrollment UUIDs are redacted from narrative proof where not required.

---

## 29. Limitations and Conditions

| ID | Condition |
| --- | --- |
| C1 | `ADMIN LIVE JOURNEY NOT EXECUTED` — Admin membership absent; non-blocking (see §23). |
| C2 | `ARCHIVED ENROLLMENT DETAIL NOT DIRECTLY OBSERVED` — no archived enrollment fixture; archived empty list mode was observed for Owner. |
| C3 | Multi-page pagination not exercised — fixture volume remained one page. |
| C4 | Eligible create-option lists capped at 100; live org had far fewer options (cap not stress-tested live). |
| C5 | Soft client navigations sometimes required hard `location.assign` in the automation browser; product routes themselves loaded correctly on hard navigation. |

None of the above blocks publication readiness under R1 verdict rules.

---

## 30. Publication-Readiness Conclusion

Authenticated Owner/Staff/Viewer browser evidence, controlled Staff create inventory (exactly one retained), duplicate-open rejection, Viewer create denial, archived fail-closed behavior, unavailable detail collapse, and green post-browser regression are sufficient for **B1.5.5 controlled publication**. Do not start B1.5.6 (or edit/lifecycle/archive/restore UI) until B1.5.5-PUB completes per track process. Retain the Staff-created Pending enrollment for later phases; do not mutate it outside approved scope.

---

## 31. Exact Next Safe Phase

```text
NEXT SAFE PHASE:
B1.5.5-PUB — Enrollments List, Create and Detail Workflows Controlled Publication
```

---

## Appendix — Evidence Sources

| Source | Use |
| --- | --- |
| Human operator role-login checkpoints | Authoritative Owner/Staff/Viewer session establishment |
| Agent-assisted Owner session on `:3014` | List/filters/archived empty/create-page inspection/unavailable probes/logout |
| Agent-assisted Staff session on `:3014` | Archived tamper, validation fail, `ONE QA ENROLLMENT CREATED`, detail/refresh/list, duplicate-open, logout |
| Agent-assisted Viewer session on `:3014` | List/detail read, create denial, archived fail-closed + filters-adjusted warning, logout |
| Read-only SQL enrollment count | Mutation inventory corroboration (1 pending active enrollment) |
| Post-browser vitest + typecheck + lint + `git diff --check` | Regression gate |

## Appendix — R1 Mutation Statement

This R1 phase created **only** this report as a documentation artifact, plus the single permitted Staff UI Enrollment create retained in the QA org. Application source and tests were **not** modified during R1. No Auth/membership/migration/deploy mutation occurred. No commit or push occurred as part of writing this report.
