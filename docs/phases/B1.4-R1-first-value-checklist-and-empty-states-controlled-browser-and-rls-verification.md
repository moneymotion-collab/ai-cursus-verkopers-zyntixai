# B1.4-R1 — Controlled First-Value Checklist and Empty States Browser and RLS Verification

| Field | Value |
| --- | --- |
| Phase | B1.4-R1 — Controlled Authenticated Browser and Live RLS Verification |
| Parent track | B1 — Controlled Beta Entry |
| Status | **BLOCKED** — authenticated browser / live dismiss path not completed |
| Baseline HEAD | `071169fc3e5ad8e3375a07d9b8b77c88f88119e1` |
| Branch | `core/platform-readiness-20260707` |
| Contracts | B1.4 contract + B1.4-QA report |
| Production alias claimed to contain B1.4 | **NO** (implementation remains uncommitted / undeployed) |
| Commit / push / deploy | **NO** |
| Staging | **NO** |

---

## 1. Executive Verdict

```text
BLOCKED
```

Controlled R1 could **not** complete contract-required authenticated browser journeys or an authenticated live dismiss write through the application.

**Blocking cause:** safe QA **credentials are unavailable** to this agent session. The dedicated QA organization and owner membership exist and are in an ideal pre-checklist state, but the local worktree environment only provides public Supabase client keys (no QA password, no service-role key for a controlled magic-link bootstrap). Without an authenticated session, `/leads` cannot render the checklist, and live RLS-backed dismiss cannot be exercised as the product does.

Automated gates remain green. Repository + hosted SQL evidence support readiness of the **data** and **policy** side, but that is insufficient for a PASS / PASS WITH CONDITIONS under the R1 verdict rules.

---

## 2. Verified Git Baseline

| Item | Result |
| --- | --- |
| Worktree | `C:\project ai cursus verkopers.worktrees\laptop-current-20260722` |
| Branch | `core/platform-readiness-20260707` |
| HEAD | `071169fc3e5ad8e3375a07d9b8b77c88f88119e1` |
| Upstream | `origin/core/platform-readiness-20260707` |
| Divergence | `0 0` |
| Staged files | None |
| Starting unique changed paths | **14** (exact match) |
| Ending unique changed paths | **15** (14 + this R1 report) |

B1.4 product remains uncommitted working-tree changes only. Public production deployment was **not** treated as containing B1.4.

---

## 3. Environment Safety

| Check | Result |
| --- | --- |
| Worktree `.env.local` at R1 start | **Missing** |
| Parent main checkout `.env.local` | Present (gitignored) |
| Copied parent `.env.local` into worktree for local app boot | **YES** (gitignored; not part of the 14/15 git path inventory) |
| Keys present after copy (names only) | `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY` |
| Service role / QA password keys present | **NO** |
| Hosted Supabase project | `dmctinrcjvsgmoxwwodw` (ACTIVE_HEALTHY) |
| Local Docker / local Supabase | **Unavailable** (Docker not installed / not running) |
| Supabase MCP | Authenticated during R1; used for **read-only** SQL |
| Desktop workspace | Inactive; not used as an editor |

Secrets, tokens, and env **values** are omitted from this report.

---

## 4. Dedicated QA Tenant — Initial State (read-only SQL)

Organization (clearly QA-labelled; IDs already documented historically in ACCESS):

| Field | Value |
| --- | --- |
| Name / slug | `ZyntixAI Production QA` / `zyntixai-production-qa` |
| Organization id | `2fc07699-ece5-44b9-bbb3-abbc23e9fffb` |
| Onboarding complete | **YES** |
| Checklist dismissed | **NO** (`first_run_checklist_dismissed_at` null) |
| Active non-archived leads | **0** |
| Archived leads | **0** |
| Active non-archived tasks | **0** |
| Archived tasks | **0** |
| Active owner memberships | **1** |
| Admin / staff / viewer memberships on this org | **0** (owner only) |

Aggregate project counts at R1 read (no intentional mutation performed):

| Metric | Count |
| --- | ---: |
| Auth users | 21 |
| Organizations | 5 |
| Memberships | 12 |
| Profiles | 21 |
| Leads | 57 |
| Customers | 109 |
| Tasks | 1 |

This QA org state is **ideal** for Journey A once authentication is possible.

---

## 5. Local Application Identity

| Check | Result |
| --- | --- |
| App started from this worktree | **YES** — `next build` + `next start -p 3014` |
| Port | `http://127.0.0.1:3014` |
| `/login` | **200**, email/password form present |
| `/leads` unauthenticated | **307** → `/login?next=%2Fleads` |
| B1.4 strings in local build output | **YES** — `Get your first results`, `Dismiss checklist`, empty-state copy present under `.next/.../leads/page*.js` |
| Production alias used for B1.4 UI proof | **NO** (forbidden / incorrect for undeployed B1.4) |

Local server was stopped after evidence collection.

---

## 6. Browser Journey Results

| Journey | Environment | Result |
| --- | --- | --- |
| A Owner checklist / empty CTA | Local `:3014` + hosted Auth | **NOT EXECUTED** — no QA credentials |
| B Filtered / archived empty CTA | Local | **NOT EXECUTED** — requires auth + records |
| C First-lead completion | Local + QA org only | **NOT EXECUTED** |
| D First-task completion | Local + QA org only | **NOT EXECUTED** |
| E Fully completed hide rule | Local | **NOT EXECUTED** |
| F Live dismiss + RLS via app | Local + hosted DB | **NOT EXECUTED** — no authenticated session |
| G Admin / staff / viewer live roles | Hosted QA org | **NOT LIVE-VERIFIED** — QA org has **owner only**; also no credentials |
| H Tenant isolation (mutating proofs) | Hosted | **NOT EXECUTED** — no auth session for product path |
| I Auth / onboarding / Step 2 browser regression | Local | **NOT EXECUTED** for authenticated paths |

Unauthenticated smoke only:

- Login page loads on local B1.4 build.
- Protected `/leads` redirects to login.

---

## 7. Live RLS / SQL Evidence (not a substitute for Journey F)

| Evidence class | Finding |
| --- | --- |
| Policy `organizations_update_admin` | Present live: `USING` / `WITH CHECK` = `private.has_org_role(id, ARRAY['owner','admin'])` |
| QA dismiss column currently null | Confirmed |
| App dismiss path exercised under user JWT | **NO** |
| Service-role SQL used to force-dismiss for “proof” | **NO** (would bypass RLS; invalid as R1 dismiss evidence) |

**Explicit:** hosted policy text + unit mocks ≠ authenticated product dismiss under RLS.

---

## 8. Automated Regression Gates (re-run during R1)

| Gate | Result |
| --- | --- |
| Focused B1.4 + related onboarding/leads | **55 / 55 PASS** |
| Typecheck | **PASS** |
| Lint | **PASS** |
| Full suite | **145 files / 982 tests PASS** |
| Production build | **PASS** (`ƒ /onboarding`, `ƒ /leads`) |

These remain **automated-test evidence** only.

---

## 9. Defects and Remediation

| Item | Action |
| --- | --- |
| Product B1.4 defect found in R1 | **None identified** (authenticated UI not reached) |
| Product code changed in R1 | **NO** |
| Extra test files added in R1 | **NO** |

---

## 10. Data Mutations

| Mutation | Performed? |
| --- | --- |
| QA lead/task create | **NO** |
| QA checklist dismiss | **NO** |
| Non-QA org changes | **NO** |
| Schema / RPC / membership changes | **NO** |
| Final QA org state vs initial | **Unchanged** (read-only SQL) |

Temporary operational artifacts:

- Gitignored `.env.local` copy into this worktree (for attempted local boot).
- Local `next start` on port 3014 (stopped).
- Playwright Chromium download via npx (tooling only).

---

## 11. Remaining Assumptions / Unverified Claims

1. QA owner password / recovery mailbox access exists with a human operator but was **not** provided to this session.
2. Admin/staff/viewer live role journeys require additional controlled memberships (currently absent on the QA org) **or** remain automated-only.
3. Public production deployment content was not audited for B1.4 strings; by Git evidence B1.4 is undeployed.

---

## 12. Publication / Git Hygiene Confirmation

| Action | Performed? |
| --- | --- |
| Stage | **NO** |
| Commit | **NO** |
| Push | **NO** |
| Merge / rebase | **NO** |
| Deploy / Vercel alias change | **NO** |
| Claim production contains B1.4 | **NO** |

---

## 13. Changed-Path Inventory (ending)

Same 14 implementation/QA/contract paths as B1.4-QA end state, plus:

- `docs/phases/B1.4-R1-first-value-checklist-and-empty-states-controlled-browser-and-rls-verification.md` (**this report**)

**Total unique git-visible changed paths: 15**

---

## 14. What Is Required to Unblock R1

Provide **one** of the following under controlled QA rules (no customer tenants):

1. QA owner credentials (or an approved recovery flow) usable only against `ZyntixAI Production QA`, **or**
2. An approved temporary Auth bootstrap method that does not require storing secrets in git,

then re-run Journeys A–F (and H) on `http://127.0.0.1:<port>` from this worktree, with SQL before/after dismiss proving only `first_run_checklist_dismissed_at` changes on the QA org.

Role journeys G remain **optional for PASS WITH CONDITIONS** if owner+RLS pass but admin/staff/viewer identities stay unavailable.

---

## 15. Final Verdict

```text
BLOCKED
```

Core authenticated browser behavior, live application dismiss under RLS, and tenant-isolation product proofs were **not** completed because safe QA authentication material was unavailable—despite favorable QA org state, live update policy presence, local B1.4 build identity, and green automated gates.

---

## 16. Exact Next Step

```text
NEXT SAFE PHASE:
Re-attempt B1.4-R1 after QA authentication material is available
(still on laptop worktree; still no production alias B1.4 claim)
```

Do not begin B1.4-PUB while R1 is BLOCKED.
