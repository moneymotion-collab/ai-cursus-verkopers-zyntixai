# B1.5.6-R1-FIXTURE — Controlled QA Eligibility Fixture Preparation

| Field | Value |
| --- | --- |
| Phase | B1.5.6-R1-FIXTURE — Controlled QA Eligibility Fixture Preparation |
| Parent track | B1.5 — Programs & Enrollments Workspace |
| Status | **PASS** — supporting Customer fixture prepared; authenticated R1 reverification still required |
| Baseline commit | `e318f3c7b54b340c6a2216a323c47cd4549a4f40` (`docs(enrollments): close B1.5.5 publication`) |
| Branch | `core/platform-readiness-20260707` |
| Blocked R1 report | `docs/phases/B1.5.6-R1-enrollment-lifecycle-ownership-metadata-archive-and-restore-authenticated-browser-and-live-authorization-verification.md` |
| Implementation report | `docs/phases/B1.5.6-enrollment-lifecycle-ownership-metadata-archive-and-restore.md` |
| Independent QA report | `docs/phases/B1.5.6-QA-enrollment-lifecycle-ownership-metadata-archive-and-restore-independent-qa.md` |
| Repository mutation | Exactly one new fixture-preparation report (this file). No source/test/fixture or prior-report edits |
| Application-data mutation | Path A only: exactly one Customer create via normal Owner UI |

---

## 1. Executive Summary

B1.5.6-R1 was previously blocked because the dedicated QA organization had only one eligible Customer and one active Program, and that pair already held the retained B1.5.5 open pending Enrollment. This fixture-preparation phase used a fresh production-mode local runtime (`http://127.0.0.1:3014`), confirmed **Owner** in **ZyntixAI Production QA**, and created exactly one new eligible Customer through the normal authenticated Customer-create UI.

The new Customer defaults to non-archived status `onboarding`, appears in `/enrollments/new` alongside the existing active Program, and has no Enrollment. That yields a non-duplicate eligible Customer/Program pair for dedicated B1.5.6 lifecycle verification. No Enrollment, Program, Auth, membership, or existing-record mutation occurred. Authenticated browser reverification remains required.

---

## 2. Final Preparation Verdict

```text
PASS — READY FOR B1.5.6-R1-R2
```

Operational verdict:

```text
B1.5.6-R1-FIXTURE PASS — READY FOR AUTHENTICATED REVERIFICATION
```

---

## 3. Git Baseline

| Item | Value |
| --- | --- |
| Worktree / repository root | `D:\project ai cursus verkopers.worktrees\parallel__laptop-product-track-20260707-1` |
| Branch | `core/platform-readiness-20260707` |
| HEAD / upstream | `e318f3c7b54b340c6a2216a323c47cd4549a4f40` |
| Subject | `docs(enrollments): close B1.5.5 publication` |
| Divergence | `0 0` |
| Staged | `0` |
| Deletes / renames / conflicts | `0` |
| Commit / push during this phase | **None** |
| Source / test / prior-report edits during this phase | **None** |

Preflight dirty inventory remained exclusively B1.5.6-scoped (implementation + QA + blocked R1 docs/routes/UI/tests). This phase adds only this report.

`git diff --check` exit `0`.

---

## 4. Initial QA Data Baseline

Organization UI label: **ZyntixAI Production QA**. Times shown in UTC.

| Domain | Baseline before fixture mutation |
| --- | --- |
| Customers | Showing **1 of 1** — display label `zynrtix`, status **Onboarding**, not archived, Assigned to **Unassigned** |
| Eligible non-archived Customers (`onboarding` / `active`) | **1** |
| Programs | Showing **3 of 3** — `B1.5.2 QA Test Program` **Active** (open enrollments **1**); `test` **Draft** (0); `guzzu3` **Draft** (0) |
| Non-archived active Programs | **1** |
| Enrollments | Showing **1 of 1** — customer `zynrtix`, program `B1.5.2 QA Test Program`, status **Pending**, owner **Unassigned**, enrolled Jul 25, 2026, 10:13 PM |
| Non-archived open Enrollments | **1** |

`/enrollments/new` before mutation:

- Customer options: exactly **`zynrtix`**
- Program options: exactly **`B1.5.2 QA Test Program`**
- That pair already holds the retained open pending Enrollment
- Form was **not** submitted

---

## 5. Blocked R1 Reason

Historical operational verdict: `B1.5.6-R1 CONTROLLED FIXTURE BLOCKED`.

Blocking facts retained as the reason for this preparation phase:

- exactly one eligible Customer;
- exactly one active Program;
- that Customer/Program pair already had the retained B1.5.5 open pending Enrollment;
- other Programs were Draft and therefore ineligible for Enrollment create;
- reuse or mutation of the B1.5.5 pending Enrollment was prohibited.

---

## 6. Selected Fixture Path

```text
PATH A — CUSTOMER FIXTURE
```

Path B was **not** executed.

---

## 7. Fixture-Selection Rationale

Published Customer create requires only display name (optional first/last/email/phone/owner). Database / `create_customer` default status is `onboarding`, which is Enrollment-eligible. Create does not archive the record and needs no follow-up lifecycle mutation.

Therefore Path A is safer than Path B: one Customer create yields a second eligible Customer for the existing active Program without Program create/activation and without touching Draft Programs.

---

## 8. Owner Role Verification

Human operator completed Owner login (`QA OWNER LOGIN COMPLETE`).

Independent role evidence (not Staff):

| Signal | Observation |
| --- | --- |
| `/programs` | **New program** link present |
| `/programs` filters | **Show archived programs** present |
| `/customers` filters | **Show archived customers** present |
| `/enrollments` filters | **Show archived enrollments** present |
| Organization label | **ZyntixAI Production QA** |
| Owner option label (masked) | UI shows **ZyntixAI QA Owner** among assignable members |

Resolved role: **owner**. Active membership in the dedicated QA organization confirmed via authenticated workspace access.

An earlier Staff session was rejected as `OWNER ROLE MISMATCH` before any fixture mutation.

---

## 9. Exact Supporting Record Created

| Item | Value |
| --- | --- |
| Path | Path A — Customer create only |
| UI workflow | `/customers/new` → submit **Create customer** |
| Display name | `B1.5.6 Lifecycle QA Customer` |
| Fields set | Display name only (required) |
| Optional fields | Left empty; Assigned to left **Unassigned** |
| Programs created | **0** |
| Enrollments created | **0** |

No Lead conversion, no archive, no post-create Customer edit/status change.

---

## 10. Final Supporting-Record State

| Attribute | Value |
| --- | --- |
| Display name | `B1.5.6 Lifecycle QA Customer` |
| Status | **Onboarding** (`onboarding`) |
| Archive | **Not archived** (Archive customer action available; list shows under default non-archived view) |
| Assigned to | **Unassigned** |
| Organization | **ZyntixAI Production QA** (same workspace) |
| Enrollment summary on detail | **No enrollments are linked to this customer** |
| Customers list after create | Showing **2 of 2** (new Customer + unchanged `zynrtix`) |

---

## 11. Enrollment Create-Option Verification

`/enrollments/new` inspected **without submission** after Path A:

| Control | Observation |
| --- | --- |
| Customer options | **2**: `B1.5.6 Lifecycle QA Customer`, `zynrtix` |
| Program options | **1**: `B1.5.2 QA Test Program` (active) |
| Initial status | **Pending**, **Active** only |
| Owner (optional) | **Unassigned** default; Owner/member options present |
| Source | Server-controlled (no source field on form) |
| Metadata field | **Absent** (copy still mentions metadata in help text only) |

---

## 12. Non-Duplicate Pair Verification

Prepared eligible pair:

| Side | Label | Notes |
| --- | --- | --- |
| Customer | `B1.5.6 Lifecycle QA Customer` | Non-archived, `onboarding`, no linked Enrollments |
| Program | `B1.5.2 QA Test Program` | Non-archived, `active` |
| Open Enrollment for this pair | **None** | New Customer enrollment summary empty; Enrollments list still shows only the B1.5.5 row |

Distinct from retained B1.5.5 pair (`zynrtix` + `B1.5.2 QA Test Program` with open pending Enrollment).

Readiness condition met: at least one eligible Customer/active Program combination without a non-archived open Enrollment (`pending` / `active` / `paused`).

---

## 13. Existing B1.5.5 Enrollment Unchanged Evidence

After Path A, Enrollments list still **Showing 1 of 1**.

Detail of the retained Enrollment:

| Attribute | Value |
| --- | --- |
| Heading | `zynrtix · B1.5.2 QA Test Program` |
| Status | **Pending** |
| Archive status | **Not archived** |
| Owner | **Unassigned** |
| Source | **Manual** |
| Enrolled | Jul 25, 2026, 10:13 PM |
| Created / Updated | Jul 25, 2026, 10:13 PM (unchanged) |

No second Enrollment appeared. No archive/restore/owner/status mutation was performed on this record.

---

## 14. Exact Application-Data Mutation Inventory

Path A inventory:

| Mutation class | Count |
| --- | --- |
| Customers created | **1** (`B1.5.6 Lifecycle QA Customer`) |
| Programs created | **0** |
| Program transitions | **0** |
| Enrollments created | **0** |
| Enrollment lifecycle / owner / archive / restore | **0** |
| Existing Customer mutations | **0** |
| Existing Program mutations | **0** |
| Lead / Task mutations | **0** |
| Auth / membership / role / organization mutations | **0** |

---

## 15. Repository Integrity

| Check | Result |
| --- | --- |
| HEAD remains `e318f3c7b54b340c6a2216a323c47cd4549a4f40` | **Yes** |
| Upstream equal / divergence `0 0` | **Yes** |
| Staged `0` | **Yes** |
| Source / test / fixture files changed by this phase | **No** |
| Previous B1.5.6 reports unchanged by this phase | **Yes** |
| New report | Exactly this file |
| Dirty inventory remains B1.5.6-scoped | **Yes** (prior implementation/QA/R1 dirty set + this report) |
| `git diff --check` | Exit **0** |

No commit or push.

---

## 16. Credential-Handling Confirmation

- Operator performed login manually.
- No passwords, cookies, tokens, emails, user IDs, or organization IDs are recorded in this report.
- Masked UI labels only (`ZyntixAI QA Owner`, organization display name, Customer/Program display names).

---

## 17. Manual-Testing Classification

```text
CONTROLLED QA FIXTURE PREPARATION COMPLETE — BROWSER REVERIFICATION STILL REQUIRED
```

This phase prepared eligibility data only. It did **not** execute Enrollment lifecycle, ownership, archive, restore, or live authorization proof for B1.5.6.

---

## 18. Exact Next Safe Phase

Re-run authenticated browser and live authorization verification against the prepared non-duplicate pair:

```text
B1.5.6-R1-R2 — Enrollment Lifecycle, Ownership, Metadata, Archive and Restore Authenticated Browser and Live Authorization Reverification
```

Use the new Customer `B1.5.6 Lifecycle QA Customer` with active Program `B1.5.2 QA Test Program` for the dedicated lifecycle fixture Enrollment. Do **not** mutate the retained B1.5.5 pending Enrollment.
