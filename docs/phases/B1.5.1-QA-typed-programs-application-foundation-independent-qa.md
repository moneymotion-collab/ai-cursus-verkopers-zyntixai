# B1.5.1-QA — Typed Programs Application Foundation Independent QA

| Field | Value |
| --- | --- |
| Phase | B1.5.1-QA — Independent QA, Security and Regression Verification |
| Parent | B1.5.1 — Typed Programs Application Foundation |
| Status | Independent QA complete |
| Baseline HEAD | `26e4574e7af59526a51d76a6bd95e95dae48b815` |
| Branch | `core/platform-readiness-20260707` |
| Authoritative contract | `docs/phases/B1.5-programs-and-enrollments-workspace-design-security-and-readiness-contract.md` |
| Implementation report reviewed | `docs/phases/B1.5.1-typed-programs-application-foundation.md` |
| QA mutation | This report only |

---

## 1. Executive Summary

Independent QA re-inspected the B1.5.1 Programs foundation against the B1.5 contract, migrations/generated RPC types, and repository security patterns. Findings: domain/RPC/permission/validation/route foundations match the database contract; mutations use approved RPCs only; no direct Program table writes; no service-role usage; no dead visible Programs navigation; Enrollments/Progress UI absent; full automated regression green. Visible Programs nav activation remains correctly deferred to B1.5.2. Loader/action modules lack dedicated automated tests (covered indirectly via adapters, permissions, validation, mapping) — recorded as a non-blocking publication condition.

---

## 2. Final QA Verdict

```text
PASS WITH CONDITIONS — READY FOR B1.5.1 PUBLICATION
```

---

## 3. Git Baseline

| Item | Value |
| --- | --- |
| Worktree | `D:/project ai cursus verkopers.worktrees/parallel__laptop-product-track-20260707-1` |
| Branch | `core/platform-readiness-20260707` |
| Local HEAD | `26e4574e7af59526a51d76a6bd95e95dae48b815` |
| Subject | `docs(programs): add B1.5 workspace readiness contract` |
| Upstream | same SHA |
| Divergence | `0 0` |
| Staged / deleted / renamed / conflicts | all `0` |
| Same-branch worktrees | only this worktree |

---

## 4. Dirty-Path Baseline (pre-QA)

### Modified (2)

| Path | Classification |
| --- | --- |
| `src/features/auth/server/safe-return-path.ts` | route/security integration |
| `tests/auth/safe-return-path.test.ts` | safe-return-path test |

### Untracked (26 files)

| Path | Classification |
| --- | --- |
| `docs/phases/B1.5.1-typed-programs-application-foundation.md` | B1.5.1 implementation report |
| `src/features/programs/domain/types.ts` | Programs domain |
| `src/features/programs/domain/status.ts` | Programs metadata |
| `src/features/programs/domain/delivery-mode.ts` | Programs metadata |
| `src/features/programs/domain/permissions.ts` | Programs permissions |
| `src/features/programs/domain/read-types.ts` | Programs domain |
| `src/features/programs/domain/programs-navigation.ts` | navigation groundwork |
| `src/features/programs/validation/*` | Programs validation |
| `src/features/programs/server/program-rpc-adapters.ts` | Programs server adapter |
| `src/features/programs/server/normalize-program-error.ts` | Programs error mapping |
| `src/features/programs/server/program-read-queries.ts` | Programs loader |
| `src/features/programs/server/load-program-foundations.ts` | Programs loader |
| `src/features/programs/server/program-mutations.ts` | Programs action (mutation layer) |
| `src/features/programs/server/map-program-read-model.ts` | Programs domain mapping |
| `src/features/programs/server/program-query-columns.ts` | Programs loader |
| `src/features/programs/server/resolve-program-page-organization.ts` | membership / org resolution |
| `src/features/programs/actions/*` | Programs action |
| `tests/domain/program-*.test.ts` (3) | Programs test |
| `tests/server/*program*` / map / normalize (3) | Programs test |
| `tests/validation/program-schemas.test.ts` | Programs test |

**Unrelated / uncertain dirty paths:** none. Precheck passed.

---

## 5. Authoritative Contract

B1.5 contract present and reviewed. Verdict remains `PASS WITH CONDITIONS — READY FOR B1.5.1`. Next phase named: B1.5.1 (completed under review).

---

## 6. Implementation Report Reviewed

`docs/phases/B1.5.1-typed-programs-application-foundation.md` claims `PASS WITH CONDITIONS — READY FOR B1.5.2`. Independently verified; not trusted alone.

---

## 7. Requirement Traceability Matrix

| # | Requirement | Classification |
| --- | --- | --- |
| 1 | Exact Program statuses | Implemented and verified |
| 2 | Exact delivery modes | Implemented and verified |
| 3 | Program domain types | Implemented and verified |
| 4 | Database/domain mapping | Implemented and verified |
| 5 | Role permissions | Implemented and verified |
| 6 | Owner/admin mutation rights | Implemented and verified |
| 7 | Staff/viewer read-only | Implemented and verified |
| 8 | Archived-record visibility | Implemented and verified |
| 9 | Validation schemas | Implemented and verified |
| 10 | RPC reuse | Implemented and verified |
| 11 | Membership resolution | Implemented and verified |
| 12 | Organization scoping | Implemented and verified |
| 13 | Program error mapping | Implemented and verified |
| 14 | List loader | Implemented but insufficiently tested |
| 15 | Detail loader | Implemented but insufficiently tested |
| 16 | History loader | Implemented but insufficiently tested |
| 17 | Create action | Implemented but insufficiently tested |
| 18 | Update action | Implemented but insufficiently tested |
| 19 | Transition action | Implemented but insufficiently tested |
| 20 | Archive action | Implemented but insufficiently tested |
| 21 | Restore action | Implemented but insufficiently tested |
| 22 | Protected Program paths | Implemented and verified |
| 23 | Safe return paths | Implemented and verified |
| 24 | Navigation groundwork | Implemented and verified (not visible) |
| 25 | Programs UI exclusion | Intentionally deferred |
| 26 | Enrollments exclusion | Intentionally deferred |
| 27 | Progress exclusion | Intentionally deferred |
| 28 | Automated test coverage | Implemented and verified (foundation layers); loader/action dedicated suites deferred |

No missing required foundation items. No contract deviations that undermine security.

---

## 8. Database-Contract Verification

Compared implementation to migrations `20260705180000`–`07` and `database.generated.ts`:

| Contract item | Result |
| --- | --- |
| Statuses `draft/active/paused/retired` | Match |
| Delivery modes (6 values) | Match |
| RPCs create/update/transition/archive/restore | Exact names used |
| RPC args `p_organization_id`, `p_program_id`, etc. | Match |
| Create returns uuid; others void | Match |
| Owner/admin mutations | Match permission helpers + RPC |
| Archive blocked by open enrollments | Error mapped |
| Restore clears archive only | Adapter present |
| SELECT via RLS (no insert/update/delete on programs) | Verified — only `.select` on programs |
| Open enrollment definition for counts | `pending\|active\|paused` + `archived_at is null` |

**No material mismatch.** No invented fields / unsupported statuses / unapproved migrations.

---

## 9. Domain and Metadata Findings

- Row types vs read models separated.
- Status/delivery constants centralized; labels/order deterministic.
- Archive orthogonal to lifecycle; no invented terminal lifecycle status.
- No `any`; no broad unsafe assertions found.
- Invalid status/mode rejected by guards/schemas.
- Soft mapping fallback (`draft` / `self_paced`) for corrupt strings is fail-soft only; CHECK constraints make this rare — non-blocking observation.

---

## 10. Permission Findings

Verified matrix:

| Capability | owner | admin | staff | viewer |
| --- | --- | --- | --- | --- |
| List/view non-archived | allow | allow | allow | allow |
| View archived | allow | allow | deny | deny |
| Create/update/transition/archive | allow | allow | deny | deny |
| Restore | allow (when archived) | allow (when archived) | deny | deny |
| History | allow | allow | non-archived only | non-archived only |

Unknown/absent role fail closed. Role from membership context; browser cannot set role. Mutations check capabilities before RPC. **No privilege escalation found.**

---

## 11. Validation Findings

Create/update/transition/archive/restore schemas match RPC fields; enums exact; trim + non-empty name; pagination bounded; sort allowlisted; `.strict()` rejects unsupported keys including `expectedEndDate`; org id is UUID input only — authority via `resolveOrganizationContext`.

---

## 12. Adapter and RPC Findings

| Check | Result |
| --- | --- |
| Server-only adapters | Yes |
| Authenticated client | `createSupabaseServerClient` in actions; adapters receive client |
| Service-role / admin client | **Not found** |
| Exact RPC names | Verified |
| Exact parameter names | Verified |
| Direct programs insert/update/delete | **None** |
| Browser org authority alone | Blocked by membership resolution |
| Enrollment queries | SELECT-only for open counts |

Matches of `.from("programs")`: SELECT list/detail only — explained and acceptable under RLS.

---

## 13. Loader Findings

List/detail/history loaders resolve membership, apply archived visibility by role, org-scope filters, return typed results. No cross-tenant caching. Dedicated automated loader tests absent — see conditions.

---

## 14. Action Findings

Actions: `"use server"` → validate → membership → role → mutation → RPC → revalidate `/programs` (+ detail). Revalidating routes that do not yet exist is safe in Next.js (cache tag/path no-op until pages exist). No unsafe redirects. Dedicated action tests absent — condition.

---

## 15. Error-Mapping Findings

Known RPC messages map to stable codes (`ARCHIVE_BLOCKED_OPEN_ENROLLMENTS`, `DUPLICATE_PROGRAM`, `TRANSITION_NOT_ALLOWED`, etc.). Unknown errors → safe user message without SQL/policy leakage. Tests cover primary mappings.

---

## 16. Tenant-Isolation Findings

Org context from `resolveOrganizationContext` / page org resolver. Queries filter `organization_id`. RPCs receive membership-resolved org id. Fail closed without membership. **Pass.**

---

## 17. Route-Protection Findings

`/programs` and `/programs/*` protected. Prefix does not match `/programmes`. Leads/Customers/Tasks/onboarding unchanged. No Program pages created.

---

## 18. Safe-Return-Path Findings

Programs paths allowlisted. External/protocol-relative rejected. Tests extended. **Pass.**

---

## 19. Navigation Findings

| Check | Result |
| --- | --- |
| Canonical `/programs` + label `Programs` | Present |
| `PROGRAMS_NAV_VISIBLE` | `false` |
| App shell Programs link | **Absent** |
| Dead clickable nav | **None** |
| Enrollments nav | **Not added** |
| Unrelated nav order | Unchanged |

---

## 20. Scope-Boundary Findings

| Search | Result |
| --- | --- |
| `src/app/**/programs/**` | None |
| Programs forms/pages | None |
| Enrollments UI/routes | None |
| Progress / Attention / NBA / AI / health / churn / Dashboard | None (except delivery-mode copy “progress independently”) |
| Migrations / env / vercel | Unchanged |
| Broad app-shell refactor | None |

Enrollment `.from("enrollments").select` used only for open-count foundation — not Enrollments workspace.

---

## 21. Automated-Test Review

Strong: status/delivery metadata, permissions matrix, schemas (incl. hostile keys), RPC adapter arg names + membership fail-closed + archive-blocked mapping, error normalize, read-model mapping, nav visibility flag, protected/safe-return paths, CRM regression via full suite.

Gap (non-blocking): no dedicated tests for `program-actions.ts`, `program-mutations.ts`, `program-read-queries.ts`, `load-program-foundations.ts`. Behavior is partially covered by lower-layer tests. Recommend B1.5.2 add focused suites.

---

## 22. Validation Command Results

| Command | Exit | Outcome |
| --- | ---: | --- |
| Targeted Program + safe-return vitest (8 files) | 0 | **50** tests PASS |
| `npm run test:run` | 0 | **152** files / **1016** tests PASS |
| `npm run typecheck` | 0 | PASS |
| `npm run lint` | 0 | PASS |
| `npm run build` | 0 | PASS |
| `git diff --check` | 0 | PASS |

Totals match known baseline; no unexplained drift.

---

## 23. Diff-Integrity Review

All dirty paths B1.5.1-scoped. No migration/env/config churn. No unrelated formatting-only noise beyond necessary safe-return-path edits.

| Count | Value |
| --- | ---: |
| Tracked modified | 2 |
| Untracked files | 26 (+ this QA report after creation) |
| Deleted / renamed / staged | 0 |
| Source paths | programs feature + safe-return-path |
| Test paths | 8 files (7 new + 1 modified) |
| Documentation paths | impl report + this QA report |

---

## 24. Secret-Safety Review

No `.env`, tokens, cookies, service keys, or credential-like strings in dirty Program docs/code. UUIDs in tests are synthetic fixtures. No debug logs of auth users. **Pass.**

---

## 25. Defect Inventory

| ID | Severity | Finding |
| --- | --- | --- |
| — | — | **No security or publication-blocking defects** |

Observations (non-defects):

- Soft fallback mapping for unknown status/delivery strings.
- `revalidatePath('/programs')` before pages exist (harmless).

---

## 26. Condition Inventory

| # | Condition | Class |
| --- | --- | --- |
| C1 | Visible Programs navigation remains disabled until B1.5.2 pages exist | Non-blocking (known; preserved) |
| C2 | Add dedicated automated tests for loaders/mutations/actions in B1.5.2 (or follow-up) | Non-blocking follow-up |
| C3 | B1.5 contract Progress/Enrollments exclusions remain in force | Non-blocking scope carry-forward |

---

## 27. Publication-Readiness Conclusion

B1.5.1 is ready for controlled documentation/source publication of the foundation (implementation + tests + reports) under **PASS WITH CONDITIONS**. Do not activate visible Programs nav or claim Programs UI completeness. Do not begin Enrollments/Progress. Safest product next step after publication remains **B1.5.2 — Programs List, Create and Detail Workflows**.

---

## 28. Exact Next Safe Phase

```text
NEXT SAFE PHASE:
B1.5.1-PUB — Typed Programs Application Foundation Controlled Publication
```

(If process prefers skipping a named PUB and going straight to B1.5.2 after an equivalent publication commit, that is an owner process choice; technically the foundation is publication-ready.)

Alternative product phase after publication:

```text
B1.5.2 — Programs List, Create and Detail Workflows
```

---

## Appendix — QA mutation statement

This QA phase created **only** this report. Implementation source, tests, B1.5 contract, and B1.5.1 implementation report were **not** modified. No commit, push, migration, database, Auth, deployment, or production mutation occurred.
