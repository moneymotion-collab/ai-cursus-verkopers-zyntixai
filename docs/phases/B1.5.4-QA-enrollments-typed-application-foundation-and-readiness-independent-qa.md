# B1.5.4-QA — Enrollments Typed Application Foundation and Readiness Independent QA

| Field | Value |
| --- | --- |
| Phase | B1.5.4-QA — Independent QA, Security and Regression Verification |
| Parent track | B1.5 — Programs & Enrollments Workspace |
| Implementation under review | `docs/phases/B1.5.4-enrollments-typed-application-foundation-and-readiness.md` |
| Authoritative contract | `docs/phases/B1.5-programs-and-enrollments-workspace-design-security-and-readiness-contract.md` |
| Branch | `core/platform-readiness-20260707` |
| Baseline HEAD | `8914391a1e238715b6947326783135eea37ec522` |
| QA mutation | Exactly one new QA report (this file). No source/test/fixture changes. |

---

## 1. Executive Summary

Independent code, security and regression QA of the B1.5.4 Enrollments typed foundation confirms that application modules align with authoritative PE migrations for schema, lifecycle matrix, RPCs, grants/RLS intent, role permissions, limited owner/metadata update boundaries, tenant scoping, hidden navigation, and absence of user-facing `/enrollments` routes.

**However, `npm run typecheck` fails** on three TypeScript errors inside `tests/server/enrollment-rpc-adapters.test.ts`. Per QA rules, defects were not fixed during this phase. Vitest, lint, build, and `git diff --check` pass; the project typecheck gate does not.

Live RLS was **not** claimed from mocked tests.

---

## 2. Final QA Verdict

```text
VALIDATION FAILED
```

Blocking validation defect:

1. `npm run typecheck` exit `2` — three errors in `tests/server/enrollment-rpc-adapters.test.ts` (missing required `reason` on typed transition inputs; invalid status string assigned to typed `toStatus`).

No blocking contract/database mismatch or application-layer security defect was found in the Enrollments foundation source under review. Publication is **not** authorized until typecheck is green without weakening gates.

---

## 3. Git Baseline

| Item | Value |
| --- | --- |
| Working directory | `D:\project ai cursus verkopers.worktrees\parallel__laptop-product-track-20260707-1` |
| Repository root | same |
| Branch | `core/platform-readiness-20260707` |
| HEAD SHA | `8914391a1e238715b6947326783135eea37ec522` |
| HEAD subject | `docs(programs): close B1.5.3 publication` |
| Upstream SHA | `8914391a1e238715b6947326783135eea37ec522` |
| Upstream subject | same |
| Divergence | `0 0` |
| Staged count | `0` |
| Deleted / renamed / conflicts | `0` / `0` / `0` |
| Commit / push during QA | **None** |

---

## 4. Pre-QA Dirty Inventory

| Path | Classification |
| --- | --- |
| `src/features/enrollments/domain/types.ts` | Enrollments domain |
| `src/features/enrollments/domain/status.ts` | Enrollments constants or presentation |
| `src/features/enrollments/domain/permissions.ts` | Enrollments permissions |
| `src/features/enrollments/domain/read-types.ts` | Enrollments domain |
| `src/features/enrollments/domain/enrollments-navigation.ts` | Hidden navigation registration |
| `src/features/enrollments/validation/mutation-schemas.ts` | Enrollments validation |
| `src/features/enrollments/validation/read-query-schemas.ts` | Enrollments validation |
| `src/features/enrollments/server/normalize-enrollment-error.ts` | Mutation/error interpretation |
| `src/features/enrollments/server/enrollment-query-columns.ts` | Enrollments read foundation |
| `src/features/enrollments/server/map-enrollment-read-model.ts` | Enrollments read foundation |
| `src/features/enrollments/server/enrollment-read-queries.ts` | Enrollments read foundation |
| `src/features/enrollments/server/enrollment-rpc-adapters.ts` | Enrollments mutation foundation + limited owner/metadata update foundation |
| `src/features/enrollments/server/enrollment-mutations.ts` | Enrollments mutation foundation |
| `src/features/enrollments/server/load-enrollment-foundations.ts` | Enrollments action or loader |
| `src/features/enrollments/server/resolve-enrollment-page-organization.ts` | Authenticated organization resolver |
| `src/features/enrollments/actions/enrollment-action-schemas.ts` | Enrollments action or loader |
| `src/features/enrollments/actions/enrollment-actions.ts` | Enrollments action or loader |
| `src/features/auth/server/safe-return-path.ts` | Route-protection enhancement |
| `src/components/app-shell.tsx` | Hidden navigation registration |
| `tests/helpers/enrollment-test-fixtures.ts` | Enrollments fixture |
| `tests/domain/enrollment-*.test.ts` (3) | Enrollments test |
| `tests/validation/enrollment-schemas.test.ts` | Enrollments test |
| `tests/server/enrollment-*.test.ts` / normalize / map / load (6) | Enrollments test |
| `tests/auth/safe-return-path.test.ts` | Route-protection enhancement |
| `docs/phases/B1.5.4-enrollments-typed-application-foundation-and-readiness.md` | B1.5.4 implementation report |

No unrelated, uncertain, or sensitive dirty paths. Implementation report present; target QA report absent at preflight. Precheck **passed**.

---

## 5. Authoritative Schema Reconciliation

Independent inspection of migrations `20260705180002`–`07` and `database.generated.ts` agrees with the implementation report and typed foundation:

| Claim | Evidence |
| --- | --- |
| Tables `public.enrollments`, `public.enrollment_status_history` | migrations + generated types |
| Status CHECK five values | `180002` |
| Soft-archive `archived_at`; terminal-only archive CHECK | `180002` |
| Open unique index `(org, customer, program)` for open non-archived | `180002` |
| Org-scoped FKs; customer/program `ON DELETE RESTRICT` | `180002` |
| No `update_enrollment` RPC | generated types grep; RPC inventory only create/transition/archive/restore |
| Column UPDATE grant `(owner_member_id, metadata)` only | `180005` / `180006` |
| INSERT/DELETE revoked for authenticated | `180005` / `180006` |

**No contract/database mismatch.**

---

## 6. Enrollment Row Model

Authoritative columns match foundation `EnrollmentRow` / read models:

`id`, `organization_id`, `customer_id`, `program_id`, `status`, `owner_member_id`, `created_by_member_id`, `enrolled_at`, `started_at`, `completed_at`, `cancelled_at`, `source`, `metadata`, `created_at`, `updated_at`, `archived_at`.

History: `id`, `organization_id`, `enrollment_id`, `from_status`, `to_status`, `changed_by_member_id`, `reason`, `source`, `changed_at`, `created_at`.

---

## 7. Lifecycle-Status and Transition Verification

Domain `ALLOWED_TRANSITIONS` mirrors `private.is_allowed_enrollment_status_transition` exactly:

| From | To |
| --- | --- |
| pending | active, cancelled |
| active | paused, completed, cancelled |
| paused | active, completed, cancelled |
| completed / cancelled | none |

Same-status rejected. Open = `pending|active|paused` ∧ `archived_at is null`. Terminal = `completed|cancelled`. Initial create statuses = `pending|active`. No Program `retired` confusion in Enrollment domain.

---

## 8. RPC Inventory

| Adapter constant | RPC name | Used |
| --- | --- | --- |
| `ENROLLMENT_RPC_NAMES.create` | `create_enrollment` | yes |
| `ENROLLMENT_RPC_NAMES.transitionStatus` | `transition_enrollment_status` | yes |
| `ENROLLMENT_RPC_NAMES.archive` | `archive_enrollment` | yes |
| `ENROLLMENT_RPC_NAMES.restore` | `restore_enrollment` | yes |
| `ENROLLMENT_UNSUPPORTED_RPC_NAMES.update` | `update_enrollment` | documented unsupported; never called |

All RPC adapters pass `p_organization_id: org.context.organizationId` after `resolveOrganizationContext`.

---

## 9. Direct-Grant and RLS Findings

| Control | Finding |
| --- | --- |
| Authenticated INSERT/DELETE | Revoked in migrations; foundation create uses RPC only (no `.insert`) |
| Authenticated UPDATE columns | Only `owner_member_id`, `metadata` |
| SELECT RLS | Admin includes archived; member requires `archived_at is null` |
| UPDATE RLS | owner/admin/staff + non-archived |
| History | SELECT for members; writes via private SECURITY DEFINER helper |

**Important:** Automated tests mock Supabase and **do not constitute live RLS proof**. Application code is structured to keep the normal authenticated client and membership-scoped filters; database policies remain the authoritative enforcement layer.

---

## 10. Role-Permission Findings

`resolveEnrollmentPermissions` matches the known matrix:

| Action | Owner/Admin | Staff | Viewer |
| --- | ---: | ---: | ---: |
| View non-archived | Allow | Allow | Allow |
| View archived | Allow | Deny | Deny |
| Create | Allow | Allow | Deny |
| Transition | Allow | Allow | Deny |
| Update owner/metadata | Allow | Allow | Deny |
| Archive | Allow | Deny | Deny |
| Restore | Allow | Deny | Deny |

Unknown/null/undefined roles fail closed. Mutation layer re-checks permissions before adapters. Read layer forces `archived_at is null` for staff/viewer even if `includeArchived` requested. UI hiding is correctly **not** treated as authorization evidence.

---

## 11. Create-Foundation Findings

- Uses `create_enrollment` RPC; no direct insert
- Validates UUIDs / initial status via Zod; source forced `manual`
- Organization/role from membership resolution + verified role
- Owner/Admin/Staff allowed; Viewer denied before RPC
- Customer/Program eligibility enforced by RPC (DB), mapped to safe codes
- Duplicate open mapped to `DUPLICATE_OPEN_ENROLLMENT` without SQL leakage
- Revalidation hooks prepared for future `/enrollments` paths

---

## 12. Lifecycle-Foundation Findings

- Uses `transition_enrollment_status`; no direct status column update
- Domain matrix matches DB helper; RPC remains authoritative for enforcement
- Terminal statuses have empty next transitions in domain helpers
- Owner/Admin/Staff allowed; Viewer denied
- Archived records blocked at mutation permission layer (`ARCHIVED_RECORD`) before RPC
- Adapter always sends `p_source: "manual"` for app transitions
- Milestone timestamps / history writes remain DB-owned

---

## 13. Limited Owner/Metadata-Update Findings

Extra scrutiny applied:

| Check | Result |
| --- | --- |
| Unrestricted `update_enrollment` adapter/action | Absent |
| Patch keys | Only `owner_member_id` and/or `metadata` |
| Prohibited columns in patch | org/customer/program/status/created_by/archive/timestamps not included |
| Filters | `.eq(organization_id, membership org)`, `.eq(id, enrollmentId)`, `.is(archived_at, null)` |
| Roles | Owner/Admin/Staff when non-archived; Viewer denied; archived denied |
| Schema | Strict Zod; rejects empty update; rejects extra fields |
| Service-role | Not present in feature |
| Owner org membership | Enforced by DB trigger/helper on write |

No path permitting broader Enrollment mutation was found. **No blocking security defect.**

---

## 14. Archive Findings

- Uses `archive_enrollment` RPC only
- Owner/Admin allowed; Staff/Viewer denied before RPC
- No direct `archived_at` assignment in app adapters
- Repeated/non-terminal cases mapped from RPC exceptions (`ENROLLMENT_UNAVAILABLE` / `ARCHIVE_REQUIRES_TERMINAL`)
- Lifecycle status not changed by archive RPC (DB sets `archived_at` only)

---

## 15. Restore Findings

- Uses `restore_enrollment` RPC only
- Owner/Admin on archived; Staff denied
- Clears archive only; status unchanged per RPC
- Repeated restore / not-archived mapped safely
- Duplicate-open after restore remains a DB concern if a conflicting open row exists; app maps unique-violation class errors where raised

---

## 16. Customer Relationship Findings

Create RPC (authoritative) requires same-org customer, not archived, status `onboarding|active`. App validates UUID shape and relies on RPC eligibility + org-scoped FKs. Foreign/unavailable collapse to safe customer eligibility/unavailable codes via error mapping.

---

## 17. Program Relationship Findings

Create RPC requires same-org program, not archived, status `active`. App validates UUID; eligibility errors map safely. No Program `retired` treated as Enrollment terminal.

---

## 18. Read-Foundation Findings

Loaders/queries support list, detail, history, customer/program summaries, search (org-scoped label lookups), status/customer/program/owner/archived filters, sort, pagination, counts. Authenticated client only. Staff/Viewer archived forced off. Unavailable/foreign detail → `ENROLLMENT_UNAVAILABLE`. No Progress fields selected.

---

## 19. Tenant-Isolation Findings

- Membership-derived org authority in adapters/actions/page resolver
- Browser org UUID accepted for lookup only; RPC/update use resolved org id
- List/detail/history filter by organization_id
- Search customer/program queries scoped by organization_id
- No service-role / Admin client in Enrollments foundation

---

## 20. Safe-Error Findings

`normalizeEnrollmentError` maps proven RPC messages to stable codes and user-safe copy; unique violation → duplicate open; unknown/SQL-like messages fall back to `UNEXPECTED_ERROR` without exposing constraint/SQL text in `message` (raw may remain in `cause` for server logging only — consistent with Programs pattern).

---

## 21. Route-Protection Findings

`safe-return-path.ts` allowlists and protects `/enrollments` and `/enrollments/*`. Open redirects rejected. Existing CRM paths unchanged. Tests cover protect/allowlist/regression.

---

## 22. Hidden-Navigation Findings

| Check | Result |
| --- | --- |
| `ENROLLMENTS_NAV_VISIBLE` | exactly `false` |
| AppShell gated render | present; no link when false |
| `src/app/**/enrollments/**` | absent |
| Build routes | Programs present; **no** `/enrollments` |
| Programs nav regression | `programs-navigation.test.tsx` asserts HTML does not contain `Enrollments` |

---

## 23. Test-Quality Findings

Dedicated suite (10 files) behaviorally covers status constants, transition matrix, terminal behavior, role matrix, viewer denial, unknown-role fail-closed, membership failure, validation, org-input rejection, pagination/sort, archived visibility, list/detail scoping, unavailable/foreign collapse, history, customer/program labels, create/transition/archive/restore permissions, limited update column restriction, duplicate/error mapping, unsupported update RPC documentation, hidden nav, safe return paths.

**Source-string / boundary notes:**

- `ENROLLMENT_UNSUPPORTED_RPC_NAMES.update === "update_enrollment"` is documentary; compensated by behavioral asserts that adapters never call that RPC name and limited update uses `.update` with only allowed columns.
- Create “no direct `.from`” test forces `.from` to throw and still succeeds via mocked RPC — behavioral for create path.
- **Live RLS is not proven by these mocks** (explicit QA rule observed).

Coverage is otherwise sufficient for foundation QA **except** that the typecheck gate currently fails on the RPC adapter test file (see defects).

---

## 24. Validation Results

| Command | Exit | Result |
| --- | ---: | --- |
| Dedicated 10 Enrollments foundation files | `0` | **10 files / 124 tests passed** |
| B1.5 Programs + Enrollments relevant vitest (34 files) | `0` | **263 tests passed** |
| `npm run test:run` | `0` | **178 files / 1229 tests passed** |
| `npm run typecheck` | `2` | **FAIL** — 3 errors in `tests/server/enrollment-rpc-adapters.test.ts` |
| `npm run lint` | `0` | PASS (Next deprecation notice only) |
| `npm run build` | `0` | PASS; Programs routes present; **no** `/enrollments` route |
| `git diff --check` | `0` | PASS |

### Count baseline reconciliation

| Suite | Implementation report baseline | Independent QA observed | Explanation |
| --- | ---: | ---: | --- |
| Dedicated Enrollments | 10 files / 84 tests | 10 files / **124** tests | Test files expanded in worktree after initial report; all still pass under Vitest |
| B1.5 combined | 191 | **263** | Follows expanded Enrollments tests |
| Complete suite | 178 / 1165 | 178 / **1229** | Same explanation |

Legitimate count increase; not a mismatch of product scope. **Typecheck failure remains blocking for PASS.**

### Typecheck defect detail (not fixed in QA)

1. Missing `reason` on transition `input` objects typed as inferred schema output (`reason: string | null` required after Zod transform).
2. Same missing `reason` in unsupported-RPC boundary transition call.
3. Assignment of `"teleported"` to typed `toStatus` without cast/`as never`.

Non-secret warnings: npm `devdir` config warning; Next.js `next lint` deprecation notice.

---

## 25. Scope and Secret Review

| Check | Result |
| --- | --- |
| Migration created/run | No |
| Database/Auth mutation | No |
| Env / Vercel / deploy | No |
| Visible Enrollment page/nav | No |
| Progress / AI / NBA / health / churn | No |
| Unrestricted update | No |
| Direct insert/delete | No |
| Service-role | No |
| Secrets in dirty paths | None observed |
| Build artifacts committed | No |
| Published B1.5.1–B1.5.3 reports modified | No |
| Unrelated formatting churn | No |

---

## 26. Defect Inventory

| ID | Severity | Description |
| --- | --- | --- |
| D1 | **Blocking validation** | `npm run typecheck` fails on `tests/server/enrollment-rpc-adapters.test.ts` (3 TS errors). Vitest still passes; project typecheck gate does not. |

No application security defect found that expands mutation surface beyond proven grants/RPCs.

---

## 27. Condition Inventory

| ID | Condition |
| --- | --- |
| C1 | Live RLS/grant enforcement is not proven by mocked automated tests; database remains authoritative. |
| C2 | App transition Zod schema accepts any of the five statuses as `toStatus`; impossible edges are rejected by the RPC/DB matrix (acceptable for foundation; UI phases should still offer only allowed next statuses). |
| C3 | Metadata Zod validation does not impose an explicit max size; relies on jsonb object shape + DB. |
| C4 | Customer/Program eligibility is enforced by RPC, not duplicated as app pre-checks (acceptable; errors are mapped). |
| C5 | Implementation report’s numeric test baseline (84 / 191 / 1165) is stale versus current expanded suite (124 / 263 / 1229). |

---

## 28. Manual-Testing Classification

```text
NO MANUAL BROWSER TEST REQUIRED — FOUNDATION ONLY
```

---

## 29. Publication-Readiness Conclusion

**Not ready for B1.5.4-PUB.**
Foundation design and security posture reviewed favorably, but controlled publication requires a green `npm run typecheck` (and reconfirmation of the full validation set) after correcting D1 **without** weakening typecheck or altering production authorization.

---

## 30. Exact Next Safe Phase

```text
Remediate D1 (test TypeScript typing in enrollment-rpc-adapters.test.ts only) → re-run typecheck + dedicated/full validation → then B1.5.4-PUB only if all gates pass
```

Do **not** start Enrollments UI (B1.5.5+) or flip `ENROLLMENTS_NAV_VISIBLE` until B1.5.4 publication closes.

---

## QA Process Confirmations

- Source, tests, and fixtures were **not** modified during this QA phase (only this report was added).
- No database, Auth, migration, deployment, or Vercel mutation occurred.
- No browser testing; no Enrollment create/transition/update/archive/restore against live data.
- HEAD remained `8914391a1e238715b6947326783135eea37ec522`; divergence `0 0`; staged `0`.
