# B1.6-R1-FIXTURE-3 — Enrollment Status Fixtures

| Field | Value |
| --- | --- |
| Phase | **B1.6-R1-FIXTURE-3** — Enrollment Status Fixtures |
| Parent | After **FIXTURE-2 CLOSED WITH EVIDENCE**; owner approvals **APPROVE M8 / M9 / M10** |
| Document type | Controlled enrollment-status fixture evidence |
| Date | 2026-08-04 |
| Branch | `core/platform-readiness-20260707` |
| Start HEAD | `a00be10e372a760f7bd7fb64d12ad0ad6e848b3d` |
| Parallel | **B1.6-R1-FIXTURE PARALLEL BLOCKED** |
| Product / test / schema / RLS / RPC / migration changes | **NONE** |
| Registration flag | Unchanged (fail-closed) |
| Lifecycle | **PERSISTENT QA FIXTURE** |

---

## 1. Status / verdict

```text
B1.6-R1-FIXTURE-3 CLOSED WITH EVIDENCE — ENROLLMENT STATUS FIXTURES READY
```

---

## 2. Goal and scope

Install missing **paused**, **completed**, and **archived** Enrollment fixtures under Production QA so later R1 Q4 / Progress gates can exercise status-dependent Progress contracts without inventing statuses or mutating protected controls.

In scope: Org A customers + enrollments on program `B1.5.2 QA Test Program`; official create / transition / archive / Progress RPCs; verification matrix; this evidence document.

Out of scope: product code, RLS/RPC/migrations, registration enablement, FIXTURE-VERIFY browser run, R1 resume, B1.6-PUB.

---

## 3. Initial Git baseline

| Item | Value |
| --- | --- |
| Worktree | `D:\project ai cursus verkopers.worktrees\parallel__laptop-product-track-20260707-1` |
| Branch | `core/platform-readiness-20260707` |
| HEAD / upstream | `a00be10e372a760f7bd7fb64d12ad0ad6e848b3d` |
| Divergence | `0 0` |
| Dirty inventory | clean |
| Laptop | frozen `parallel/laptop-product-track-20260707` |

Pre-mutation protected snapshot: F1-A `0` memberships; F1-B exactly one `suspended` on Org A; zynrtix **active** non-archived; lifecycle cancelled non-archived; Tenant B enrollment **active** non-archived; Org B intact.

---

## 4. Authoritative status set

Enrollment `status` values only:

| Status | Role |
| --- | --- |
| `pending` | Create-capable open |
| `active` | Open participation |
| `paused` | Open participation |
| `completed` | Terminal |
| `cancelled` | Terminal |

`archived` is **not** a status value. Soft-archive is exclusively `archived_at IS NOT NULL` via official archive/restore paths.

---

## 5. Transition matrix

Mirrors `private.is_allowed_enrollment_status_transition` / `src/features/enrollments/domain/status.ts`:

| From | Allowed to |
| --- | --- |
| `pending` | `active`, `cancelled` |
| `active` | `paused`, `completed`, `cancelled` |
| `paused` | `active`, `completed`, `cancelled` |
| `completed` | _(none)_ |
| `cancelled` | _(none)_ |

Archived enrollments: status transitions denied (`archived enrollments cannot transition status`).

---

## 6. Status-dependent Progress contract

| Enrollment state | Manual Progress **record** | Owner/Admin **correct** / **void** |
| --- | --- | --- |
| `active`, not archived | Allowed | Allowed |
| `paused`, not archived | Allowed | Allowed |
| `completed`, not archived | Denied (`enrollment status does not allow progress`) | Allowed on existing non-voided facts |
| `cancelled`, not archived | Denied (same message) | Allowed on existing non-voided facts |
| Archived (`archived_at` set) | Denied / safe not found (`enrollment not found`) | Denied / safe not found (`enrollment not found`) |

Staff may correct/void only on `active`/`paused` (RPC role gate); Owner was used for terminal correct/void proofs.

---

## 7. Chosen fixture design

| Code | Design | Label |
| --- | --- | --- |
| **F3-PAUSED** | New customer → `create_enrollment` (`active`) → `transition_enrollment_status` → `paused` | `B1.6-R1 Fixture — Enrollment paused` |
| **F3-COMPLETED** | New customer → create `active` → one Progress fact → transition → `completed` (not archived) | `B1.6-R1 Fixture — Enrollment completed` |
| **F3-ARCHIVED** | Separate customer → create `active` → Progress fact → `completed` → `archive_enrollment` | `B1.6-R1 Fixture — Enrollment archived` |

Program reused: **B1.5.2 QA Test Program** (prefix `11e95736…`). Open-participation uniqueness preserved via distinct customers.

Residual shell (not a primary F3 code): after void-proof on the first completed enrollment, that terminal row was archived via `archive_enrollment` and customer display renamed to `B1.6-R1 Fixture — Enrollment completed (void-proof shell)` so the live **F3-COMPLETED** control could be reseeded with one retained non-voided fact. Primary archived control remains **F3-ARCHIVED**.

---

## 8. Masked fixture matrix (final)

| Code | Status | Archived | Enrollment | Customer | Program | Active Progress |
| --- | --- | --- | --- | --- | --- | --- |
| F3-PAUSED | `paused` | no | `aca64f96…` | `5aee8836…` | `11e95736…` | `3ab216fd…` manual |
| F3-COMPLETED | `completed` | no | `78dd21b6…` | `2bfed755…` | `11e95736…` | `6a061e86…` correction (predecessor voided by official correct) |
| F3-ARCHIVED | `completed` | **yes** | `a7226c87…` | `58b20456…` | `11e95736…` | `ec4ec46d…` manual (immutable under archive) |

Full UUIDs: operator password manager only. Docs use masked prefixes only.

Protected controls (unchanged):

| Control | Final |
| --- | --- |
| zynrtix active | `active`, non-archived (`e405c5c8…`) |
| B1.5.6 Lifecycle cancelled | `cancelled`, non-archived (`1bd8138a…`) |
| Tenant B enrollment | `active`, non-archived (`e63562aa…`); fact `4cb0c129…` non-voided |

---

## 9. Create / transition / archive paths executed

All under Org A owner JWT claim (`set_config` request subject) calling existing security-definer RPCs only:

1. `public.create_customer`
2. `public.create_enrollment` (`p_initial_status = active`, `manual`)
3. `public.record_progress_fact` (where required before terminal/archive)
4. `public.transition_enrollment_status`
5. `public.archive_enrollment` (F3-ARCHIVED; also void-proof shell only)

No ad-hoc `status` / `archived_at` writes as lifecycle substitutes. No product/RLS/RPC/migration edits.

---

## 10. Progress read/write results

| # | Check | Result | Exact RPC semantics observed |
| --- | --- | --- | --- |
| 1 | Active control manual write | **PASS** | `record_progress_fact` + `void_progress_fact` cleanup on zynrtix (disposable keys); control remains `active` with prior fact intact |
| 2 | Paused fixture manual write | **PASS** | Record accepted; idempotent re-call returns existing fact |
| 3 | Completed: no new manual record | **PASS** | `enrollment status does not allow progress` |
| 4 | Cancelled control: no new manual record | **PASS** | `enrollment status does not allow progress` |
| 5 | Owner correct/void on completed facts | **PASS** | Correct via `record_progress_fact(..., p_corrected_from_fact_id)`; void via `void_progress_fact`; live F3-COMPLETED retains correction fact `6a061e86…` |
| 6 | Archived non-writable | **PASS** | Record and void → `enrollment not found` |
| 7 | Invalid transitions | **PASS** | See §11 |
| 8 | Terminal cannot reactivate | **PASS** | `status transition not allowed` |
| 9 | Foreign Enrollment ID | **PASS** | Org A org + Tenant B enrollment → `enrollment not found` |
| 10 | Foreign Progress ID | **PASS** | Org A org + Tenant B fact → `progress fact not found` |
| 11 | Org A actor on Org B | **PASS** | `active organization membership required` |
| 12 | Tenant B actor on Org A | **PASS** | `active organization membership required` / foreign enrollment `enrollment not found` |

---

## 11. Invalid transition probes

| Probe | Result message |
| --- | --- |
| completed → active | `status transition not allowed` |
| cancelled → active | `status transition not allowed` |
| paused → pending | `status transition not allowed` |
| archived → paused | `archived enrollments cannot transition status` |

---

## 12. Foreign-ID and cross-tenant probes

| Actor | Target | Outcome |
| --- | --- | --- |
| Org A owner | Org A scope + Tenant B enrollment | `enrollment not found` |
| Org A owner | Org A scope + Tenant B progress fact | `progress fact not found` |
| Org A owner | Org B scope + Tenant B enrollment | `active organization membership required` |
| Tenant B owner | Org A scope + Org A paused enrollment | `active organization membership required` |
| Tenant B owner | Org B scope + Org A enrollment ID | `enrollment not found` |

No cross-tenant data returned in success payloads.

---

## 13. F1-A / F1-B / Tenant B regression

| Check | Result |
| --- | --- |
| F1-A memberships | **0** |
| F1-B total memberships | **1** |
| F1-B suspended on Org A | **1** |
| F1-B on audit-org / Org B | **0** |
| Org A users also in Org B | **0** |
| Tenant B owner active owner only on Org B | **PASS** |
| Tenant B enrollment / fact | Intact (`e63562aa…` / `4cb0c129…`) |

---

## 14. Proof existing controls were not mutated

| Control | Pre | Post |
| --- | --- | --- |
| zynrtix | active, non-archived | unchanged (disposable verify facts soft-voided only) |
| Lifecycle cancelled | cancelled, non-archived, 0 facts | unchanged; deny-new only |
| Tenant B | active enrollment + 1 fact | unchanged |
| F1-A / F1-B | as FIXTURE-1 | unchanged |
| Org B | as FIXTURE-2 | unchanged |

---

## 15. Secret and identifier control

| Rule | Result |
| --- | --- |
| Full UUIDs / emails / tokens / credentials in Git | **None** (masked prefixes only) |
| Full UUIDs in chat/docs | Avoided; operator password manager for operational IDs |
| This commit contents | Docs-only evidence file |

---

## 16. Lifecycle and incident procedure

| Item | Policy |
| --- | --- |
| Lifecycle | **PERSISTENT QA FIXTURE** — retain paused / completed / archived controls |
| Credentials | N/A for these customers (no Auth identities created in F3) |
| Progress facts | Soft-void only; no hard delete |
| Incident | If a protected control is mutated, stop and reopen FIXTURE track; do not “fix” with ad-hoc status SQL |
| Retirement | Requires separate owner approval |

---

## 17. Evidence paths

- This document: `docs/phases/B1.6-R1-FIXTURE-3-enrollment-status-fixtures.md`
- Hosted DB labels under Org A slug `zyntixai-production-qa`
- Prior: `docs/phases/B1.6-R1-FIXTURE-2-second-tenant-isolation-fixtures.md`
- Prior: `docs/phases/B1.6-R1-FIXTURE-1-membership-state-qa-identities.md`
- Contract sources: `src/features/enrollments/domain/status.ts`; Progress RPCs in `supabase/migrations/20260707190002_add_progress_core_rpcs.sql`

---

## 18. Dashboard

```text
FIXTURE-3 COMPLETION: 100%

Authoritative baseline: PASS
Approvals M8/M9/M10 + PERSISTENT QA FIXTURE: PASS
F3-PAUSED: PASS
F3-COMPLETED: PASS
F3-ARCHIVED: PASS
Progress status matrix: PASS
Invalid transitions: PASS
Foreign / cross-tenant probes: PASS
F1-A / F1-B / Tenant B / protected controls: PASS
Docs masked IDs only: PASS
Product/RLS/RPC changes: NONE
Secrets in Git: 0
Open critical blockers: 0
Final verdict: B1.6-R1-FIXTURE-3 CLOSED WITH EVIDENCE — ENROLLMENT STATUS FIXTURES READY
```

---

## 19. Exact next step

```text
B1.6-R1-FIXTURE-VERIFY — Fixture Readiness Verification
```

Do **not** start FIXTURE-VERIFY, R1 resume, or B1.6-PUB automatically from this closure.
