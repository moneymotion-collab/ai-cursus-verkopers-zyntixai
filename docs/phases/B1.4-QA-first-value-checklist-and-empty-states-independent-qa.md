# B1.4-QA — First-Value Checklist and Empty States Independent QA

| Field | Value |
| --- | --- |
| Phase | B1.4-QA — First-Value Checklist and Empty States Independent QA |
| Parent track | B1 — Controlled Beta Entry |
| Status | Independent QA complete |
| Baseline HEAD | `071169fc3e5ad8e3375a07d9b8b77c88f88119e1` |
| Branch | `core/platform-readiness-20260707` |
| Authoritative contract | `docs/phases/B1.4-first-value-checklist-and-empty-states-design-and-readiness-contract.md` |
| Production / Supabase mutation | **NO** |
| Commit / push / deploy | **NO** |

---

## 1. Executive Verdict

```text
PASS WITH CONDITIONS — READY FOR B1.4-R1
```

Independent adversarial review of the uncommitted B1.4 implementation confirms contract-aligned checklist derivation, owner/admin visibility, staff/viewer exclusion, org-scoped count queries, idempotent dismiss wiring, Leads empty-state CTA behavior, and freeze of auth / middleware / B1.3 Step 2 remediation surfaces. Automated gates were re-run fresh and passed. Conditions below are non-blocking and do not prevent controlled production verification.

---

## 2. Verified Git Baseline

| Item | Evidence |
| --- | --- |
| Worktree | `C:\project ai cursus verkopers.worktrees\laptop-current-20260722` |
| Branch | `core/platform-readiness-20260707` |
| HEAD | `071169fc3e5ad8e3375a07d9b8b77c88f88119e1` — `docs(onboarding): close B1.3 production verification` |
| Upstream | `origin/core/platform-readiness-20260707` |
| Divergence | `0 0` |
| Staging | Nothing staged |
| Production mutation | None performed in this QA phase |

### Changed-path counts

| Moment | Unique changed paths |
| --- | ---: |
| QA start (implementation state) | **13** |
| After loader isolation test additions (same test file) | **13** |
| After this QA report | **14** |

Budget: preferred maximum 15; final **14** (within range). No product remediation required.

---

## 3. Scope Compliance

| Check | Result |
| --- | --- |
| Every changed path necessary for B1.4 | **PASS** |
| Tasks empty-state redesign | **NONE** |
| Customers empty-state redesign | **NONE** (soft link in checklist only) |
| Broad dashboard / CRM refactor | **NONE** |
| Migrations / RPC | **NONE** |
| Auth / middleware / registration / recovery | **NONE** |
| Onboarding wizard Step 1–3 / Step 2 remediation files | **UNTOUCHED** |
| Unrelated formatting / cleanup | **NONE** |
| Generated secrets / env files in status | **NONE** |

`onboarding-actions.ts` was modified only to append dismiss wiring; draft/complete action bodies are unchanged (diff-reviewed).

---

## 4. Checklist Derivation Findings

| Requirement | Finding |
| --- | --- |
| Owner eligible visibility | **PASS** — pure derivation + page gate on `visible` |
| Admin eligible visibility | **PASS** |
| Staff hidden | **PASS** — derivation + loader short-circuit `null` |
| Viewer hidden | **PASS** |
| Ineligible roles skip count queries | **PASS** — proven by added loader tests |
| Incomplete onboarding hides checklist | **PASS** — requires `onboarding_completed_at` |
| Dismissed hides checklist | **PASS** |
| Fully complete hides checklist | **PASS** — `completedCount < 3` |
| Company setup uses onboarding completion | **PASS** — `Boolean(onboardingCompletedAt)` |
| First lead = org-scoped non-archived | **PASS** — `.eq(organization_id).is(archived_at, null)` |
| Archived leads do not complete | **PASS** (by query filter + derivation flags) |
| First task = org-scoped non-archived | **PASS** |
| Archived tasks do not complete | **PASS** |
| Customer soft link non-gating | **PASS** — not in `completedCount` / visibility gate |

Loader uses organization id from already-resolved `loadLeadsPage` / `resolveLeadPageOrganization` context, then re-filters all CRM counts by that id.

---

## 5. Role and Tenant Isolation Findings

| Check | Result |
| --- | --- |
| Checklist reads use resolved active org | **PASS** (caller passes `orgResult.organizationId`) |
| Dismiss rejects foreign org id | **PASS** — `resolveOnboardingOrganizationId` membership match; unit-proven |
| Counts cannot target another org via loader params alone without that id being passed | **PASS** — queries bind the provided resolved id; isolation tests assert ORG_A only |
| Staff/viewer never hit leads/tasks count tables | **PASS** — added QA tests |

**Gap closed during QA:** loader short-circuit / org-scoped query assertions were missing; four tests were added to `tests/onboarding/first-value-checklist.test.ts` (no new product files).

---

## 6. Dismiss Authorization / Idempotency / RLS Findings

Full path inspected: panel `onDismiss` → `dismissFirstValueChecklistAction` → Zod UUID + strict object → `dismissFirstValueChecklist` → membership resolve → owner/admin gate → read existing dismiss → optional update of **only** `first_run_checklist_dismissed_at` with `.is(..., null)` → `revalidatePath("/leads")` on success → client `router.refresh()`.

| Check | Result |
| --- | --- |
| Owner/admin may dismiss | **PASS** |
| Staff/viewer structured `forbidden` | **PASS** |
| Arbitrary org id not trusted | **PASS** — membership resolution required |
| Update payload single-column | **PASS** |
| Idempotent preserve timestamp | **PASS** — no update when already set |
| Safe user-facing errors | **PASS** — fixed message map; no raw PostgREST |
| `revalidatePath` limited to `/leads` | **PASS** |
| Pending guard against duplicate dismiss | **PASS** — `pendingRef` + disabled button |

### RLS caveat (condition)

Repository evidence shows `organizations_update_admin` allows owner/admin UPDATE. **Unit mocks are not equivalent to live Supabase RLS verification.** Controlled B1.4-R1 should confirm dismiss succeeds for owner/admin and fails for staff under real RLS without mutating unrelated production tenants beyond an approved QA identity.

---

## 7. onboarding-actions.ts Freeze Findings

Diff is strictly additive:

- Imports for `revalidatePath`, `z`, checklist dismiss helper/types
- `dismissChecklistInputSchema`
- New `dismissFirstValueChecklistAction`

Unchanged: `getOnboardingContextAction`, `saveOnboardingDraftAction`, `completeOnboardingAction`, existing Zod field-error helper behavior for draft/complete.

**Verdict:** B1.2/B1.3 draft/complete action contract preserved.

---

## 8. B1.3 Step 2 Regression Findings

| Surface | Status |
| --- | --- |
| `src/app/onboarding/page.tsx` `force-dynamic` | Present; **not in diff** |
| Wizard `router.refresh()` after draft | Present; **not in diff** |
| Remount / Step 2 persistence tests | `onboarding-step2-value-preservation.test.tsx` **12/12 PASS** |
| Onboarding UI / server suites | **PASS** |

No behavioral change to Step 2 refresh, dynamic rendering, rehydration, or remount remediation.

---

## 9. Leads Empty-State Findings

| Check | Result |
| --- | --- |
| Unfiltered empty shows “Add your first lead” when create allowed | **PASS** |
| Uses `buildLeadCreateHref` + `canShowCreateLeadWorkflow` | **PASS** |
| Viewer omits CTA | **PASS** |
| Filtered / archived empty omit first-ever CTA | **PASS** — early branches set no `actionHref` |
| Non-empty list unchanged | **PASS** — presentation + existing list tests |
| Filters / pagination / archive / “Lead” terminology | **PASS** — no contract-breaking edits |

Description copy strengthened for unfiltered empty only; archived/filtered copy preserved.

---

## 10. Accessibility Findings

| Check | Result |
| --- | --- |
| Dismiss is a real `<button type="button">` | **PASS** |
| Item CTAs / soft link are real `<a>` | **PASS** |
| Checklist region `aria-labelledby` + progress text | **PASS** |
| Failure `role="alert"` | **PASS** |
| No nested interactive controls | **PASS** |
| CSS modules locally scoped | **PASS** |
| Focus-visible styles on dismiss / empty CTA / links | **PASS** |
| Tap target ≥ 44px intent (`min-height: 2.75rem`) | **PASS** |

**Non-blocking condition:** item “Done / To do” status text is adjacent to labels but not programmatically bound via `aria-describedby` / single accessible name. Screen-reader users still hear both status and label in list order; improve later if needed. Not a contract blocker.

---

## 11. Defects and Remediation

| Severity | Item | Action |
| --- | --- | --- |
| Coverage gap | Loader short-circuit / org-scoped query paths untested | **Remediated** — 4 tests added to existing `tests/onboarding/first-value-checklist.test.ts` |
| Product defect | None proven | **No product code changes in QA** |

---

## 12. Automated Verification (fresh QA run)

| Gate | Result |
| --- | --- |
| Focused B1.4 + related onboarding/leads | **55/55 PASS** (includes 19 checklist tests) |
| Typecheck | **PASS** |
| Lint | **PASS** |
| Full suite | **145 files / 982 tests PASS** (+4 vs implementation report) |
| Production build | **PASS** — `ƒ /onboarding`, `ƒ /leads` |
| Post-test git inventory | Still implementation + QA report only; no surprise files |

---

## 13. Complete Changed-File Inventory (ending state)

### Documentation (2)

- `docs/phases/B1.4-first-value-checklist-and-empty-states-design-and-readiness-contract.md` (pre-existing untracked)
- `docs/phases/B1.4-QA-first-value-checklist-and-empty-states-independent-qa.md` (**this report**)

### Modified product/source (5)

- `src/app/(authenticated)/leads/page.tsx`
- `src/features/leads/ui/lead-list.tsx`
- `src/features/leads/ui/lead-list.module.css`
- `src/features/leads/ui/load-leads-page.ts`
- `src/features/onboarding/actions/onboarding-actions.ts`

### New product/source (5)

- `src/features/onboarding/domain/first-value-checklist.ts`
- `src/features/onboarding/server/load-first-value-checklist.ts`
- `src/features/onboarding/server/dismiss-first-value-checklist.ts`
- `src/features/onboarding/ui/first-value-checklist-panel.tsx`
- `src/features/onboarding/ui/first-value-checklist-panel.module.css`

### Tests (2)

- `tests/onboarding/first-value-checklist.test.ts` (modified during QA — isolation cases)
- `tests/ui/first-value-checklist-ui.test.tsx`

### Migrations / deleted

- **0** / **0**

**Total unique changed paths: 14**

---

## 14. Remaining Assumptions

1. Live `organizations_update_admin` RLS behavior for dismiss is **not** re-proven here (mocks ≠ live RLS).
2. Production deployment still matching B1.3-PUB is **historical documentation**, not re-inspected in B1.4-QA.
3. Browser QA against local/production UI remains for B1.4-R1 / controlled verification, not this phase.

---

## 15. Final Verdict

```text
PASS WITH CONDITIONS — READY FOR B1.4-R1
```

Conditions: live RLS dismiss verification; optional a11y status/label binding polish; production browser journeys still required before publication.

---

## 16. Exact Next Step

```text
NEXT SAFE PHASE:
B1.4-R1 — Controlled Production First-Value Checklist and Empty States Verification
```

Do not commit, push, deploy, or mutate production from this QA phase without a separate explicit authorization.
