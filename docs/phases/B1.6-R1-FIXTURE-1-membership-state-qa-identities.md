# B1.6-R1-FIXTURE-1 — Membership-State QA Identities

| Field | Value |
| --- | --- |
| Phase | **B1.6-R1-FIXTURE-1** — Membership-State QA Identities |
| Parent | `B1.6-R1-FIXTURE-PREFLIGHT PASS WITH RESTRICTIONS` |
| Document type | Controlled fixture implementation evidence (**blocked before Auth mutation**) |
| Date | 2026-08-03 |
| Branch | `core/platform-readiness-20260707` |
| Start HEAD | `220e4f9aa113dd7dceb7d245de17271b5071f3e4` |
| Parallel | **B1.6-R1-FIXTURE PARALLEL BLOCKED** |
| Auth/membership mutations in this phase | **NONE** |
| Product / test / schema / RLS / RPC changes | **NONE** |

---

## 1. Executive verdict

```text
B1.6-R1-FIXTURE-1 BLOCKED — APPROVAL REQUIRED
```

F1-A and F1-B were **not** created. Hosted QA organization ownership is proven. The Auth Admin create path and the targeted `suspended` membership update path are designable, but **mandatory operational approvals and handoff inputs are incomplete**. No credentials were generated, stored, or printed. Q8 was not executed. B1.6-R1 remains blocked on all three prior gaps.

---

## 2. Git baseline

| Item | Value |
| --- | --- |
| Worktree | `D:\project ai cursus verkopers.worktrees\parallel__laptop-product-track-20260707-1` |
| Branch | `core/platform-readiness-20260707` |
| HEAD / upstream at start | `220e4f9aa113dd7dceb7d245de17271b5071f3e4` |
| Divergence | `0 0` |
| Dirty inventory at start | clean |
| Last commit at start | `220e4f9 docs(progress): define B1.6 R1 fixture readiness` |
| Remotes | `origin` → `https://github.com/moneymotion-collab/ai-cursus-verkopers-zyntixai.git` |
| Laptop worktree | `D:\project ai cursus verkopers` @ `parallel/laptop-product-track-20260707` / `3cc8ce5559f66b049e53a31ffea9df45ea3d0490` — frozen; no parallel F1 work |
| Local app runtime | Not started (not required for this blocker) |

Forbidden git operations were not used.

---

## 3. Binding sources

| Source | Use |
| --- | --- |
| `docs/governance/B1-GATE.1-100-percent-phase-completion-and-evidence-standard.md` | Evidence / stop rules |
| `docs/phases/B1.6-R1-FIXTURE-design-identity-tenant-status-readiness.md` | F1 contract, approvals M1–M4, admin procedure, cleanup |
| `docs/phases/B1.6-QA-full-security-and-regression-closure-readiness.md` | Q8 role matrix |
| `docs/phases/B1.6-QA-full-security-and-regression-closure.md` | Automated missing/inactive coverage (not a Q8 substitute) |
| Migrations / resolvers | `organization_members.status` ∈ `invited\|active\|suspended\|removed`; product accepts only `active` |
| `docs/phases/B1.1-R1-ACCESS-…` | Precedent: Management API keys in-memory; never print secrets; mailbox omitted from Git |

---

## 4. Environment

| Item | Result |
| --- | --- |
| Classification | **ALLOWED WITH RESTRICTIONS** (preflight) |
| Hosted project | Supabase project linked for Production QA backend |
| Organization A | **ZyntixAI Production QA** / slug `zyntixai-production-qa` / status **active** |
| Ownership proof | Read-only SQL confirmed exact QA-labelled active org |
| Production alias used for mutations | **NO** |
| `PUBLIC_REGISTRATION_ENABLED` flipped | **NO** |
| Ad-hoc untargeted SQL | **NO** |
| Service-role key printed / committed | **NO** |

```text
QA environment ownership: PASS
```

---

## 5. Approvals

Owner issued the FIXTURE-1 execution prompt. That is treated as **intent to run the phase**, not as a complete checklist for every M-gate / A–F gate.

| Approval | Status | Approver | Bewijs aanwezig | Actie toegestaan |
| --- | --- | --- | --- | --- |
| A — QA Auth-user creation (two users) | **PARTIAL** | Product owner (execution prompt) | Phase authorized; **controlled mailbox addresses not supplied** | **NO** create yet |
| B — Missing-membership negative fixture | **PARTIAL** | Product owner | Intent yes; mailbox + storage incomplete | **NO** |
| C — Suspended membership (`suspended`) | **PARTIAL** | Product owner | Intent yes; user/membership not created | **NO** |
| D — Admin membership update to `suspended` | **PARTIAL** | Product owner | Procedure available (targeted update); blocked pending C | **NO** |
| E — Credential storage outside Git/chat | **MISSING** | Product owner | No designated password-manager location / handoff confirmation in evidence | **NO** |
| F — Persistent vs temporary lifecycle | **MISSING** | Product owner | Preflight default suggested persistent; **explicit F decision not recorded in this phase** | **NO** |

Preflight M-gates still open for mutation:

| ID | Status |
| --- | --- |
| M1 Production QA fixture mutations | Not cleared with explicit written OK beyond phase intent |
| M2 F1-A Auth user | Blocked — mailbox + E/F |
| M3 F1-B Auth user + suspend | Blocked — mailbox + E/F |
| M4 Password-manager confirmation | **Missing** |

```text
B1.6-R1-FIXTURE-1 BLOCKED — APPROVAL REQUIRED
```

### Unblock package (operator — no secrets in chat)

Provide **exactly** (roles/labels only in chat; addresses via approved private channel if needed):

1. Explicit lines: `APPROVE A`, `APPROVE B`, `APPROVE C`, `APPROVE D`, `APPROVE E`, `APPROVE F`
2. For **E**: storage class confirmation, e.g. `CREDENTIAL STORE: operator password manager` (no vault paths that leak org secrets)
3. For **F**: exactly one of `LIFECYCLE: PERSISTENT NEGATIVE QA FIXTURE` or `LIFECYCLE: TEMPORARY — REVOKE AFTER R1`
4. For **A**: two controlled QA mailbox identifiers (or authorize plus-aliases on the existing approved QA mailbox **without** pasting the base address into chat if preferred — operator may create users in Auth dashboard instead)
5. Optional: `AUTH CREATE PATH: dashboard` **or** `AUTH CREATE PATH: admin-api` (desktop operator runs Admin API with in-memory key; agent must not print keys)

---

## 6. Admin / Auth procedure readiness (not executed)

### 6.1 Auth user creation

| Check | Result |
| --- | --- |
| Public registration | Remains fail-closed — **must stay off** |
| Product owner self-registration | Creates Organization + owner membership — **forbidden** for F1-A/F1-B |
| Invite UI | Not present in product — **not used** |
| Auth Admin / Dashboard create user | **Required** path per preflight |
| Agent session `SUPABASE_SERVICE_ROLE_KEY` / `SUPABASE_ACCESS_TOKEN` in env | **Absent** |
| Supabase CLI linked to correct project | **Yes** (api-keys available with `--reveal` — **not invoked** this phase to avoid secret exposure in transcripts) |
| MCP `execute_sql` | Suitable for **read** + **targeted membership status update**; **not** a substitute for Auth Admin user+password creation |

### 6.2 F1-A postconditions (target — unmet)

```text
Auth-user exists: NOT CREATED
Active organization membership: N/A (no user)
Suspended organization membership: N/A
Access to QA tenant: N/A
Indirect organization access: N/A
Credentials stored in Git/chat: NO (none created)
```

```text
F1-A BLOCKED — awaiting approvals / mailbox / credential handoff
```

(Not `AUTOMATIC PROVISIONING CANNOT BE SAFELY AVOIDED` — avoidance path exists; inputs missing.)

### 6.3 F1-B postconditions (target — unmet)

Current hosted snapshot (read-only): **0** `suspended` memberships on Organization A; **0** users whose only membership statuses are `{suspended}`.

Target after approved execution:

```text
Auth-user exists: YES
Membership exists: YES
Membership organization: ZyntixAI Production QA
Membership status: suspended
Additional memberships: NO
Access should be denied by contract: YES
Credentials stored in Git/chat: NO
Reactivation/cleanup path: DEFINED
```

Planned admin update (when approved): targeted `organization_members` row for Org A only, `active` → `suspended`, executor = desktop Auth/ops under Approval D. Reactivation: reverse update to `active` or revoke Auth user per F.

```text
F1-B BLOCKED — SUSPENDED MEMBERSHIP ADMIN PROCEDURE WAITING ON APPROVALS
```

(Procedure itself is **available**; not `… PROCEDURE UNAVAILABLE`.)

---

## 7. Credential handoff (not established)

| Fixture | Credential owner | Storage class | Handoff ready | Secret documented |
| --- | --- | --- | --- | --- |
| Missing membership | Desktop operator | **unconfirmed** | **no** | NO |
| Suspended membership | Desktop operator | **unconfirmed** | **no** | NO |

No passwords, emails, tokens, cookies, or magic links were created or written to Git/Markdown/chat.

Related classification: **FIXTURE CREDENTIAL GAP** / **FIXTURE APPROVAL GAP** (Approval E).

---

## 8. External effects

| Effect | Classification | This phase |
| --- | --- | --- |
| Production registration emails | MUST BE SUPPRESSED | Registration not enabled |
| Auth Admin create / invite mail | REQUIRES APPROVAL | Not executed |
| Stripe | NO EXTERNAL EFFECT | — |
| Customer notifications | FORBIDDEN | None |
| Audit logs | SAFE QA EFFECT | Read-only SQL only |

```text
External effects: PASS (no mutating Auth actions performed)
```

---

## 9. Security boundaries

| Check | Result |
| --- | --- |
| RLS changes | NONE |
| Policy bypass | NONE |
| Auth guards disabled | NONE |
| Registration flag | unchanged / fail-closed |
| Service-role in repository | NONE |
| Generic admin script added | NONE |
| Membership created for missing identity | NONE |
| Extra / wrong-tenant membership | NONE |
| Real customer tenant used | NONE |
| Cross-tenant relation created | NONE |

```text
F1 SECURITY BOUNDARIES PASS
```

(No weakening; fixtures also not yet ready.)

---

## 10. Ownership (assigned in preflight; mutation not started)

| Fixture | Data owner | Technical creator | Approver | Credential owner | Cleanup owner |
| --- | --- | --- | --- | --- | --- |
| Missing membership | Desktop product owner | Desktop Auth/ops (pending) | Product owner | Desktop operator | Desktop operator |
| Suspended membership | Desktop product owner | Desktop Auth/ops (pending) | Product owner | Desktop operator | Desktop operator |

Ownership roles are assigned → not `FIXTURE OWNERSHIP GAP`. Execution blocked on approvals/handoff, not ownership assignment.

---

## 11. Cleanup / lifecycle

Not applied (no identities created). When F is approved, default recommendation remains:

```text
PERSISTENT NEGATIVE QA FIXTURE
```

unless owner selects temporary revoke-after-R1.

---

## 12. Readiness matrix

| Controle | Missing membership | Suspended membership |
| --- | --- | --- |
| Auth-user bestaat | FAIL | FAIL |
| Credentials veilig opgeslagen | FAIL | FAIL |
| Geen credentials in Git/chat | PASS | PASS |
| Juiste organizationrelatie | geen (n/a) | FAIL (not created) |
| Membership aanwezig | nee | nee (required: ja) |
| Membershipstatus | N/A | FAIL (required: suspended) |
| Extra memberships | nee | nee |
| Indirecte tenanttoegang | nee | nee |
| External effects veilig | PASS | PASS |
| Cleanup/lifecycle gedefinieerd | FAIL (F open) | FAIL (F open) |
| Geschikt voor Q8 | FAIL | FAIL |

Q8 **not** executed; Q8 **not** marked PASS.

---

## 13. Limited runtime control

| Item | Result |
| --- | --- |
| Local `build`/`start` | **Not started** |
| Production alias | **Not used** |
| Sourcecommit | `220e4f9aa113dd7dceb7d245de17271b5071f3e4` |

---

## 14. Defect / gap inventory

| Class | Item |
| --- | --- |
| FIXTURE APPROVAL GAP | Approvals E and F missing; A–D incomplete without mailboxes |
| FIXTURE CREDENTIAL GAP | No approved storage/handoff confirmation |
| CLOSED WITH CONTRACT EVIDENCE | QA org ownership; `suspended` is authoritative status; no product UI for suspend |
| FALSE POSITIVE | None |
| Product defect | **None identified** — stop is fixture/ops readiness |

Remaining B1.6-R1 blockers (unchanged):

```text
B1.6-R1 BLOCKED — QA CREDENTIAL OR ROLE GAP
B1.6-R1 BLOCKED — SECOND-TENANT VERIFICATION GAP
B1.6-R1 BLOCKED — QA STATUS FIXTURE GAP
```

Only the first is in scope for FIXTURE-1; it remains open.

---

## 15. Dashboard

```text
FIXTURE-1 COMPLETION: 25%

Authoritative baseline: PASS
Approval gates: FAIL
QA environment ownership: PASS
Missing membership Auth-user: FAIL
Missing membership postconditions: FAIL
Suspended membership Auth-user: FAIL
Suspended membership creation: FAIL
Suspended status verification: FAIL
Credential storage: FAIL
Credential handoff readiness: FAIL
External effects: PASS
Security boundaries: PASS
Fixture ownership: PASS
Cleanup/lifecycle contract: FAIL
Limited runtime readiness: PASS
Documentation: PASS
Publication: PASS
Productcode changes: NONE
Testcode changes: NONE
Schema/RLS/RPC changes: NONE
Secrets exposed: 0
Open critical blockers: 1
Open mandatory conditions: 6
Accepted polish items: 0
Final verdict: B1.6-R1-FIXTURE-1 BLOCKED — APPROVAL REQUIRED
```

Open critical blocker: missing Approvals E/F (+ operational mailbox/handoff inputs for A–D).
Open mandatory conditions: Approvals A–F completion package in §5.

---

## 16. Exact next step

Do **not** start FIXTURE-2, FIXTURE-3, VERIFY, or B1.6-R1 resume.

Operator action:

```text
Supply APPROVE A–F + credential-store confirmation + lifecycle choice + controlled QA mailboxes (or dashboard Auth-create commitment)
```

Then re-run:

```text
B1.6-R1-FIXTURE-1 — Membership-State QA Identities
```

After F1 PASS, still required later:

```text
B1.6-R1-FIXTURE-2 — Second-Tenant Isolation Fixtures
B1.6-R1-FIXTURE-3 — Enrollment Status Fixtures
B1.6-R1-FIXTURE-VERIFY — Fixture Readiness Verification
```
