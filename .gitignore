# dependencies
/node_modules

# env files
.env
.env.*
!.env.example

# next.js
/.next/
/out/
tsconfig.tsbuildinfo

# supabase local state
.supabase/
supabase/.temp/
supabase/.branches/

# superseded draft migrations (active history is in supabase/migrations/)
supabase/pending_foundation/

# misc
.DS_Store
*.pem
npm-debug.log*

# local Vercel link metadata (never commit)
.vercel/

# Playwright / authenticated browser QA (never commit secrets or local auth state)
/playwright/.auth/
/playwright-report/
/test-results/
/blob-report/
/playwright/.cache/
